---
plan: "01-05"
title: "Social Auth — Google + Apple Sign-In Buttons, Mock OAuth Flow"
wave: 3
depends_on: ["01-03"]
requirements: [AUTH-04]
files_modified:
  - src/components/common/SocialAuthButton.tsx
  - src/screens/auth/SignUpScreen.tsx
  - src/screens/auth/LoginScreen.tsx
autonomous: true

must_haves:
  truths:
    - "Google Continue with Google button appears on Sign Up and Login screens, tapping it triggers mock Google sign-in and navigates to AppTabs"
    - "Continue with Apple button appears on Sign Up and Login screens on iOS only; on Android the button is absent"
    - "Apple Sign-In uses expo-apple-authentication AppleAuthenticationButton (App Store compliant UI)"
    - "Social auth cancellation (user dismisses Google/Apple dialog) shows no error — silently returns to form"
    - "Social auth mock success stores a mock token to SecureStore and routes to AppTabs via AuthContext.signIn"
  artifacts:
    - path: "src/components/common/SocialAuthButton.tsx"
      provides: "Google social auth button component — outlined full-width row with icon and label"
    - path: "src/screens/auth/SignUpScreen.tsx"
      provides: "Updated SignUpScreen with wired Google + Apple handlers replacing the no-op stubs"
    - path: "src/screens/auth/LoginScreen.tsx"
      provides: "Updated LoginScreen with wired Google + Apple handlers replacing the no-op stubs"
  key_links:
    - from: "src/screens/auth/SignUpScreen.tsx (and LoginScreen.tsx)"
      to: "src/context/AuthContext.tsx"
      via: "authContext.signIn('mock-google-token') after mock Google success"
      pattern: "signIn.*mock.*token"
    - from: "src/screens/auth/SignUpScreen.tsx (and LoginScreen.tsx)"
      to: "expo-apple-authentication"
      via: "AppleAuthentication.signInAsync() wrapped in Platform.OS==='ios' guard"
      pattern: "AppleAuthentication\\.signInAsync"
---

<objective>
Wire the social auth buttons that were stubbed in Plans 01-03 and 01-04. Google sign-in: renders a custom outlined SocialAuthButton, triggers mock GoogleSignin flow, stores a mock token, routes to AppTabs. Apple sign-in: renders Apple's required AppleAuthenticationButton (iOS-only), triggers expo-apple-authentication.signInAsync(), stores a mock token, routes to AppTabs.

Purpose: Delivers AUTH-04. Social auth uses mock flows in Phase 1 — no real Google Cloud Console credentials or Apple developer entitlements are needed. The UI is fully functional and the code structure is ready to swap in real credentials when the backend integration phase arrives.

Output: SocialAuthButton component for Google. Both SignUpScreen and LoginScreen updated with wired Google and Apple handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-UI-SPEC.md
@.planning/phases/01-foundation-and-authentication/01-03-SUMMARY.md
@.planning/phases/01-foundation-and-authentication/01-04-SUMMARY.md

<interfaces>
From src/context/AuthContext.tsx (created in 01-01):
  useAuthContext(): { state, signIn, signOut, signUp }
  signIn(token: string): Promise<void>

From src/theme/theme.ts (created in 01-01):
  DSColors.surface, DSColors.border, DSColors.textPrimary, DSColors.error, DSColors.primary

From RESEARCH.md Pattern 6 — Social Auth with Platform Guard:
  expo-apple-authentication:
    import * as AppleAuthentication from 'expo-apple-authentication'
    AppleAuthentication.AppleAuthenticationButton component (required by App Store guidelines)
    buttonType: AppleAuthentication.AppleAuthenticationButtonType.SIGN_IN
    buttonStyle: AppleAuthentication.AppleAuthenticationButtonStyle.BLACK
    cornerRadius: 8
    style: { width: '100%', height: 44 }
    signInAsync requestedScopes: FULL_NAME, EMAIL
    ERR_REQUEST_CANCELED code: user cancelled — silent return

  @react-native-google-signin/google-signin:
    import { GoogleSignin } from '@react-native-google-signin/google-signin'
    GoogleSignin.configure() — call once (App.tsx or screen mount)
    GoogleSignin.hasPlayServices() — check before signIn
    GoogleSignin.signIn() — returns { data: { idToken, user } }
    In Phase 1 MOCK: do not call real GoogleSignin.signIn() — it requires dev build and real OAuth credentials.
    Instead: simulate with a 1-second delay then resolve with a fake token.

From src/screens/auth/SignUpScreen.tsx (01-03):
  Social auth stub section currently renders no-op TouchableOpacity buttons labeled "Continue with Google" and "Continue with Apple"

From src/screens/auth/LoginScreen.tsx (01-04):
  Same social auth stub section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SocialAuthButton component for Google and mock sign-in handler</name>
  <files>src/components/common/SocialAuthButton.tsx</files>

  <read_first>
    - src/theme/theme.ts (DSColors — surface, border, textPrimary, label font size)
    - .planning/phases/01-foundation-and-authentication/01-UI-SPEC.md (Social Auth Buttons interaction contract: outlined full-width, icon left-aligned, label centered; Component Token Map: Social Auth Button row)
    - .planning/phases/01-foundation-and-authentication/01-RESEARCH.md (Pattern 6: Social Auth, Anti-Patterns: WebView OAuth)
  </read_first>

  <action>
    Create src/components/common/SocialAuthButton.tsx. This handles the Google button visual. Apple uses expo-apple-authentication's native AppleAuthenticationButton directly in the screen — it cannot be wrapped because App Store guidelines require the exact Apple-provided UI.

    SocialAuthButton props interface: { label: string; iconName: string; onPress: () => void; disabled?: boolean; testID?: string }

    Layout: TouchableOpacity (onPress, disabled). Inner View: flexDirection='row', alignItems='center', justifyContent='center'. Style: full-width (width: '100%'), height: 48dp, borderRadius: 8dp, backgroundColor: DSColors.surface, borderWidth: 1, borderColor: DSColors.border. Icon: @expo/vector-icons MaterialCommunityIcons with iconName prop, size: 20, color: DSColors.textPrimary, marginRight: 8dp. Label: Text, Label typography (14dp, weight 700), color: DSColors.textPrimary.

    When disabled={true}: opacity 0.5 on the outer TouchableOpacity.

    Export as default. This is generic — used for Google button; Apple uses its own native component.

    Also create a helper function handleGoogleSignIn in a separate exportable function (not part of the component — export from the same file for use by screens):

    export async function getMockGoogleToken(): Promise<string> — simulates a 1-second async delay (setTimeout promise), then returns 'mock-google-token-' + Date.now(). This mock bypasses the real GoogleSignin.signIn() which requires dev build and real credentials. Add a comment: // TODO: Replace with real GoogleSignin.configure() + GoogleSignin.signIn() in integration phase. GoogleSignin requires dev build (see RESEARCH.md Pitfall 1).

    Note: Do NOT import or call GoogleSignin in this Phase 1 implementation. The comment explains why. The real implementation will uncomment the import and replace getMockGoogleToken.
  </action>

  <verify>
    npx tsc --noEmit — zero errors
    Visual check: SocialAuthButton renders with outlined style (surface bg, border stroke), icon left-aligned, label centered.
    Disabled state: opacity 0.5.
  </verify>

  <acceptance_criteria>
    - SocialAuthButton.tsx: props — label (string), iconName (string), onPress (function), disabled (boolean optional), testID (string optional)
    - SocialAuthButton renders TouchableOpacity > View with flexDirection='row', height 48dp, borderRadius 8dp, backgroundColor DSColors.surface, borderWidth 1, borderColor DSColors.border
    - MaterialCommunityIcons with iconName prop, size 20, color DSColors.textPrimary, marginRight 8dp
    - Label Text: 14dp, weight 700, color DSColors.textPrimary
    - disabled={true}: outer TouchableOpacity opacity 0.5
    - getMockGoogleToken exported from same file: returns Promise<string> resolving with 'mock-google-token-' + Date.now() after 1s delay
    - Comment in file: "TODO: Replace with real GoogleSignin.configure() + GoogleSignin.signIn() in integration phase"
    - No import or call of @react-native-google-signin/google-signin in this file
    - tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire social auth on SignUpScreen and LoginScreen</name>
  <files>src/screens/auth/SignUpScreen.tsx, src/screens/auth/LoginScreen.tsx</files>

  <read_first>
    - src/screens/auth/SignUpScreen.tsx (read current file — replace no-op social auth stubs)
    - src/screens/auth/LoginScreen.tsx (read current file — replace no-op social auth stubs)
    - src/components/common/SocialAuthButton.tsx (just created — SocialAuthButton props, getMockGoogleToken export)
    - src/context/AuthContext.tsx (useAuthContext — signIn function)
    - .planning/phases/01-foundation-and-authentication/01-RESEARCH.md (Pattern 6: Apple Sign-In with ERR_REQUEST_CANCELED handling)
    - .planning/phases/01-foundation-and-authentication/01-UI-SPEC.md (Social Auth Buttons interaction contract, Error Copy: "Social auth cancelled" = silent; "Social auth failure" = show inline error)
  </read_first>

  <action>
    Update src/screens/auth/SignUpScreen.tsx and src/screens/auth/LoginScreen.tsx. The changes are identical in both files — apply the same pattern to each.

    Replace the no-op social auth stubs with wired handlers.

    Google sign-in handler (handleGoogleSignIn — add to both screens):
    1. setIsLoading(true).
    2. try: const token = await getMockGoogleToken(); await authContext.signIn(token).
    3. catch: show formError "Sign in with Google failed. Try again or use email."
    4. finally: setIsLoading(false).

    In JSX, replace the no-op Google TouchableOpacity stub with:
    SocialAuthButton component: label="Continue with Google", iconName="google", onPress={handleGoogleSignIn}, disabled={isLoading}.

    Apple sign-in handler (handleAppleSignIn — add to both screens):
    Wrap in Platform.OS === 'ios' conditional. Handler logic per RESEARCH.md Pattern 6:
    1. setIsLoading(true).
    2. try: const credential = await AppleAuthentication.signInAsync({ requestedScopes: [FULL_NAME, EMAIL] }); const token = 'mock-apple-token-' + Date.now(); await authContext.signIn(token).
    3. catch (e: any): if e.code === 'ERR_REQUEST_CANCELED' → return silently (no error, per UI-SPEC "Social auth cancelled" = no error shown). Otherwise: setFormError("Sign in with Apple failed. Try again or use email.").
    4. finally: setIsLoading(false).

    In JSX, replace the no-op Apple TouchableOpacity stub with a Platform.OS==='ios' conditional block rendering AppleAuthentication.AppleAuthenticationButton:
    - buttonType: AppleAuthentication.AppleAuthenticationButtonType.SIGN_IN
    - buttonStyle: AppleAuthentication.AppleAuthenticationButtonStyle.BLACK
    - cornerRadius: 8
    - style: { width: '100%', height: 44 }
    - onPress: handleAppleSignIn

    Social auth buttons must remain disabled (disabled prop or wrapper disabled) when isLoading is true — prevents triggering social auth mid-request.

    Add import for AppleAuthentication from 'expo-apple-authentication' and getMockGoogleToken from '../components/common/SocialAuthButton' (or the path relative to the screen file's location).

    Add a comment above Apple block: // Apple Sign-In — iOS only per D-16 and App Store requirement (AppleAuthenticationButton)
  </action>

  <verify>
    npx tsc --noEmit — zero errors
    Manual test (SignUpScreen): tap "Continue with Google" → 1-second delay → AppTabs visible.
    Manual test (LoginScreen): tap "Continue with Google" → 1-second delay → AppTabs visible.
    iOS only: Apple button rendered; Android: Apple button absent.
    Cancellation: If Apple auth dialog dismissed (ERR_REQUEST_CANCELED): no error shown, form remains active.
  </verify>

  <acceptance_criteria>
    - SignUpScreen.tsx: imports SocialAuthButton and getMockGoogleToken; imports AppleAuthentication from 'expo-apple-authentication'
    - SignUpScreen.tsx: Google section replaced with SocialAuthButton label="Continue with Google" iconName="google" onPress={handleGoogleSignIn} disabled={isLoading}
    - SignUpScreen.tsx: Apple section replaced with Platform.OS==='ios' conditional rendering AppleAuthentication.AppleAuthenticationButton with buttonType.SIGN_IN, buttonStyle.BLACK, cornerRadius 8, height 44
    - LoginScreen.tsx: same Google and Apple replacements as SignUpScreen
    - handleGoogleSignIn in both screens: calls getMockGoogleToken(), then authContext.signIn(token); sets formError on failure
    - handleAppleSignIn in both screens: calls AppleAuthentication.signInAsync(); for ERR_REQUEST_CANCELED returns silently; calls authContext.signIn(token) on success
    - Both social buttons disabled when isLoading is true
    - Apple handler wrapped in Platform.OS==='ios' guard; Apple button rendered conditionally on iOS only
    - Comment "Apple Sign-In — iOS only per D-16" present above Apple conditional block
    - tsc --noEmit exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
**Threats addressed in this plan:**

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-17 | Spoofing | Google OAuth via WebView | mitigate | Using native SDK structure (getMockGoogleToken placeholder). Comment in code documents that real GoogleSignin.signIn() replaces mock — WebView OAuth is deprecated by Google and must not be used (Anti-Patterns in RESEARCH.md) |
| T-01-18 | Spoofing | Apple credential only returned once | accept | In Phase 1 mock, credential object is not real. Comment in code: "Store name/email from first Apple sign-in — credential not re-returned on subsequent sign-ins (Pitfall 5 in RESEARCH.md). Implement in integration phase." |
| T-01-19 | Tampering | Double-submit on social auth | mitigate | setIsLoading(true) before await; SocialAuthButton disabled={isLoading}; AppleAuthentication.AppleAuthenticationButton wrapped in disabled check |
| T-01-20 | Information Disclosure | ERR_REQUEST_CANCELED silent handling | accept | Cancellation returns silently — no data exposed. Correct per UI-SPEC and Apple HIG guidance. |
| T-01-21 | Tampering | Mock token accepted without validation | accept | Phase 1 mock: 'mock-google-token-*' and 'mock-apple-token-*' are stored as-is. Real token validation (Google idToken verify via backend, Apple identityToken verify) is a backend concern in integration phase. Noted in code comments. |

**Out of scope for this plan:** Real Google Cloud Console OAuth setup (requires iosUrlScheme, google-services.json, SHA-1 — blocked behind dev build; separate integration task). Apple developer entitlements setup. Token verification on backend.
</threat_model>

<verification>
After both tasks complete:

1. npx tsc --noEmit exits 0
2. SignUpScreen: "Continue with Google" button visible (outlined, icon + label); tapping → 1s delay → AppTabs
3. LoginScreen: "Continue with Google" button visible; same flow
4. iOS simulator: "Continue with Apple" AppleAuthenticationButton visible below Google button on both screens
5. Android simulator: Apple button absent — not rendered
6. Social auth buttons disabled when isLoading=true (e.g. during email signIn request)
7. SocialAuthButton: outlined style with surface background and border stroke visible
</verification>

<success_criteria>
- AUTH-04: User can trigger Google sign-in mock from Sign Up and Login screens; mock resolves and routes to AppTabs
- AUTH-04: Apple sign-in button renders on iOS using AppleAuthenticationButton (App Store compliant)
- Platform guard: Apple button absent on Android — no Platform.OS check failures
- Social auth cancellation: ERR_REQUEST_CANCELED handled silently per UI-SPEC
- Code structure ready for real credential swap: getMockGoogleToken clearly documented for replacement
</success_criteria>

<output>
Create .planning/phases/01-foundation-and-authentication/01-05-SUMMARY.md when done.
</output>
