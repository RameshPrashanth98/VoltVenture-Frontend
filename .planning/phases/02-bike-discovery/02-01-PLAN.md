---
phase: 02-bike-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VoltVenture/app.json
  - VoltVenture/App.tsx
  - VoltVenture/src/types/bike.ts
  - VoltVenture/src/services/bikeService.ts
  - VoltVenture/src/components/map/BikeMarker.tsx
  - VoltVenture/src/screens/app/MapScreen.tsx
autonomous: false
requirements:
  - DISC-01
user_setup:
  - service: google-maps-android
    why: "Map tiles will not render on Android without a valid API key — the map shows a grey screen."
    env_vars:
      - name: GOOGLE_MAPS_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials → Create API Key. Enable 'Maps SDK for Android' on the key."
    dashboard_config:
      - task: "Enable Maps SDK for Android"
        location: "Google Cloud Console → APIs & Services → Library → search 'Maps SDK for Android' → Enable"

must_haves:
  truths:
    - "Map tab loads a full-bleed map (no placeholder text) centered on Amsterdam (52.3676, 4.9041)"
    - "10–15 Electric Green bike markers appear at distinct Amsterdam coordinates"
    - "Map tiles render correctly on Android (requires API key in app.json)"
    - "Location permission is requested on mount; fallback to Amsterdam if denied"
  artifacts:
    - path: "VoltVenture/src/types/bike.ts"
      provides: "Bike interface and FilterState interface"
      exports: ["Bike", "FilterState"]
    - path: "VoltVenture/src/services/bikeService.ts"
      provides: "Mock bike data and bikeService singleton"
      exports: ["bikeService", "mockBikeService", "BikeService"]
    - path: "VoltVenture/src/components/map/BikeMarker.tsx"
      provides: "Branded Electric Green circular marker with bolt icon"
    - path: "VoltVenture/src/screens/app/MapScreen.tsx"
      provides: "Full-bleed map with Marker array rendered from bikeService data"
  key_links:
    - from: "VoltVenture/src/screens/app/MapScreen.tsx"
      to: "VoltVenture/src/services/bikeService.ts"
      via: "bikeService.getNearbyBikes() in useEffect"
      pattern: "bikeService\\.getNearbyBikes"
    - from: "VoltVenture/src/screens/app/MapScreen.tsx"
      to: "VoltVenture/src/components/map/BikeMarker.tsx"
      via: "BikeMarker rendered as Marker child"
      pattern: "<BikeMarker"
    - from: "VoltVenture/App.tsx"
      to: "@gorhom/bottom-sheet"
      via: "BottomSheetModalProvider wrapping"
      pattern: "BottomSheetModalProvider"
---

<objective>
Deliver the first vertical slice: a live map showing branded Electric Green bike pins at mock locations. This plan installs and configures all Phase 2 dependencies, defines the shared data types and mock service, creates the BikeMarker component, and rewrites MapScreen.tsx from placeholder to functional map screen.

Purpose: Establish the complete map foundation that Slices 2–4 build on top of. After this plan, a user opening the Map tab sees a full-bleed map with bike markers — not a placeholder.
Output: app.json (maps plugins), App.tsx (BottomSheetModalProvider), bike.ts, bikeService.ts, BikeMarker.tsx, MapScreen.tsx (full rewrite)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-bike-discovery/02-CONTEXT.md
@.planning/phases/02-bike-discovery/02-RESEARCH.md
@.planning/phases/02-bike-discovery/02-PATTERNS.md

<interfaces>
<!-- Key existing contracts the executor must respect. -->

From VoltVenture/App.tsx (lines 42–56):
Current App root structure — BottomSheetModalProvider must wrap inside GestureHandlerRootView,
outside NavigationContainer, so that sheets rendered from any screen can portal to root:
```typescript
export default function App() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <PaperProvider theme={paperTheme}>
        <SafeAreaProvider>
          <NavigationContainer>
            <AuthProvider>
              <AppInner />
            </AuthProvider>
          </NavigationContainer>
        </SafeAreaProvider>
      </PaperProvider>
    </GestureHandlerRootView>
  );
}
```

From VoltVenture/src/theme/theme.ts (key tokens for this plan):
```typescript
DSColors.primary        // '#C6FF2D' — Electric Green — bike marker background
DSColors.textOnPrimary  // '#0F0F0F' — bolt icon color on green
DSColors.background     // '#FFFFFF'
DSColors.border         // '#EBEBEB'
DSColors.textPrimary    // '#0F0F0F'
DSColors.textSecondary  // '#808080'

DSTypography.heading    // { fontSize: 20, lineHeight: 26, fontWeight: '600' }
DSTypography.body       // { fontSize: 15, lineHeight: 22, fontWeight: '400' }
```

From VoltVenture/src/services/authService.ts (mirror pattern exactly):
```typescript
// Pattern: interface + const delay + mockXxxService + export const xxxService = mockXxxService
const delay = (ms: number) => new Promise<void>(resolve => setTimeout(resolve, ms));
export const authService: AuthService = mockAuthService;
```

From VoltVenture/src/components/common/PrimaryButton.tsx (props interface to reuse later):
```typescript
interface PrimaryButtonProps {
  label: string;
  onPress: () => void;
  loading?: boolean;
  disabled?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Obtain Google Maps Android API Key</name>
  <what-built>Nothing yet — this gate ensures the dev build will render map tiles on Android. Without a valid key, MapView renders as a grey screen with no tiles.</what-built>
  <how-to-verify>
    1. Go to https://console.cloud.google.com/ and sign in.
    2. Create or select a project.
    3. Navigate to: APIs & Services → Library → search "Maps SDK for Android" → click Enable.
    4. Navigate to: APIs & Services → Credentials → Create Credentials → API Key.
    5. Copy the generated key (e.g., AIzaSy...).
    6. Keep the key ready — the next task will write it into app.json.
    For local development, an unrestricted key is fine. Restrict by SHA-1 fingerprint before production release.
  </how-to-verify>
  <resume-signal>Paste your Google Maps Android API key here (e.g., AIzaSyXXXXXXXXXXXXXX) to continue, or type "skip" to proceed with a placeholder key (map will show grey on Android until a real key is added).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages, configure app.json plugins, and add BottomSheetModalProvider to App.tsx</name>
  <files>VoltVenture/app.json, VoltVenture/App.tsx</files>
  <read_first>
    - VoltVenture/app.json — read current plugins array before appending; do not remove existing plugins
    - VoltVenture/App.tsx — read full file before editing; BottomSheetModalProvider wraps inside GestureHandlerRootView, around PaperProvider
    - .planning/phases/02-bike-discovery/02-RESEARCH.md — app.json plugin config example in "Code Examples" section
  </read_first>
  <action>
    Step A — Install new packages (run from VoltVenture/ directory):
    `npx expo install react-native-maps expo-location @gorhom/bottom-sheet`
    This adds three packages; existing peer deps (react-native-reanimated@4.5.3, react-native-gesture-handler@3.1.0) already satisfy @gorhom/bottom-sheet v5 peer deps.

    Step B — Update VoltVenture/app.json: append two entries to the existing `plugins` array, after `"expo-apple-authentication"`. Do not reorder or remove existing entries:
    - Entry 1: `["react-native-maps", { "androidGoogleMapsApiKey": "<KEY_FROM_TASK_1>" }]`
      Use the key provided by the user in Task 1. If the user typed "skip", use the string `"YOUR_ANDROID_MAPS_KEY_HERE"` as a placeholder.
    - Entry 2: `["expo-location", { "locationWhenInUsePermission": "Allow VoltVenture to show nearby e-bikes on the map." }]`

    Step C — Update VoltVenture/App.tsx: add `BottomSheetModalProvider` from `@gorhom/bottom-sheet`.
    Import at top: `import { BottomSheetModalProvider } from '@gorhom/bottom-sheet';`
    Wrap the existing tree inside `<BottomSheetModalProvider>` as the outermost child of `<GestureHandlerRootView>`, around `<PaperProvider>`. The resulting nesting order must be:
    GestureHandlerRootView > BottomSheetModalProvider > PaperProvider > SafeAreaProvider > NavigationContainer > AuthProvider > AppInner.
    This is harmless if @gorhom's ref-based pattern does not require it, and mandatory if it does (RESEARCH.md A1).
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && node -e "const a=require('./app.json'); const plugins=a.expo.plugins; const hasRNMaps=plugins.some(p=>Array.isArray(p)&&p[0]==='react-native-maps'); const hasLocation=plugins.some(p=>Array.isArray(p)&&p[0]==='expo-location'); console.log('react-native-maps plugin:', hasRNMaps); console.log('expo-location plugin:', hasLocation);"</automated>
  </verify>
  <acceptance_criteria>
    - `app.json` plugins array contains a `["react-native-maps", { "androidGoogleMapsApiKey": "..." }]` entry
    - `app.json` plugins array contains a `["expo-location", { "locationWhenInUsePermission": "Allow VoltVenture to show nearby e-bikes on the map." }]` entry
    - No existing plugins (expo-secure-store, expo-font, expo-splash-screen, @react-native-google-signin/google-signin, expo-apple-authentication) are removed or reordered
    - `App.tsx` imports `BottomSheetModalProvider` from `@gorhom/bottom-sheet`
    - `App.tsx` wraps `<PaperProvider>` inside `<BottomSheetModalProvider>` inside `<GestureHandlerRootView>`
    - `package.json` (or `node_modules`) contains `react-native-maps`, `expo-location`, `@gorhom/bottom-sheet`
  </acceptance_criteria>
  <done>app.json has both plugin entries; App.tsx wraps tree with BottomSheetModalProvider; all three packages installed.</done>
</task>

<task type="auto">
  <name>Task 3: Create bike.ts types, bikeService.ts mock, BikeMarker component, and rewrite MapScreen</name>
  <files>
    VoltVenture/src/types/bike.ts,
    VoltVenture/src/services/bikeService.ts,
    VoltVenture/src/components/map/BikeMarker.tsx,
    VoltVenture/src/screens/app/MapScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/services/authService.ts — mirror this pattern exactly for bikeService.ts (interface + delay + mockService + export singleton)
    - VoltVenture/src/components/common/SocialAuthButton.tsx — mirror View+icon+StyleSheet.create pattern for BikeMarker.tsx
    - VoltVenture/src/screens/app/AccountScreen.tsx — mirror StyleSheet.create + DSColors pattern for MapScreen styles
    - VoltVenture/src/theme/theme.ts — extract DSColors and DSTypography (already in context above)
    - .planning/phases/02-bike-discovery/02-PATTERNS.md — read all pattern assignments for the four files
    - VoltVenture/src/screens/app/MapScreen.tsx — current placeholder to be fully replaced
  </read_first>
  <action>
    Create `VoltVenture/src/types/bike.ts` — pure TypeScript, no imports. Export:
    - `Bike` interface: id (string), name (string), type ('standard' | 'speed' | 'cargo'), batteryPct (number 0–100), pricePerMin (number, EUR), latitude (number), longitude (number), distanceKm (number, optional — computed client-side)
    - `FilterState` interface: battery? ('low' | 'med' | 'high'), price? ('low' | 'med' | 'high'), type? ('standard' | 'speed' | 'cargo')

    Create `VoltVenture/src/services/bikeService.ts` — mirror authService.ts pattern:
    - Export `BikeService` interface with `getNearbyBikes(): Promise<Bike[]>`
    - Define `const delay = (ms: number) => new Promise<void>(resolve => setTimeout(resolve, ms))`
    - Define `mockBikes`: array of 13 bikes spread around Amsterdam city centre (52.3676° N, 4.9041° E). Vary lat/lon by ±0.005 to ±0.025 degrees to place bikes at realistic street-level distances. Mix of types: 5 standard, 4 speed, 4 cargo. Battery: range from 15 to 95. pricePerMin: 0.20 for standard, 0.30 for speed, 0.25 for cargo.
    - Define `mockBikeService` implementing `BikeService`: `getNearbyBikes` awaits `delay(500)` then returns `mockBikes`
    - Export `const bikeService: BikeService = mockBikeService` as the swap point

    Create `VoltVenture/src/components/map/BikeMarker.tsx` — no props, no imports from react-native-maps:
    - Import React, View, StyleSheet from 'react-native'; MaterialCommunityIcons from '@expo/vector-icons'; DSColors from '../../theme/theme'
    - Render: View (pinContainer) > View (circle, 36×36, borderRadius 18, backgroundColor DSColors.primary, borderWidth 2, borderColor DSColors.textOnPrimary) containing MaterialCommunityIcons name="lightning-bolt" size={18} color={DSColors.textOnPrimary}, plus a sibling View (tail) using CSS triangle trick: width 0, height 0, borderLeftWidth 6, borderRightWidth 6, borderTopWidth 8, borderLeftColor/borderRightColor 'transparent', borderTopColor DSColors.primary
    - StyleSheet at module bottom; no NativeWind; no hardcoded hex strings (use DSColors tokens only)

    Rewrite `VoltVenture/src/screens/app/MapScreen.tsx` completely:
    - Remove SafeAreaView import and usage — MapView uses StyleSheet.absoluteFillObject
    - Imports: React (useState, useEffect, useRef, useCallback, useMemo), View, StyleSheet from 'react-native'; FAB, IconButton from 'react-native-paper'; BottomSheetModal from '@gorhom/bottom-sheet'; MapView, Marker from 'react-native-maps'; * as Location from 'expo-location'; DSColors from '../../theme/theme'; bikeService; Bike, FilterState from '../../types/bike'; BikeMarker, BikeDetailSheet (stub import — BikeDetailSheet does not exist yet, so add a TODO comment and do NOT import it yet), FilterSheet (same — stub/TODO), BikeListView (same — stub/TODO)
    - State: bikes (Bike[]), selectedBike (Bike | null), userLocation ({latitude, longitude} | null), isListView (boolean, false), activeFilters (FilterState, {})
    - Refs: bikeDetailRef (useRef<BottomSheetModal>(null)), filterSheetRef (useRef<BottomSheetModal>(null))
    - snapPoints: useMemo(() => ['45%'], [])
    - filteredBikes: useMemo computed from bikes + activeFilters + userLocation (Haversine distance). Include the haversine helper inline in the file as a module-level function `haversineKm(lat1, lon1, lat2, lon2): number`. Filter logic: battery 'low' = batteryPct <= 40, 'med' = 41–75, 'high' = > 75. Price 'low' = pricePerMin <= 0.22, 'med' = 0.23–0.27, 'high' = > 0.27. Type = exact match on bike.type. Map each result to add distanceKm field.
    - useEffect on mount: requestForegroundPermissionsAsync → if status !== 'granted', setUserLocation({latitude: 52.3676, longitude: 4.9041}); else getCurrentPositionAsync({}) → setUserLocation. Then bikeService.getNearbyBikes() → setBikes. Wrap in try/catch; on catch log error.
    - handleMarkerPress: useCallback((bike: Bike) => { setSelectedBike(bike); bikeDetailRef.current?.present(); }, [])
    - handleFilterPress: useCallback(() => { filterSheetRef.current?.present(); }, [])
    - handleApplyFilters: useCallback((filters: FilterState) => { setActiveFilters(filters); filterSheetRef.current?.dismiss(); }, [])
    - JSX structure: root View (style={StyleSheet.absoluteFillObject}, backgroundColor DSColors.background):
      - MapView (style={StyleSheet.absoluteFillObject}, initialRegion Amsterdam 52.3676/4.9041/latDelta 0.05/lonDelta 0.05, showsUserLocation={true}, showsMyLocationButton={false})
        - filteredBikes.map: Marker (key=bike.id, coordinate={latitude, longitude}, onPress={() => handleMarkerPress(bike)}, tracksViewChanges={false}) > BikeMarker — NOTE: BikeDetailSheet and FilterSheet are stubs for now; replace these with TODO comments instead of rendering them
      - Filter IconButton: position absolute, top 52, right 12, zIndex 10; icon="filter-variant"; onPress={handleFilterPress}; iconColor DSColors.textOnPrimary; containerColor DSColors.primary
      - FAB: position absolute, bottom 88, right 16, zIndex 10; backgroundColor DSColors.primary; icon="format-list-bulleted"; label="List view"; color DSColors.textOnPrimary; onPress={() => setIsListView(true)}
      - {/* TODO: BikeDetailSheet BottomSheetModal — wired in Plan 02-02 */}
      - {/* TODO: FilterSheet BottomSheetModal — wired in Plan 02-03 */}
      - {/* TODO: BikeListView — wired in Plan 02-04 */}
    - StyleSheet.create at module bottom with DSColors tokens; no NativeWind className attributes anywhere in the file
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && node -e "const fs=require('fs'); const bike=fs.readFileSync('src/types/bike.ts','utf8'); const svc=fs.readFileSync('src/services/bikeService.ts','utf8'); const marker=fs.readFileSync('src/components/map/BikeMarker.tsx','utf8'); const map=fs.readFileSync('src/screens/app/MapScreen.tsx','utf8'); console.log('Bike interface:', bike.includes('interface Bike')); console.log('FilterState:', bike.includes('interface FilterState')); console.log('bikeService export:', svc.includes('export const bikeService')); console.log('mockBikes count:', (svc.match(/id:/g)||[]).length, '>= 10'); console.log('BikeMarker lightning-bolt:', marker.includes('lightning-bolt')); console.log('BikeMarker DSColors.primary:', marker.includes('DSColors.primary')); console.log('MapScreen tracksViewChanges:', map.includes('tracksViewChanges={false}')); console.log('MapScreen absoluteFillObject:', map.includes('StyleSheet.absoluteFillObject')); console.log('MapScreen haversine:', map.includes('haversineKm')); console.log('MapScreen no NativeWind:', !map.includes('className='));"</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/bike.ts` exports `Bike` interface with all 8 fields (id, name, type, batteryPct, pricePerMin, latitude, longitude, distanceKm?)
    - `src/types/bike.ts` exports `FilterState` interface with battery?, price?, type? fields
    - `src/services/bikeService.ts` exports `bikeService` as the singleton with `getNearbyBikes(): Promise<Bike[]>`
    - `bikeService.ts` mock array contains at least 10 bikes with distinct Amsterdam-area coordinates (varying lat/lon within ±0.025° of 52.3676/4.9041)
    - `BikeMarker.tsx` uses `backgroundColor: DSColors.primary` (not a hardcoded '#C6FF2D' string) for the circle
    - `BikeMarker.tsx` uses `color={DSColors.textOnPrimary}` on the MaterialCommunityIcons lightning-bolt (not '#0F0F0F' string)
    - `MapScreen.tsx` has NO `className=` attributes (NativeWind rule)
    - `MapScreen.tsx` has NO `<SafeAreaView>` wrapper
    - `MapScreen.tsx` uses `style={StyleSheet.absoluteFillObject}` on the MapView
    - `MapScreen.tsx` renders `<Marker tracksViewChanges={false}>` for each bike
    - `MapScreen.tsx` includes `haversineKm` function and uses it inside the `filteredBikes` useMemo
    - `MapScreen.tsx` gates `getCurrentPositionAsync` behind `status === 'granted'` check with Amsterdam fallback
    - `snapPoints` is declared with `useMemo(() => ['45%'], [])` — not as an inline array literal in JSX
  </acceptance_criteria>
  <done>Map tab shows a full-bleed map centered on Amsterdam with 10+ Electric Green bike markers. Location permission is requested on mount. Filtering and bottom sheets are stubbed with TODO comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → OS Location API | expo-location requests foreground permission; response is trusted OS output |
| App → Google Maps API | API key in app.json is embedded in the native build; must not be committed to public repos without key restriction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | app.json `androidGoogleMapsApiKey` | accept (dev) | For local dev builds, an unrestricted key is acceptable. Before public release: restrict key by SHA-1 fingerprint in Google Cloud Console and add `.env` / EAS Secrets approach |
| T-02-02 | Tampering | bikeService.ts mock data | accept | Mock data; no real user or financial data. No mitigation needed at this stage |
| T-02-03 | Denial of Service | expo-location `getCurrentPositionAsync` hang | mitigate | Always gate behind `status === 'granted'` check; use Amsterdam fallback on denied — prevents infinite hang on Android (RESEARCH.md Pitfall 2) |
| T-02-SC | Tampering | npm install (react-native-maps, expo-location, @gorhom/bottom-sheet) | mitigate | All three packages are [APPROVED] per RESEARCH.md Package Legitimacy Audit (decade-old repos, official Expo SDK references, millions of weekly downloads) |
</threat_model>

<verification>
After completing all tasks, verify the Map tab end-to-end:

1. Run `npx expo run:android` from VoltVenture/ directory (dev build required — react-native-maps is not compatible with Expo Go)
2. Navigate to the Map tab in the app
3. Confirm: permission dialog appears asking for location access
4. Confirm: full-bleed map loads (not a grey screen — requires valid API key from Task 1)
5. Confirm: Electric Green circular markers with bolt icon visible at multiple locations around Amsterdam
6. Confirm: FAB labeled "List view" visible at bottom-right
7. Confirm: Filter icon button visible at top-right
8. Confirm: TypeScript compiles without errors — `npx tsc --noEmit` from VoltVenture/
</verification>

<success_criteria>
- Map tab loads full-bleed MapView (no placeholder text "Bikes Coming Soon")
- Electric Green (#C6FF2D) circular markers with lightning-bolt icon visible on map
- At least 10 markers at distinct Amsterdam-area coordinates
- No TypeScript compilation errors
- No NativeWind className attributes in any new file
- bikeService singleton exported and connected to MapScreen via useEffect
- app.json has react-native-maps and expo-location plugin entries
- App.tsx has BottomSheetModalProvider wrapping the component tree
</success_criteria>

<output>
Create `.planning/phases/02-bike-discovery/02-01-SUMMARY.md` when done.
</output>
