---
phase: 02-bike-discovery
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - VoltVenture/src/components/map/BikeListView.tsx
  - VoltVenture/src/components/map/BikeCard.tsx
  - VoltVenture/src/screens/app/MapScreen.tsx
autonomous: true
requirements:
  - DISC-04

must_haves:
  truths:
    - "Tapping the 'List view' FAB switches from map to a scrollable list of nearby bikes"
    - "List is sorted by distance from user (nearest first)"
    - "Each list row shows bolt icon, bike name, bike type (top row), and battery %, distance (bottom row)"
    - "Tapping a list row opens the same BikeDetailSheet bottom sheet as the map pin"
    - "List view shows a 'Map' button in the screen header area to switch back to the map"
    - "Active filters apply to the list view the same way they apply to the map (shared filteredBikes)"
    - "Empty state shown when no bikes match active filters in list view"
  artifacts:
    - path: "VoltVenture/src/components/map/BikeListView.tsx"
      provides: "FlatList of BikeCard rows with empty state and onSelectBike callback"
      exports: ["BikeListView"]
    - path: "VoltVenture/src/components/map/BikeCard.tsx"
      provides: "Two-row card showing bolt icon, bike name, type, battery %, distance"
      exports: ["BikeCard"]
    - path: "VoltVenture/src/screens/app/MapScreen.tsx"
      provides: "isListView toggle; FAB switches to list; Map button switches back; BikeListView rendered conditionally"
      contains: "isListView"
  key_links:
    - from: "VoltVenture/src/screens/app/MapScreen.tsx"
      to: "VoltVenture/src/components/map/BikeListView.tsx"
      via: "filteredBikes sorted by distanceKm passed as bikes prop"
      pattern: "<BikeListView"
    - from: "VoltVenture/src/components/map/BikeListView.tsx"
      to: "VoltVenture/src/components/map/BikeCard.tsx"
      via: "renderItem function passes Bike to BikeCard"
      pattern: "<BikeCard"
    - from: "VoltVenture/src/components/map/BikeCard.tsx"
      to: "VoltVenture/src/components/map/BikeDetailSheet.tsx"
      via: "onPress calls onSelectBike in MapScreen which opens bikeDetailRef sheet"
      pattern: "onPress"
---

<objective>
Deliver the fourth and final vertical slice: a full list view fallback for the map. This plan creates BikeListView and BikeCard components, then wires the FAB toggle into MapScreen. After this plan, Phase 2 is complete — all four DISC requirements are delivered.

Purpose: Some tourists prefer a scrollable list to a map (especially in dense cities). The list reuses the existing BikeDetailSheet so tapping a bike row gives the same detail experience as tapping a map pin.
Output: BikeListView.tsx (new), BikeCard.tsx (new), MapScreen.tsx (final update — list toggle, Map header button, list rendering)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-bike-discovery/02-CONTEXT.md
@.planning/phases/02-bike-discovery/02-RESEARCH.md
@.planning/phases/02-bike-discovery/02-PATTERNS.md
@.planning/phases/02-bike-discovery/02-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. -->

From VoltVenture/src/types/bike.ts (from 02-01):
```typescript
export interface Bike {
  id: string;
  name: string;
  type: 'standard' | 'speed' | 'cargo';
  batteryPct: number;
  pricePerMin: number;
  latitude: number;
  longitude: number;
  distanceKm?: number;  // present on filteredBikes (computed via haversine)
}
```

From VoltVenture/src/screens/app/MapScreen.tsx — existing state (from 02-01 through 02-03):
```typescript
const [isListView, setIsListView] = useState(false);
// filteredBikes: Bike[] — already computed with distanceKm from Haversine
// handleMarkerPress — already opens bikeDetailRef; reuse same pattern for list row tap
// Currently has: {/* TODO: BikeListView — wired in Plan 02-04 */}
```

From VoltVenture/src/theme/theme.ts:
```typescript
DSColors.primary        // '#C6FF2D' — FAB background
DSColors.textOnPrimary  // '#0F0F0F' — FAB icon/label color
DSColors.accent         // '#7D9220' — bolt icon color on white surface
DSColors.surface        // '#FAFAFA' — list card background
DSColors.border         // '#EBEBEB' — card border
DSColors.textPrimary    // '#0F0F0F'
DSColors.textSecondary  // '#808080'
DSTypography.heading    // { fontSize: 20, lineHeight: 26, fontWeight: '600' }
DSTypography.headingMd  // { fontSize: 17, lineHeight: 24, fontWeight: '600' }
DSTypography.body       // { fontSize: 15, lineHeight: 22, fontWeight: '400' }
DSTypography.label      // { fontSize: 13, lineHeight: 16, fontWeight: '600' }
```

BikeDetailSheet is already wired in 02-02 and will continue to work when triggered from list taps.
The same `handleMarkerPress` callback (renamed or aliased to `handleBikeSelect`) can be reused for list taps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BikeCard and BikeListView components</name>
  <files>
    VoltVenture/src/components/map/BikeCard.tsx,
    VoltVenture/src/components/map/BikeListView.tsx
  </files>
  <read_first>
    - .planning/phases/02-bike-discovery/02-PATTERNS.md — "BikeCard.tsx" section and "BikeListView.tsx" section for imports, props interfaces, component structure, FlatList pattern, empty state, and StyleSheet patterns
    - VoltVenture/src/components/common/SocialAuthButton.tsx — mirror TouchableOpacity + View + icon + text + StyleSheet.create pattern for BikeCard
    - VoltVenture/src/screens/auth/LoginScreen.tsx — ScrollView/list structure reference for BikeListView
    - VoltVenture/src/theme/theme.ts — DSColors and DSTypography tokens
  </read_first>
  <action>
    Create `VoltVenture/src/components/map/BikeCard.tsx`. This is a new file.

    Imports: React from 'react'; TouchableOpacity, View, Text, StyleSheet from 'react-native'; MaterialCommunityIcons from '@expo/vector-icons'; DSColors, DSTypography from '../../theme/theme'; Bike type from '../../types/bike'.

    Props interface `BikeCardProps`: bike (Bike), onPress (() => void).

    Component structure (mirrors SocialAuthButton TouchableOpacity pattern):
    - TouchableOpacity (style=styles.card, onPress={onPress}, activeOpacity={0.7}, accessibilityRole="button")
      - Top row View (flexDirection 'row', alignItems 'center', gap 8, marginBottom 4):
        - MaterialCommunityIcons name="lightning-bolt" size={16} color={DSColors.accent}
        - Text style=styles.bikeName: {bike.name}
        - Text style=styles.bikeType: {bike.type} (textTransform 'capitalize')
      - Bottom row View (flexDirection 'row', justifyContent 'space-between'):
        - Text style=styles.stat: `🔋 {bike.batteryPct}%` — but using MaterialCommunityIcons inline is complex; instead use a plain Text: `{bike.batteryPct}% battery`
        - Text style=styles.stat: `{bike.distanceKm != null ? bike.distanceKm.toFixed(2) + ' km' : '—'}`

    StyleSheet.create at module bottom: card (backgroundColor DSColors.surface, borderWidth 1, borderColor DSColors.border, borderRadius 8, paddingHorizontal 16, paddingVertical 12, marginBottom 8); bikeName (DSTypography.headingMd spread, color DSColors.textPrimary, flex 1); bikeType (DSTypography.label spread, color DSColors.textSecondary, textTransform 'capitalize'); stat (DSTypography.label spread, color DSColors.textSecondary). No NativeWind. No hardcoded hex.

    Create `VoltVenture/src/components/map/BikeListView.tsx`. This is a new file.

    Imports: React from 'react'; FlatList, View, Text, StyleSheet, ListRenderItem from 'react-native'; DSColors, DSTypography from '../../theme/theme'; BikeCard from './BikeCard'; Bike type from '../../types/bike'.

    Props interface `BikeListViewProps`: bikes (Bike[]), onSelectBike ((bike: Bike) => void).

    Component: if bikes.length === 0, render empty state View (style=styles.emptyState) with Text "No bikes match your filters." (style=styles.emptyText, DSTypography.body, DSColors.textSecondary, textAlign 'center'). Otherwise render FlatList: data={bikes}, keyExtractor={item => item.id}, renderItem={({ item }) => <BikeCard bike={item} onPress={() => onSelectBike(item)} />}, contentContainerStyle={styles.list}, showsVerticalScrollIndicator={false}.

    StyleSheet.create at module bottom: list (paddingHorizontal 16, paddingTop 8, paddingBottom 100), emptyState (flex 1, alignItems 'center', justifyContent 'center', paddingHorizontal 24, paddingTop 60), emptyText (DSTypography.body spread, color DSColors.textSecondary, textAlign 'center').
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && node -e "const fs=require('fs'); const card=fs.readFileSync('src/components/map/BikeCard.tsx','utf8'); const list=fs.readFileSync('src/components/map/BikeListView.tsx','utf8'); console.log('BikeCard TouchableOpacity:', card.includes('TouchableOpacity')); console.log('BikeCard lightning-bolt:', card.includes('lightning-bolt')); console.log('BikeCard batteryPct:', card.includes('batteryPct')); console.log('BikeCard distanceKm:', card.includes('distanceKm')); console.log('BikeCard no className:', !card.includes('className=')); console.log('BikeListView FlatList:', list.includes('FlatList')); console.log('BikeListView BikeCard:', list.includes('BikeCard')); console.log('BikeListView empty state:', list.includes('No bikes match')); console.log('BikeListView no className:', !list.includes('className='));"</automated>
  </verify>
  <acceptance_criteria>
    - `BikeCard.tsx` exports `BikeCard` as default
    - `BikeCard` renders a TouchableOpacity with `accessibilityRole="button"` and `activeOpacity={0.7}`
    - Top row has `MaterialCommunityIcons name="lightning-bolt"`, bike name (Text with DSTypography.headingMd), bike type (Text, capitalize)
    - Bottom row shows battery percentage and distance (handles `distanceKm == null` → displays '—')
    - `BikeListView.tsx` exports `BikeListView` as default
    - `BikeListView` renders a `FlatList` when `bikes.length > 0`
    - `BikeListView` renders empty state text "No bikes match your filters." when `bikes.length === 0`
    - `keyExtractor` uses `item.id`
    - No `className=` in either file
    - No hardcoded hex strings in either file — DSColors tokens only
    - StyleSheet declared at module bottom in both files
  </acceptance_criteria>
  <done>BikeCard renders a tappable two-row card; BikeListView renders a scrollable FlatList of BikeCard components with empty state handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire list view toggle into MapScreen</name>
  <files>VoltVenture/src/screens/app/MapScreen.tsx</files>
  <read_first>
    - VoltVenture/src/screens/app/MapScreen.tsx — read the full current file to find: isListView state, the TODO comment for BikeListView, the existing FAB ("List view"), filteredBikes, handleMarkerPress, and the map rendering section
    - VoltVenture/src/components/map/BikeListView.tsx — confirm props interface just created
    - .planning/phases/02-bike-discovery/02-CONTEXT.md — D-10, D-11: FAB toggles list view; list view header has "Map" button; list sorted by distance
  </read_first>
  <action>
    Edit `VoltVenture/src/screens/app/MapScreen.tsx`:

    1. Add import: `import BikeListView from '../../components/map/BikeListView';`

    2. Create a `sortedBikes` constant derived from `filteredBikes`, sorted by distanceKm ascending:
       `const sortedBikes = useMemo(() => [...filteredBikes].sort((a, b) => (a.distanceKm ?? 0) - (b.distanceKm ?? 0)), [filteredBikes]);`

    3. Create `handleBikeSelect` callback (alias for list taps, reuses bikeDetailRef):
       `const handleBikeSelect = useCallback((bike: Bike) => { setSelectedBike(bike); bikeDetailRef.current?.present(); }, []);`

    4. Replace the comment `{/* TODO: BikeListView — wired in Plan 02-04 */}` with the full list view block. The JSX structure should conditionally render either the map or the list:

       In the root View:
       - When `!isListView`: render the MapView with markers + filter button IconButton + "List view" FAB (as already exists)
       - When `isListView`: render a View (style={styles.listContainer}, flex 1, backgroundColor DSColors.background) containing:
         - Header View (style=styles.listHeader, flexDirection 'row', alignItems 'center', justifyContent 'space-between', paddingHorizontal 16, paddingVertical 12, borderBottomWidth 1, borderBottomColor DSColors.border):
           - Text "Nearby Bikes" (DSTypography.heading spread, color DSColors.textPrimary)
           - IconButton icon="map" onPress={() => setIsListView(false)} iconColor={DSColors.textPrimary} accessibilityLabel="Switch to map view"
         - BikeListView (bikes={sortedBikes}, onSelectBike={handleBikeSelect})

       The BikeDetailSheet and FilterSheet BottomSheetModal elements must remain outside both conditional branches — they must always be in the JSX tree (as siblings to the conditional content) so that refs remain valid regardless of which view is active.

    5. Add to StyleSheet: `listContainer: { flex: 1, backgroundColor: DSColors.background }` and `listHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 16, paddingVertical: 12, borderBottomWidth: 1, borderBottomColor: DSColors.border }`.

    Do not change filter logic, bottom sheet wiring, or any other state.
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && node -e "const fs=require('fs'); const s=fs.readFileSync('src/screens/app/MapScreen.tsx','utf8'); console.log('BikeListView import:', s.includes('import BikeListView')); console.log('sortedBikes:', s.includes('sortedBikes')); console.log('handleBikeSelect:', s.includes('handleBikeSelect')); console.log('isListView conditional:', s.includes('isListView')); console.log('setIsListView(false) on map button:', s.includes('setIsListView(false)')); console.log('Nearby Bikes header:', s.includes('Nearby Bikes')); console.log('BikeListView in JSX:', s.includes('<BikeListView')); console.log('No TODO 02-04 comment:', !s.includes('wired in Plan 02-04')); console.log('BottomSheetModals still present:', s.includes('ref={bikeDetailRef}') && s.includes('ref={filterSheetRef}'));"</automated>
  </verify>
  <acceptance_criteria>
    - `MapScreen.tsx` imports `BikeListView` from `'../../components/map/BikeListView'`
    - `sortedBikes` useMemo sorts `filteredBikes` by `distanceKm` ascending (nearest first)
    - `handleBikeSelect` callback is defined and opens `bikeDetailRef` sheet on call
    - `isListView` boolean controls which view is rendered: `false` = map view, `true` = list view
    - List view contains a header with "Nearby Bikes" text and a map icon button that calls `setIsListView(false)`
    - `<BikeListView bikes={sortedBikes} onSelectBike={handleBikeSelect} />` rendered in list view mode
    - Both `<BottomSheetModal ref={bikeDetailRef}>` and `<BottomSheetModal ref={filterSheetRef}>` remain in the JSX tree outside the conditional (always mounted)
    - The TODO comment `{/* TODO: BikeListView — wired in Plan 02-04 */}` is removed
    - The "List view" FAB remains in the map view (not shown in list view)
    - TypeScript: `npx tsc --noEmit` from VoltVenture/ reports 0 errors
  </acceptance_criteria>
  <done>FAB toggles to list view; list shows bikes sorted by distance; tapping a row opens BikeDetailSheet; Map button in list header returns to map view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → BikeListView | User taps list rows to open detail sheet; no input accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | sortedBikes sort over large arrays | accept | Mock data is 10–15 bikes; sort is O(n log n) on a tiny set; negligible cost |
| T-02-08 | Information Disclosure | BikeDetailSheet opened from list row | accept | Same mock data as map view; no new data exposure |
</threat_model>

<verification>
Full Phase 2 end-to-end check after both tasks:

1. Run `npx expo run:android` from VoltVenture/
2. Map tab loads with Electric Green bike markers (DISC-01 — confirmed in 02-01)
3. Tap a marker → BikeDetailSheet slides up with correct data (DISC-02 — confirmed in 02-02)
4. Filter icon → FilterSheet opens; apply "Cargo" type filter → only cargo bikes visible on map (DISC-03 — confirmed in 02-03)
5. Clear filters; tap "List view" FAB → list view appears with "Nearby Bikes" header and bike rows sorted by distance
6. Confirm first row has smallest distanceKm value (nearest bike is first)
7. Tap a list row → same BikeDetailSheet slides up; Reserve button logs TODO message
8. Tap the map icon in the list header → map view restores with markers
9. Apply a filter and switch to list view → list shows only filtered bikes
10. Apply a filter that matches 0 bikes and switch to list → "No bikes match your filters." shown
11. Run `npx tsc --noEmit` — 0 errors

## Phase 2 Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | User can find available e-bikes near them — map, details, filtering, list | 02-01, 02-02, 02-03, 02-04 |
| DISC-01 | See available e-bikes on a map | 02-01 |
| DISC-02 | Tap a bike to view battery %, price/min, distance | 02-02 |
| DISC-03 | Filter bikes by battery level, price range, or type | 02-03 |
| DISC-04 | Switch to list view of nearby bikes | 02-04 |
| D-01 | react-native-maps | 02-01 |
| D-02 | Apple Maps iOS / Google Maps Android with API key | 02-01 |
| D-03 | Electric Green marker with bolt icon, tracksViewChanges={false} | 02-01 |
| D-04 | No clustering | 02-01 (individual markers only) |
| D-05 | Map stays visible above open detail sheet | 02-02 (BottomSheetModal sibling to MapView) |
| D-06 | Detail sheet: drag handle, name, type, battery/price/distance chips, Reserve stub | 02-02 |
| D-07 | List row tap opens same bottom sheet as map pin | 02-04 |
| D-08 | Filter icon at top-right opens filter sheet | 02-03 |
| D-09 | Filter chips: battery/price/type with Apply button | 02-03 |
| D-10 | FAB toggles list view; list has Map button to return | 02-04 |
| D-11 | List sorted by distance; two-row card; tap opens sheet | 02-04 |
| RESEARCH | @gorhom/bottom-sheet BottomSheetModal with BottomSheetModalProvider | 02-01 (provider), 02-02 (detail sheet), 02-03 (filter sheet) |
| RESEARCH | expo-location gated behind requestForegroundPermissionsAsync | 02-01 |
| RESEARCH | Haversine distance function inline | 02-01 |
| RESEARCH | useMemo for snapPoints | 02-01 |
| RESEARCH | StyleSheet.create / no NativeWind on map screens | all plans |
</verification>

<success_criteria>
- "List view" FAB visible on map view; tapping it switches to list view
- List view shows bike cards sorted by distance (nearest first)
- Tapping a list row opens BikeDetailSheet (same component as map pin tap)
- "Map" icon button in list view header returns to map view
- Active filters apply equally to map and list view (shared filteredBikes/sortedBikes)
- Empty state shown in list view when no bikes match active filters
- No TypeScript errors in any Phase 2 file
- All four DISC requirements delivered and verifiable
</success_criteria>

<output>
Create `.planning/phases/02-bike-discovery/02-04-SUMMARY.md` when done.
</output>
