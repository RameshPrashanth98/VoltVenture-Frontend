---
phase: 03-booking-and-unlock
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - VoltVenture/src/screens/booking/BLEUnlockScreen.tsx
  - VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx
autonomous: true
requirements:
  - BOOK-03

must_haves:
  truths:
    - "Tapping 'Unlock via Bluetooth' opens BLEUnlockScreen showing an ActivityIndicator and 'Looking for nearby bikes...' text"
    - "After 1.5 s the BLE screen auto-advances to show the bike name and 'Bike found' text"
    - "After another 1.0 s the BLE screen shows 'Unlocking...' text"
    - "After another 1.0 s the BLE screen navigates automatically to UnlockSuccess"
    - "A 'Cancel' text button is visible throughout all BLE states and returns to BookingConfirmation"
    - "When BookingConfirmation countdown reaches 0, a Snackbar shows 'Reservation expired' and the BookingStack dismisses after 2500 ms"
  artifacts:
    - path: "VoltVenture/src/screens/booking/BLEUnlockScreen.tsx"
      provides: "BLE mock state machine screen"
      min_lines: 60
      contains: "bleState"
    - path: "VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx"
      provides: "Expiry handling wired (Snackbar + dismiss)"
      contains: "secondsLeft === 0"
  key_links:
    - from: "VoltVenture/src/screens/booking/BLEUnlockScreen.tsx"
      to: "UnlockSuccess screen"
      via: "navigation.navigate('UnlockSuccess', { bike }) after 3500 ms total"
      pattern: "navigate.*UnlockSuccess"
    - from: "VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx"
      to: "BookingStack dismiss"
      via: "navigation.getParent()?.goBack() after Snackbar timeout"
      pattern: "getParent.*goBack"
---

<objective>
Deliver the Bluetooth unlock vertical slice and wire the timer expiry behavior. After this plan the full booking flow is end-to-end: Reserve → BookingConfirmation → BLEUnlock auto-advance → UnlockSuccess → Start Ride, plus timer expiry dismisses the modal cleanly.

Purpose: Complete BOOK-03 (BLE unlock path) and finalize the expiry behavior stubbed in Plan 03-01. This plan has no new dependencies — it replaces the BLEUnlock stub and completes the BookingConfirmationScreen expiry logic.

Output: BLEUnlockScreen.tsx (full 3-state mock), BookingConfirmationScreen.tsx expiry Snackbar wired (secondsLeft === 0 effect). All three unlock paths (QR, BLE, expiry) now work end-to-end.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.claude/get-shit-done/workflows/execute-plan.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/03-booking-and-unlock/03-01-SUMMARY.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/03-booking-and-unlock/03-02-SUMMARY.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From VoltVenture/src/types/navigation.ts (after Plan 03-01):
  export type BookingStackParamList = {
    BookingConfirmation: { bike: Bike };
    QRScanner: { bike: Bike };
    BLEUnlock: { bike: Bike };
    UnlockSuccess: { bike: Bike };
  };

BLE state machine type:
  type BLEState = 'scanning' | 'found' | 'connecting';
  // Three states only — after 'connecting' the screen navigates away to UnlockSuccess
  // Timings per D-09: scanning = 1500 ms, found = 1000 ms, connecting = 1000 ms
  // Use three separate setTimeout calls, each with cumulative offset:
  //   t1 = setTimeout(() => setBleState('found'), 1500)
  //   t2 = setTimeout(() => setBleState('connecting'), 1500 + 1000)
  //   t3 = setTimeout(() => navigation.navigate('UnlockSuccess', { bike }), 1500 + 1000 + 1000)
  // All three must be cleared in useEffect cleanup

From VoltVenture/src/theme/theme.ts:
  DSColors.primary = '#C6FF2D'
  DSColors.background = '#FFFFFF'
  DSColors.textPrimary = '#0F0F0F'
  DSColors.textSecondary = '#808080'
  DSTypography.heading = { fontSize: 20, lineHeight: 26, fontWeight: '600' }
  DSTypography.body = { fontSize: 15, lineHeight: 22, fontWeight: '400' }
  DSTypography.label = { fontSize: 13, lineHeight: 16, fontWeight: '600' }

BookingConfirmationScreen expiry wiring (from Plan 03-01 implementation):
  // The secondsLeft === 0 effect must show the Snackbar, then after 2500 ms call navigation.getParent()?.goBack()
  // The Snackbar component is already imported and the visible state already exists from Plan 03-01
  // This task only verifies/completes the useEffect([secondsLeft]) branch — it may already be present
  // Read the current file before editing to see what was implemented

React Native Paper Snackbar usage:
  import { Snackbar } from 'react-native-paper';
  <Snackbar
    visible={snackbarVisible}
    onDismiss={() => setSnackbarVisible(false)}
    duration={2500}
  >
    Reservation expired — returning to map
  </Snackbar>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BLEUnlockScreen — full 3-state mock implementation</name>
  <files>VoltVenture/src/screens/booking/BLEUnlockScreen.tsx</files>
  <read_first>
    - VoltVenture/src/screens/booking/BLEUnlockScreen.tsx (current stub — read before overwriting)
    - VoltVenture/src/types/navigation.ts (BookingStackParamList — for StackScreenProps typing)
    - VoltVenture/src/theme/theme.ts (DSColors, DSTypography)
    - VoltVenture/src/screens/booking/UnlockSuccessScreen.tsx (confirm it exists and accepts { bike: Bike } params)
  </read_first>
  <action>
    Replace the stub with the full BLEUnlockScreen implementation. Type as StackScreenProps<BookingStackParamList, 'BLEUnlock'>. Extract bike from route.params.

    State:
    - const [bleState, setBleState] = useState<'scanning' | 'found' | 'connecting'>('scanning')

    useEffect (runs once on mount, no deps other than navigation and bike):
    - Declare t1, t2, t3 as ReturnType<typeof setTimeout>
    - t1 = setTimeout(() => setBleState('found'), 1500)
    - t2 = setTimeout(() => setBleState('connecting'), 2500)
    - t3 = setTimeout(() => navigation.navigate('UnlockSuccess', { bike }), 3500)
    - Return cleanup: () => { clearTimeout(t1); clearTimeout(t2); clearTimeout(t3); }
    The cleanup ensures that if the user taps Cancel during t3's pending period, the setTimeout is cleared and navigation does not fire after unmount.

    Layout (full-screen centered, background DSColors.background, flex:1, justifyContent:'center', alignItems:'center', paddingHorizontal:24):

    Icon row: MaterialCommunityIcons name="bluetooth" size=48 color={DSColors.primary} — always visible at top of content.

    Below icon — conditional content based on bleState:
    - 'scanning': ActivityIndicator size="large" color={DSColors.primary} below the icon, then Text "Looking for nearby bikes…" (DSTypography.body, DSColors.textSecondary, textAlign:'center', marginTop:16)
    - 'found': Text bike.name (DSTypography.heading, DSColors.textPrimary, textAlign:'center'), Text "Bike found" (DSTypography.body, DSColors.accent, textAlign:'center', marginTop:8)
    - 'connecting': ActivityIndicator size="large" color={DSColors.primary}, Text "Unlocking…" (DSTypography.body, DSColors.textSecondary, textAlign:'center', marginTop:16)

    Cancel button: always rendered regardless of bleState. Position at the bottom of the screen (marginTop:'auto' or absolute bottom:48). Use react-native-paper Button mode="text" onPress={() => navigation.goBack()} textColor={DSColors.textSecondary} label="Cancel". navigation.goBack() returns to BookingConfirmation.

    All styles via StyleSheet.create with DSColors/DSTypography. No NativeWind.
  </action>
  <verify>
    npx tsc --noEmit (from VoltVenture/ directory) — 0 errors.
    Confirm BLEUnlockScreen.tsx contains 'bleState' and 'clearTimeout' and 'navigation.navigate' targeting 'UnlockSuccess'.
    Confirm BLEUnlockScreen.tsx contains a Cancel button that calls navigation.goBack().
  </verify>
  <acceptance_criteria>
    - BLEUnlockScreen.tsx imports ActivityIndicator from 'react-native' and uses it in 'scanning' and 'connecting' states
    - BLEUnlockScreen.tsx declares const [bleState, setBleState] = useState<'scanning' | 'found' | 'connecting'>('scanning')
    - BLEUnlockScreen.tsx uses three setTimeout calls with cumulative delays of 1500, 2500, and 3500 ms
    - BLEUnlockScreen.tsx cleanup function calls clearTimeout on all three timers
    - BLEUnlockScreen.tsx navigates to 'UnlockSuccess' with { bike } at 3500 ms
    - BLEUnlockScreen.tsx renders a Cancel button in all states that calls navigation.goBack()
    - BLEUnlockScreen.tsx renders MaterialCommunityIcons name="bluetooth" always visible
    - npx tsc --noEmit passes with 0 errors
    - In running app: tapping "Unlock via Bluetooth" shows spinner + "Looking for nearby bikes…", then after 1.5 s shows bike name + "Bike found", then after 1.0 s shows "Unlocking…", then navigates to UnlockSuccess
    - Cancel button at any point returns to BookingConfirmation without navigating to UnlockSuccess
  </acceptance_criteria>
  <done>BLEUnlockScreen auto-advances through scanning → found → connecting → UnlockSuccess in 3.5 s total. Cancel returns to BookingConfirmation. TypeScript reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: BookingConfirmation expiry — verify and complete Snackbar + dismiss wiring</name>
  <files>VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx</files>
  <read_first>
    - VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx (READ THE FULL CURRENT FILE — determine what expiry logic Plan 03-01 implemented; only add what is missing)
  </read_first>
  <action>
    Read the current BookingConfirmationScreen.tsx. Verify the following are present; add or correct anything that is missing or incomplete:

    1. Snackbar visible state: const [snackbarVisible, setSnackbarVisible] = useState(false). If already present, leave it.

    2. secondsLeft === 0 effect: a useEffect with dependency [secondsLeft, navigation] that fires when secondsLeft hits 0:
       - Sets snackbarVisible to true
       - After 2500 ms, calls navigation.getParent()?.goBack()
       - Returns a cleanup that clears the dismiss timeout
       Example:
         useEffect(() => {
           if (secondsLeft !== 0) return;
           setSnackbarVisible(true);
           const t = setTimeout(() => navigation.getParent()?.goBack(), 2500);
           return () => clearTimeout(t);
         }, [secondsLeft, navigation]);

    3. Snackbar component in JSX: Paper Snackbar with visible={snackbarVisible}, onDismiss={() => setSnackbarVisible(false)}, duration={2500}, children="Reservation expired — returning to map". The Snackbar must be a sibling of the ScrollView/main layout, not nested inside a scroll content area — place it at the root View level so it renders over all content.

    If all three items are already correctly implemented from Plan 03-01, make no changes and note "Expiry already complete" in the SUMMARY.

    Do NOT modify the countdown timer logic, bike details layout, unlock CTAs, header, or any other part of the file.
  </action>
  <verify>
    npx tsc --noEmit (from VoltVenture/ directory) — 0 errors.
    Confirm BookingConfirmationScreen.tsx contains 'snackbarVisible' and 'getParent' and 'Reservation expired'.
    Confirm the Snackbar onDismiss handler is present.
  </verify>
  <acceptance_criteria>
    - BookingConfirmationScreen.tsx has a useEffect watching secondsLeft that fires when secondsLeft === 0
    - That effect sets snackbarVisible to true and schedules navigation.getParent()?.goBack() after 2500 ms
    - A Paper Snackbar component is rendered at the root layout level with message "Reservation expired — returning to map"
    - npx tsc --noEmit passes with 0 errors
    - In running app: when the countdown reaches 0:00, a snackbar appears saying "Reservation expired — returning to map" and the BookingStack modal dismisses ~2.5 s later returning the user to the map (Note: testing the full 10-minute timer is optional during development — test by temporarily setting HOLD_DURATION_SECONDS to 5 seconds, confirming the behavior, then restoring to 600)
  </acceptance_criteria>
  <done>Countdown expiry shows Snackbar "Reservation expired — returning to map" and dismisses the BookingStack after 2500 ms. TypeScript reports 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE mock → navigation | Pure UI state machine; no external hardware or data crosses any boundary |
| Timer expiry → navigation | setInterval fires internal callback; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | BLE setTimeout not cleared on Cancel | mitigate | Three clearTimeout calls in useEffect cleanup prevent navigation firing after unmount on Cancel |
| T-03-08 | Denial of Service | Expiry dismiss setTimeout not cleared on manual close | mitigate | secondsLeft === 0 effect returns clearTimeout(t) in cleanup; if user closes BookingConfirmation via X before expiry fires, cleanup runs and the dismiss timeout is cancelled |
| T-03-09 | Spoofing | BLE "unlock" accepting any proximity as success | accept | D-09 explicitly declares this as a mock; real BLE hardware pairing is deferred to v2 backend integration |
</threat_model>

<verification>
After Plan 03-03 completes — full Phase 3 end-to-end verification:
1. npx tsc --noEmit from VoltVenture/ returns 0 errors
2. BLEUnlockScreen.tsx contains bleState, three setTimeout calls, and clearTimeout cleanup
3. BookingConfirmationScreen.tsx contains the secondsLeft === 0 effect with Snackbar + goBack

Full flow smoke test (both paths):
Path A — QR: Reserve → BookingConfirmation (countdown ticking) → Scan QR Code → camera opens → scan QR → Bike unlocked! → Start Ride → back to map
Path B — BLE: Reserve → BookingConfirmation → Unlock via Bluetooth → spinner 1.5 s → Bike found → 1.0 s → Unlocking → 1.0 s → Bike unlocked! → Start Ride → back to map
Path C — Cancel: Any BLE state → Cancel → back to BookingConfirmation (timer still running)
Path D — Expiry: Wait for countdown → Snackbar appears → BookingStack dismisses → map visible
Path E — X button: BookingConfirmation X button → BookingStack dismisses → map visible
</verification>

<success_criteria>
All three unlock paths (QR scan, BLE auto-advance, timer expiry) are end-to-end functional. BLE path auto-advances through three UI states in 3.5 s and reaches UnlockSuccess. Expiry shows a snackbar and dismisses the modal. Cancel on BLE returns to BookingConfirmation without navigating forward. TypeScript compiles with 0 errors.
</success_criteria>

<output>
Create `.planning/phases/03-booking-and-unlock/03-03-SUMMARY.md` when done.
</output>
