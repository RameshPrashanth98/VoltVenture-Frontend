---
phase: 04-active-ride-and-payment
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - VoltVenture/src/screens/app/AccountScreen.tsx
  - VoltVenture/src/screens/app/RideHistoryScreen.tsx
  - VoltVenture/src/screens/app/PaymentMethodsScreen.tsx
autonomous: true
requirements:
  - PAY-02
  - PAY-04

must_haves:
  truths:
    - "AccountScreen has two new tappable rows above Log Out: Ride History (history icon) and Payment Methods (credit-card icon)"
    - "AccountScreen navigation prop is typed as StackScreenProps<AccountStackParamList, 'AccountMain'>"
    - "Tapping Ride History pushes to RideHistoryScreen in the AccountStack"
    - "RideHistoryScreen shows a FlatList of past rides from rideService.getRideHistory(); empty state text shown when list is empty"
    - "PaymentMethodsScreen shows a Visa •••• 4242 card row and a stub Add Payment Method row that shows a Snackbar"
    - "npx tsc --noEmit passes with 0 errors after all files are written"
  artifacts:
    - path: "VoltVenture/src/screens/app/AccountScreen.tsx"
      provides: "AccountMain with Ride History and Payment Methods navigation rows; navigation prop added"
    - path: "VoltVenture/src/screens/app/RideHistoryScreen.tsx"
      provides: "FlatList of RideSummary from rideService; replaces stub from 04-01"
      exports: ["RideHistoryScreen"]
    - path: "VoltVenture/src/screens/app/PaymentMethodsScreen.tsx"
      provides: "Mock Visa 4242 row + Add stub with Snackbar; replaces stub from 04-01"
      exports: ["PaymentMethodsScreen"]
  key_links:
    - from: "VoltVenture/src/screens/app/AccountScreen.tsx"
      to: "RideHistory"
      via: "navigation.navigate('RideHistory') on Ride History row tap"
    - from: "VoltVenture/src/screens/app/AccountScreen.tsx"
      to: "PaymentMethods"
      via: "navigation.navigate('PaymentMethods') on Payment Methods row tap"
    - from: "VoltVenture/src/screens/app/RideHistoryScreen.tsx"
      to: "VoltVenture/src/services/rideService.ts"
      via: "rideService.getRideHistory() synchronous call"
---

<objective>
Complete the Account tab additions: update AccountScreen to navigate to sub-screens, and implement RideHistoryScreen and PaymentMethodsScreen (replacing the stubs from 04-01).

Purpose: Implements PAY-02 (payment method management) and PAY-04 (ride history). Runs in Wave 2 parallel to 04-02 because there are no shared files.

Output: Updated AccountScreen.tsx with new rows and navigation prop; full RideHistoryScreen.tsx and PaymentMethodsScreen.tsx replacing stubs. Zero TypeScript errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-active-ride-and-payment/04-CONTEXT.md
@.planning/phases/04-active-ride-and-payment/04-RESEARCH.md
@.planning/phases/04-active-ride-and-payment/04-PATTERNS.md
@.planning/phases/04-active-ride-and-payment/04-01-SUMMARY.md

<interfaces>
<!-- AccountStackParamList — produced by 04-01 -->
From VoltVenture/src/types/navigation.ts (updated by 04-01):
```typescript
export type AccountStackParamList = {
  AccountMain: undefined;
  RideHistory: undefined;
  PaymentMethods: undefined;
};
export type AccountNavProp = StackNavigationProp<AccountStackParamList>;
```

<!-- rideService interface — produced by 04-01 -->
From VoltVenture/src/services/rideService.ts (created by 04-01):
```typescript
export interface RideService {
  startRide(bike: Bike): Promise<ActiveRide>;
  endRide(rideId: string, bike: Bike, durationSec: number): Promise<RideSummary>;
  getRideHistory(): RideSummary[];   // synchronous — returns in-memory array copy
}
export const rideService: RideService;
```

From VoltVenture/src/types/ride.ts (created by 04-01):
```typescript
export interface RideSummary {
  id: string;
  bikeId: string;
  bikeName: string;
  startTime: string;   // ISO 8601
  endTime: string;
  durationMin: number;
  costEur: number;
  distanceKm: number;
}
```

<!-- AccountScreen current code — key sections -->
From VoltVenture/src/screens/app/AccountScreen.tsx (current, pre-modification):

Existing logoutRow style (lines 96-111 approximate):
```typescript
logoutRow: {
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'space-between',
  paddingHorizontal: 24,
  paddingVertical: 16,
  backgroundColor: DSColors.surface,
  borderTopWidth: 1,
  borderBottomWidth: 1,
  borderColor: DSColors.border,
},
logoutText: {
  fontSize: 16,
  fontWeight: '400',
  color: DSColors.destructive,
},
```

Existing logout row JSX (lines 39-50 approximate):
```typescript
<TouchableOpacity
  style={styles.logoutRow}
  onPress={() => setShowLogout(true)}
  activeOpacity={0.7}
>
  <Text style={styles.logoutText}>Log Out</Text>
  <MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary} />
</TouchableOpacity>
```

Current component signature: export default function AccountScreen() — no navigation prop today.

<!-- Snackbar pattern from BookingConfirmationScreen -->
From VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx (Snackbar usage):
```typescript
const [snackbarVisible, setSnackbarVisible] = useState(false);
// ...
<Snackbar
  visible={snackbarVisible}
  onDismiss={() => setSnackbarVisible(false)}
  duration={2500}
>
  Reservation expired — returning to map
</Snackbar>
```

<!-- DSColors tokens -->
DSColors: primary = '#C6FF2D', background = '#FFFFFF', textPrimary = '#0F0F0F', textSecondary = '#808080', surface = '#F5F5F5', border = '#E0E0E0', destructive = '#E53935'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AccountScreen with navigation prop and new rows</name>
  <files>
    VoltVenture/src/screens/app/AccountScreen.tsx
  </files>
  <read_first>
    VoltVenture/src/screens/app/AccountScreen.tsx — read full file; identify: component signature (line ~14), imports block (lines 1-10), logoutRow TouchableOpacity JSX location and style definitions, useAuthContext usage, showLogout Dialog block
  </read_first>
  <action>
    Read AccountScreen.tsx in full before modifying. Make the following targeted changes — do not rewrite sections that are unchanged.

    Change 1 — Update component signature. Find the line: export default function AccountScreen() and replace with: export default function AccountScreen({ navigation }: import type { StackScreenProps } from '@react-navigation/stack'; type Props = StackScreenProps<AccountStackParamList, 'AccountMain'>; — in practice, add the Props type alias before the function and change the signature to: export default function AccountScreen({ navigation }: Props)

    Change 2 — Add required imports. In the existing import block, add:
      import type { StackScreenProps } from '@react-navigation/stack';
      import type { AccountStackParamList } from '../../types/navigation';
    Keep all existing imports.

    Change 3 — Add two new row TouchableOpacity components immediately ABOVE the existing logout row TouchableOpacity. Both rows use a new style menuRow (not logoutRow — the text color is textPrimary not destructive). Add them as siblings just before the logout TouchableOpacity in the JSX:

    Ride History row:
      TouchableOpacity style={styles.menuRow} onPress={() => navigation.navigate('RideHistory')} activeOpacity={0.7}
        View style={styles.menuRowLeft}
          MaterialCommunityIcons name="history" size={20} color={DSColors.textPrimary}
          Text style={styles.menuRowText} — "Ride History"
        MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary}

    Payment Methods row:
      TouchableOpacity style={styles.menuRow} onPress={() => navigation.navigate('PaymentMethods')} activeOpacity={0.7}
        View style={styles.menuRowLeft}
          MaterialCommunityIcons name="credit-card" size={20} color={DSColors.textPrimary}
          Text style={styles.menuRowText} — "Payment Methods"
        MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary}

    Change 4 — Add new style definitions to the StyleSheet.create object. Add these styles after the existing logoutRow/logoutText styles:

    menuRow: same layout as logoutRow but without borderBottomWidth. Use:
      { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingVertical: 16, backgroundColor: DSColors.surface, borderTopWidth: 1, borderColor: DSColors.border }

    menuRowLeft:
      { flexDirection: 'row', alignItems: 'center', gap: 12 }

    menuRowText:
      { fontSize: 16, fontWeight: '400', color: DSColors.textPrimary }

    Do not change: useAuthContext usage, showLogout Dialog, Dialog buttons, existing styles (container, safeArea, card, heading, subheading, logoutRow, logoutText). Only add new rows and the new prop/imports.
  </action>
  <verify>
    Run from VoltVenture/ directory: npx tsc --noEmit
    Expected: 0 errors. AccountScreen is now typed as StackScreenProps<AccountStackParamList, 'AccountMain'> which matches AccountNavigator.tsx's usage from 04-01.
  </verify>
  <acceptance_criteria>
    AccountScreen.tsx has navigation prop typed as StackScreenProps<AccountStackParamList, 'AccountMain'>. Two new TouchableOpacity rows appear above the Log Out row: Ride History (history icon, navigate to 'RideHistory') and Payment Methods (credit-card icon, navigate to 'PaymentMethods'). menuRow, menuRowLeft, menuRowText styles exist in StyleSheet.create. npx tsc --noEmit produces 0 errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement RideHistoryScreen and PaymentMethodsScreen</name>
  <files>
    VoltVenture/src/screens/app/RideHistoryScreen.tsx,
    VoltVenture/src/screens/app/PaymentMethodsScreen.tsx
  </files>
  <read_first>
    VoltVenture/src/screens/app/AccountScreen.tsx — read after Task 1 changes are saved; extract: SafeAreaView usage, DSColors import path (../../theme/theme), MaterialCommunityIcons import (@expo/vector-icons)
    VoltVenture/src/screens/booking/BookingConfirmationScreen.tsx — read lines 176-183 for Snackbar props: visible, onDismiss, duration, and Snackbar import from 'react-native-paper'
  </read_first>
  <action>
    Replace stub VoltVenture/src/screens/app/RideHistoryScreen.tsx with full implementation.

    Imports:
      React from 'react'
      { View, Text, FlatList, StyleSheet } from 'react-native'
      { SafeAreaView } from 'react-native-safe-area-context'
      { MaterialCommunityIcons } from '@expo/vector-icons'
      type { StackScreenProps } from '@react-navigation/stack'
      type { AccountStackParamList } from '../../types/navigation'
      { DSColors } from '../../theme/theme'
      { rideService } from '../../services/rideService'
      type { RideSummary } from '../../types/ride'

    Type alias: type Props = StackScreenProps<AccountStackParamList, 'RideHistory'>

    Component: export default function RideHistoryScreen({ navigation }: Props)
      (navigation prop accepted but not used — the screen is a leaf with a native back button from AccountNavigator)
      const rides = rideService.getRideHistory();

    Helper function (inside or outside component) to format a date string:
      function formatDate(isoString: string): string — return new Date(isoString).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) — e.g., "Aug 18"

    Helper to format duration minutes to MM:SS:
      function formatDuration(durationMin: number): string — compute totalSec = Math.round(durationMin * 60); mins = Math.floor(totalSec / 60); secs = totalSec % 60; return String(mins).padStart(2,'0') + ':' + String(secs).padStart(2,'0')

    JSX: SafeAreaView style={styles.safeArea} edges={['bottom']} (the top is handled by the native stack header from AccountNavigator):

      FlatList data={rides} keyExtractor={item => item.id}
        ListEmptyComponent: centered View with MaterialCommunityIcons name="history" size={48} color={DSColors.textSecondary} marginBottom:16, then Text "No rides yet — your completed rides will appear here." (textSecondary, textAlign:'center', paddingHorizontal:32, fontSize:15)

        renderItem: each ride row as View style={styles.rideRow}:
          Left side View:
            Text style={styles.rideBikeName} — {item.bikeName}
            Text style={styles.rideMeta} — {formatDate(item.startTime)} · {formatDuration(item.durationMin)}
          Right side Text style={styles.rideCost} — "€{item.costEur.toFixed(2)}"
          Separator: a horizontal View (height:1 backgroundColor:DSColors.border) as ItemSeparatorComponent prop on FlatList

    StyleSheet:
      safeArea: { flex: 1, backgroundColor: DSColors.background }
      emptyContainer: { flex: 1, justifyContent: 'center', alignItems: 'center', paddingTop: 80 }
      emptyText: { fontSize: 15, color: DSColors.textSecondary, textAlign: 'center', paddingHorizontal: 32, marginTop: 16 }
      rideRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingHorizontal: 24, paddingVertical: 14 }
      rideBikeName: { fontSize: 15, fontWeight: '500', color: DSColors.textPrimary }
      rideMeta: { fontSize: 13, color: DSColors.textSecondary, marginTop: 2 }
      rideCost: { fontSize: 15, fontWeight: '600', color: DSColors.textPrimary }
      separator: { height: 1, backgroundColor: DSColors.border, marginHorizontal: 24 }

    ---

    Replace stub VoltVenture/src/screens/app/PaymentMethodsScreen.tsx with full implementation.

    Imports:
      React, { useState } from 'react'
      { View, Text, TouchableOpacity, StyleSheet } from 'react-native'
      { SafeAreaView } from 'react-native-safe-area-context'
      { Snackbar } from 'react-native-paper'
      { MaterialCommunityIcons } from '@expo/vector-icons'
      type { StackScreenProps } from '@react-navigation/stack'
      type { AccountStackParamList } from '../../types/navigation'
      { DSColors } from '../../theme/theme'

    Type alias: type Props = StackScreenProps<AccountStackParamList, 'PaymentMethods'>

    Component: export default function PaymentMethodsScreen({ navigation }: Props)
      const [snackbarVisible, setSnackbarVisible] = useState(false);

    JSX: SafeAreaView style={styles.safeArea} edges={['bottom']}:

      Section heading Text "Saved Methods" (fontSize:12 fontWeight:'600' color:DSColors.textSecondary textTransform:'uppercase' letterSpacing:0.8 paddingHorizontal:24 paddingTop:20 paddingBottom:8)

      Saved card row View style={styles.cardRow}:
        Left: View flexDirection:'row' alignItems:'center' gap:12
          MaterialCommunityIcons name="credit-card" size={22} color={DSColors.textPrimary}
          View:
            Text "Visa •••• 4242" (fontSize:15 fontWeight:'500' color:DSColors.textPrimary)
            Text "Expires 12/26" (fontSize:13 color:DSColors.textSecondary marginTop:2)
        Right: MaterialCommunityIcons name="check-circle" size={18} color={DSColors.primary} (indicates active/default)

      Separator View height:1 backgroundColor:DSColors.border marginHorizontal:0

      Section heading Text "Actions" (same style, paddingTop:20 paddingBottom:8)

      Add Payment Method row — TouchableOpacity style={styles.addRow} onPress={() => setSnackbarVisible(true)} activeOpacity={0.7}:
        Left: View flexDirection:'row' alignItems:'center' gap:12
          MaterialCommunityIcons name="plus-circle-outline" size={22} color={DSColors.textSecondary}
          Text "Add Payment Method" (fontSize:15 fontWeight:'400' color:DSColors.textSecondary)
        Right: MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary}

      Snackbar visible={snackbarVisible} onDismiss={() => setSnackbarVisible(false)} duration={2500}:
        "Payment method management coming soon"

    StyleSheet:
      safeArea: { flex: 1, backgroundColor: DSColors.background }
      sectionHeader: { fontSize: 12, fontWeight: '600', color: DSColors.textSecondary, textTransform: 'uppercase', letterSpacing: 0.8, paddingHorizontal: 24, paddingTop: 20, paddingBottom: 8 }
      cardRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingVertical: 16, backgroundColor: DSColors.surface }
      addRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingVertical: 16, backgroundColor: DSColors.surface }
  </action>
  <verify>
    Run from VoltVenture/ directory: npx tsc --noEmit
    Expected: 0 errors.

    Secondary:
    - grep -n "getRideHistory" VoltVenture/src/screens/app/RideHistoryScreen.tsx — must appear
    - grep -n "ListEmptyComponent" VoltVenture/src/screens/app/RideHistoryScreen.tsx — must appear
    - grep -n "Snackbar" VoltVenture/src/screens/app/PaymentMethodsScreen.tsx — must appear
    - grep -n "coming soon" VoltVenture/src/screens/app/PaymentMethodsScreen.tsx — must appear
  </verify>
  <acceptance_criteria>
    RideHistoryScreen.tsx is a full FlatList implementation (not a stub): calls rideService.getRideHistory(), renders each RideSummary as a row showing bike name, date, duration (MM:SS), and cost; shows empty state text when list is empty; uses SafeAreaView edges={['bottom']} (native header handles top). PaymentMethodsScreen.tsx is a full implementation (not a stub): shows Visa •••• 4242 card row with check-circle; Add Payment Method TouchableOpacity triggers Snackbar "Payment method management coming soon". npx tsc --noEmit from VoltVenture/ produces 0 errors.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccountStack navigation prop → AccountScreen | AccountNavigator provides the typed navigation prop; StackScreenProps<AccountStackParamList, 'AccountMain'> ensures navigate() calls are type-safe |
| rideService.getRideHistory() → RideHistoryScreen | In-memory array; no network; data only flows from rideService.endRide writes in the same session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | RideHistoryScreen displays ride history | accept | In-memory only; resets on app restart; no PII beyond bike name and cost; no persistence risk |
| T-04-07 | Tampering | PaymentMethodsScreen Add stub | accept | Stub shows Snackbar; no actual payment data modified; real card entry is deferred to backend integration phase |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
After both tasks complete, run from VoltVenture/ directory:

npx tsc --noEmit

Expected result: 0 errors.

Secondary checks:
- grep -n "navigate('RideHistory')\|navigate('PaymentMethods')" VoltVenture/src/screens/app/AccountScreen.tsx — must show both calls
- grep -n "StackScreenProps.*AccountMain" VoltVenture/src/screens/app/AccountScreen.tsx — must appear
- grep -n "getRideHistory" VoltVenture/src/screens/app/RideHistoryScreen.tsx — must appear
- grep -n "Snackbar" VoltVenture/src/screens/app/PaymentMethodsScreen.tsx — must appear
</verification>

<success_criteria>
1. AccountScreen.tsx: navigation prop typed as StackScreenProps<AccountStackParamList, 'AccountMain'>; two new rows (Ride History with history icon, Payment Methods with credit-card icon) appear above Log Out row; navigation.navigate('RideHistory') and navigation.navigate('PaymentMethods') wired; menuRow/menuRowLeft/menuRowText styles added
2. RideHistoryScreen.tsx: FlatList with data from rideService.getRideHistory(); each row shows bikeName, date (month + day from startTime), duration (MM:SS from durationMin), cost (€X.XX); ListEmptyComponent shows empty state text; SafeAreaView edges={['bottom']}
3. PaymentMethodsScreen.tsx: Visa •••• 4242 saved card row with credit-card icon and check-circle active indicator; Add Payment Method row that triggers Snackbar "Payment method management coming soon" on tap; SafeAreaView edges={['bottom']}
4. npx tsc --noEmit from VoltVenture/ produces 0 errors
</success_criteria>

<output>
When all tasks pass verification, create `.planning/phases/04-active-ride-and-payment/04-03-SUMMARY.md` documenting:
- AccountScreen changes: navigation prop added, two new rows, styles added
- RideHistoryScreen: FlatList from rideService, empty state, row layout
- PaymentMethodsScreen: mock card row, Add stub with Snackbar
- TypeScript compilation result
- Note that PAY-02 is implemented as stub (view-only) per D-16; real card entry is deferred
</output>
