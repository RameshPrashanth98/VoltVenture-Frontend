---
phase: 05-account-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VoltVenture/src/types/navigation.ts
  - VoltVenture/src/services/userService.ts
  - VoltVenture/src/context/ProfileContext.tsx
  - VoltVenture/src/navigation/AccountNavigator.tsx
  - VoltVenture/src/screens/app/AccountScreen.tsx
  - VoltVenture/src/screens/app/ProfileScreen.tsx
autonomous: true
requirements:
  - PROF-01

must_haves:
  truths:
    - "User can tap the Account tab and see their name and avatar in a profile header block above the menu rows"
    - "Tapping the profile header block navigates to a ProfileScreen showing name, email, and member since date"
    - "Avatar placeholder renders initials 'JT' on a colored circle when no photo is selected"
    - "ProfileScreen displays an 'Edit' button in the top-right header area"
  artifacts:
    - path: "VoltVenture/src/services/userService.ts"
      provides: "mockUser constant — seed data for ProfileContext"
      exports: ["MockUser", "mockUser"]
    - path: "VoltVenture/src/context/ProfileContext.tsx"
      provides: "In-memory profile state shared across AccountScreen, ProfileScreen, EditProfileScreen"
      exports: ["UserProfile", "ProfileProvider", "useProfileContext"]
    - path: "VoltVenture/src/screens/app/ProfileScreen.tsx"
      provides: "View-only profile screen (PROF-01)"
      contains: "useProfileContext"
    - path: "VoltVenture/src/types/navigation.ts"
      provides: "AccountStackParamList extended with Profile, EditProfile, Settings, Preferences"
      contains: "Profile: undefined"
    - path: "VoltVenture/src/navigation/AccountNavigator.tsx"
      provides: "ProfileProvider wrapping Stack.Navigator; 4 new routes registered"
      contains: "ProfileProvider"
  key_links:
    - from: "VoltVenture/src/navigation/AccountNavigator.tsx"
      to: "VoltVenture/src/context/ProfileContext.tsx"
      via: "ProfileProvider wraps Stack.Navigator"
      pattern: "ProfileProvider"
    - from: "VoltVenture/src/screens/app/AccountScreen.tsx"
      to: "VoltVenture/src/context/ProfileContext.tsx"
      via: "useProfileContext() hook"
      pattern: "useProfileContext"
    - from: "VoltVenture/src/screens/app/ProfileScreen.tsx"
      to: "VoltVenture/src/context/ProfileContext.tsx"
      via: "useProfileContext() hook"
      pattern: "useProfileContext"
---

<objective>
Install the two required Expo SDK packages, then build the foundational profile slice: service data, context state, navigation types, navigator extension, AccountScreen header update, and ProfileScreen.

Purpose: Delivers PROF-01 (user can view their profile) and establishes the shared ProfileContext that Plan 02 (EditProfile) and Plan 03 (Settings/Preferences) both depend on. All subsequent Phase 5 plans depend on this wave.

Output:
- expo-image-picker and @react-native-async-storage/async-storage installed
- userService.ts with mockUser export
- ProfileContext.tsx with ProfileProvider + useProfileContext hook
- AccountStackParamList extended with 4 new routes
- AccountNavigator.tsx wrapped with ProfileProvider + 4 new Stack.Screen entries
- AccountScreen.tsx updated with tappable profile header block
- ProfileScreen.tsx (view-only: avatar, name, email, member since, Edit button)
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-UI-SPEC.md

<interfaces>
From VoltVenture/src/types/navigation.ts (AccountStackParamList — current, lines 23-27):
```typescript
export type AccountStackParamList = {
  AccountMain: undefined;
  RideHistory: undefined;
  PaymentMethods: undefined;
};
```

From VoltVenture/src/navigation/AccountNavigator.tsx (current, lines 1-18 — full file):
```typescript
import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';
import type { AccountStackParamList } from '../types/navigation';
import AccountScreen from '../screens/app/AccountScreen';
import RideHistoryScreen from '../screens/app/RideHistoryScreen';
import PaymentMethodsScreen from '../screens/app/PaymentMethodsScreen';

const Stack = createStackNavigator<AccountStackParamList>();

export default function AccountNavigator() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="AccountMain" component={AccountScreen} options={{ headerShown: false }} />
      <Stack.Screen name="RideHistory" component={RideHistoryScreen} options={{ title: 'Ride History' }} />
      <Stack.Screen name="PaymentMethods" component={PaymentMethodsScreen} options={{ title: 'Payment Methods' }} />
    </Stack.Navigator>
  );
}
```

From VoltVenture/src/context/AuthContext.tsx (pattern for ProfileContext — key excerpts):
- createContext, useContext, useState, useMemo, type ReactNode are all imported from 'react'
- Context is created as: createContext<ContextValue>({} as ContextValue)
- Provider uses useMemo for value to avoid re-render churn
- Hook guards with: if (!ctx || !('state' in ctx)) throw new Error(...)
- SecureStore and useReducer are NOT needed for ProfileContext (simpler pattern)

From VoltVenture/src/services/bikeService.ts (pattern for userService.ts):
- File exports a TypeScript type and a const mock data object
- No async functions needed for single-object mock (ProfileContext reads directly)

From VoltVenture/src/screens/app/AccountScreen.tsx (key patterns):
- Imports: SafeAreaView, TouchableOpacity, StyleSheet, MaterialCommunityIcons, DSColors, useAuthContext
- Screen prop type: StackScreenProps<AccountStackParamList, 'AccountMain'>
- Menu row JSX: TouchableOpacity with styles.menuRow > View styles.menuRowLeft (icon + Text) + chevron-right icon
- StyleSheet menuRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingVertical: 16, backgroundColor: DSColors.surface, borderTopWidth: 1, borderColor: DSColors.border }
- Portal > Dialog pattern used for logout confirmation (lines 99-119)

From VoltVenture/src/screens/app/PaymentMethodsScreen.tsx (section header style):
- sectionHeader: { fontSize: 12, fontWeight: '600', color: DSColors.textSecondary, textTransform: 'uppercase', letterSpacing: 0.8, paddingHorizontal: 24, paddingTop: 20, paddingBottom: 8 }

From VoltVenture/src/theme/theme.ts (DSColors tokens for Phase 5):
- background: '#FFFFFF'
- surface: '#FAFAFA'
- textPrimary: '#0F0F0F'
- textSecondary: '#808080'
- border: '#EBEBEB'
- accent: '#7D9220'       (accessible green on white — use for "Edit" button text)
- primary: '#C6FF2D'      (Electric Green — avatar placeholder bg only, NOT text on white)
- textOnPrimary: '#0F0F0F' (black text ON the Electric Green background)
- destructive: '#B00020'
- error: '#D32F2F'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages + create userService + ProfileContext + extend navigation types</name>
  <files>
    VoltVenture/src/services/userService.ts,
    VoltVenture/src/context/ProfileContext.tsx,
    VoltVenture/src/types/navigation.ts
  </files>
  <read_first>
    - VoltVenture/src/types/navigation.ts — read the full current AccountStackParamList (lines 23-27) before editing; must not remove existing routes
    - VoltVenture/src/services/bikeService.ts — read the type + const export pattern to replicate exactly in userService.ts
    - VoltVenture/src/context/AuthContext.tsx — read the createContext/useMemo/useContext/hook-guard pattern to replicate in ProfileContext.tsx
    - VoltVenture/package.json — confirm expo-image-picker and @react-native-async-storage/async-storage are absent before running installs
  </read_first>
  <action>
    Step 0 — Install packages (run from VoltVenture/ directory):
    Run: npx expo install expo-image-picker
    Run: npx expo install @react-native-async-storage/async-storage
    Both commands must complete without error before proceeding. Do not use npm install.

    Step 1 — Create VoltVenture/src/services/userService.ts:
    Export type MockUser with fields: name (string), email (string), avatarUri (string | null), memberSince (string).
    Export const mockUser: MockUser with values: name 'Jamie Torres', email 'jamie@voltventure.app', avatarUri null, memberSince '2026-08-18'.
    No async functions needed. Follow bikeService.ts file structure (type export then const export).

    Step 2 — Create VoltVenture/src/context/ProfileContext.tsx:
    Import createContext, useContext, useState, useMemo, type ReactNode from 'react'.
    Import mockUser from '../services/userService'.
    Export type UserProfile: { name: string; email: string; avatarUri: string | null; memberSince: string }.
    Define type ProfileContextValue: { profile: UserProfile; updateProfile: (patch: Partial<UserProfile>) => void }.
    Create: export const ProfileContext = createContext<ProfileContextValue>({} as ProfileContextValue).
    Export ProfileProvider function — takes { children: ReactNode } prop. Initialize useState<UserProfile> seeded from mockUser fields. Build value with useMemo: { profile, updateProfile: (patch) => setProfile(prev => ({ ...prev, ...patch })) }. Wrap children in ProfileContext.Provider with the memoized value.
    Export useProfileContext(): ProfileContextValue hook — call useContext(ProfileContext), guard with: if (!ctx || !('profile' in ctx)) throw new Error('useProfileContext must be used within ProfileProvider'). Return ctx.

    Step 3 — Edit VoltVenture/src/types/navigation.ts:
    Locate AccountStackParamList (currently has AccountMain, RideHistory, PaymentMethods). Append four new routes before the closing brace: Profile: undefined; EditProfile: undefined; Settings: undefined; Preferences: undefined.
    Do not touch any other types in this file. AccountNavProp continues to reference AccountStackParamList and gains the new routes automatically.
  </action>
  <verify>
    Run from VoltVenture/ directory: node -e "const p = require('./package.json'); console.log(p.dependencies['expo-image-picker'], p.dependencies['@react-native-async-storage/async-storage'])"
    Both values must be non-null strings (not undefined).
    Check: grep -n "Profile\|EditProfile\|Settings\|Preferences" "VoltVenture/src/types/navigation.ts" — must show all 4 new routes.
    Check: grep -n "mockUser\|MockUser" "VoltVenture/src/services/userService.ts" — must show both exports.
    Check: grep -n "ProfileProvider\|useProfileContext\|updateProfile" "VoltVenture/src/context/ProfileContext.tsx" — must show all three identifiers.
  </verify>
  <acceptance_criteria>
    - VoltVenture/package.json contains keys expo-image-picker and @react-native-async-storage/async-storage (non-null version strings)
    - VoltVenture/src/services/userService.ts exports MockUser type and mockUser const with name 'Jamie Torres', email 'jamie@voltventure.app', avatarUri null, memberSince '2026-08-18'
    - VoltVenture/src/context/ProfileContext.tsx exports ProfileProvider function, useProfileContext function, and UserProfile type
    - ProfileContext.tsx contains createContext, useState, useMemo, useContext
    - ProfileContext.tsx contains updateProfile function that calls setProfile with spread-merge: ({ ...prev, ...patch })
    - VoltVenture/src/types/navigation.ts AccountStackParamList contains all 7 routes: AccountMain, RideHistory, PaymentMethods, Profile, EditProfile, Settings, Preferences
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend AccountNavigator + update AccountScreen header + create ProfileScreen</name>
  <files>
    VoltVenture/src/navigation/AccountNavigator.tsx,
    VoltVenture/src/screens/app/AccountScreen.tsx,
    VoltVenture/src/screens/app/ProfileScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/navigation/AccountNavigator.tsx — read full current file before editing; existing 3 routes and import list must be preserved exactly
    - VoltVenture/src/screens/app/AccountScreen.tsx — read full file before editing; identify: (a) where to insert the profile header block in JSX (after title section, before first menu TouchableOpacity), (b) the StyleSheet.create block end position (append new styles there), (c) existing imports to extend
    - VoltVenture/src/context/ProfileContext.tsx — read to confirm export names: ProfileProvider, useProfileContext, UserProfile
    - VoltVenture/src/types/navigation.ts — read to confirm AccountStackParamList now includes Profile and Settings routes
    - VoltVenture/src/screens/app/PaymentMethodsScreen.tsx — read sectionHeader style (lines 83-91) as reference for ProfileScreen field label style
  </read_first>
  <action>
    Step 1 — Edit VoltVenture/src/navigation/AccountNavigator.tsx:
    Add imports at top: ProfileProvider from '../context/ProfileContext'; ProfileScreen from '../screens/app/ProfileScreen'; EditProfileScreen from '../screens/app/EditProfileScreen'; SettingsScreen from '../screens/app/SettingsScreen'; PreferencesScreen from '../screens/app/PreferencesScreen'.
    Wrap the returned JSX: the outermost element becomes ProfileProvider, wrapping the existing Stack.Navigator.
    Append 4 new Stack.Screen entries after the existing PaymentMethods screen, all with options={{ headerShown: false }}: name="Profile" component={ProfileScreen}; name="EditProfile" component={EditProfileScreen}; name="Settings" component={SettingsScreen}; name="Preferences" component={PreferencesScreen}.
    Do not change the existing 3 Screen entries or their options.
    Note: EditProfileScreen, SettingsScreen, PreferencesScreen files do not exist yet — AccountNavigator.tsx will have unresolved imports until Plans 02 and 03 complete. TypeScript compile errors on those imports are expected at this point.

    Step 2 — Edit VoltVenture/src/screens/app/AccountScreen.tsx:
    Add imports: useProfileContext from '../../context/ProfileContext'; Image from 'react-native' (add to existing react-native import).
    Add getInitials helper function before the component definition:
      function getInitials(name: string): string { return name.split(' ').map(part => part[0]).join('').toUpperCase().slice(0, 2); }
    Inside the component, call const { profile } = useProfileContext(); at the top of the function body.
    Insert a profile header TouchableOpacity block in the JSX — position it after the screen title section View and before the first existing menu row TouchableOpacity (Ride History). The TouchableOpacity has: style={styles.profileHeader}, onPress={() => navigation.navigate('Profile')}, activeOpacity={0.7}, accessibilityRole="button", accessibilityLabel="View profile for {profile.name}".
    Inside the header: conditionally render Image (source={{ uri: profile.avatarUri }}, style={styles.avatar}) if avatarUri is set, else a View (style={styles.avatarPlaceholder}) containing a Text (style={styles.avatarInitials}) showing getInitials(profile.name). Next to the avatar, a View (style={styles.profileInfo}) with Text (styles.profileName) showing profile.name and Text (styles.profileEmail) showing profile.email. After the info View, a MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary}.
    Insert a Settings menu row TouchableOpacity before the Log Out row. The row uses the same menu row pattern as Ride History: icon name="cog", label "Settings", chevron-right, onPress={() => navigation.navigate('Settings')}, accessibilityLabel="Settings", accessibilityRole="button".
    Append new styles to the StyleSheet.create block:
      profileHeader: { flexDirection: 'row', alignItems: 'center', paddingHorizontal: 24, paddingVertical: 16, backgroundColor: DSColors.surface, borderTopWidth: 1, borderBottomWidth: 1, borderColor: DSColors.border, gap: 12 }
      profileInfo: { flex: 1 }
      profileName: { fontSize: 20, fontWeight: '600', color: DSColors.textPrimary }
      profileEmail: { fontSize: 15, fontWeight: '400', color: DSColors.textSecondary, marginTop: 2 }
      avatar: { width: 48, height: 48, borderRadius: 24 }
      avatarPlaceholder: { width: 48, height: 48, borderRadius: 24, backgroundColor: DSColors.primary, alignItems: 'center', justifyContent: 'center' }
      avatarInitials: { fontSize: 16, fontWeight: '600', color: DSColors.textOnPrimary }

    Step 3 — Create VoltVenture/src/screens/app/ProfileScreen.tsx:
    Imports: React from 'react'; View, Text, Image, TouchableOpacity, StyleSheet from 'react-native'; SafeAreaView from 'react-native-safe-area-context'; MaterialCommunityIcons from '@expo/vector-icons'; StackScreenProps from '@react-navigation/stack'; AccountStackParamList from '../../types/navigation'; DSColors from '../../theme/theme'; useProfileContext from '../../context/ProfileContext'.
    Screen prop type: type Props = StackScreenProps<AccountStackParamList, 'Profile'>.
    Add getInitials helper (same 5-line function — each screen carries its own local copy).
    Component renders SafeAreaView (style={styles.safeArea}), then:
      Custom header View (styles.header): TouchableOpacity back button (onPress={() => navigation.goBack(), MaterialCommunityIcons arrow-left 24px textPrimary), Text "Profile" (styles.headerTitle), TouchableOpacity "Edit" button (onPress={() => navigation.navigate('EditProfile'), Text styles.editButton with "Edit" copy, accessibilityRole="button", accessibilityLabel="Edit profile").
      Avatar section: View (styles.avatarSection), conditional Image or initials placeholder circle at 80px diameter (width: 80, height: 80, borderRadius: 40). avatarPlaceholder backgroundColor: DSColors.surface (per UI-SPEC — note: ProfileScreen uses DSColors.surface, not DSColors.primary, for the large avatar placeholder; AccountScreen header uses DSColors.primary for the small 48px one). avatarInitials: fontSize: 24, fontWeight: '600', color: DSColors.textPrimary. marginTop: 32 on avatarSection.
      Name Text (styles.profileName — fontSize: 20, fontWeight: '600', textPrimary, textAlign: 'center', marginTop: 16).
      Email Text (styles.profileEmail — fontSize: 15, fontWeight: '400', textSecondary, textAlign: 'center', marginTop: 4).
      Field row section: View with borderTopWidth: 1, borderColor: DSColors.border, marginTop: 32, paddingHorizontal: 24, paddingTop: 20. Inside: Text "MEMBER SINCE" (fontSize: 12, fontWeight: '600', color: DSColors.accent, textTransform: 'uppercase', letterSpacing: 0.8, paddingBottom: 8). Text showing profile.memberSince (fontSize: 15, fontWeight: '400', color: DSColors.textPrimary).
    Styles: safeArea: { flex: 1, backgroundColor: DSColors.background }; header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingTop: 16, paddingBottom: 12 }; headerTitle: { fontSize: 20, fontWeight: '600', color: DSColors.textPrimary }; editButton: { fontSize: 15, fontWeight: '400', color: DSColors.accent }; avatarSection: { alignItems: 'center', marginTop: 32 }.
  </action>
  <verify>
    grep -n "ProfileProvider" "VoltVenture/src/navigation/AccountNavigator.tsx" — must appear (wrapping Stack.Navigator).
    grep -n "name=\"Profile\"\|name=\"EditProfile\"\|name=\"Settings\"\|name=\"Preferences\"" "VoltVenture/src/navigation/AccountNavigator.tsx" — must show all 4.
    grep -n "useProfileContext\|profileHeader\|navigate.*Profile" "VoltVenture/src/screens/app/AccountScreen.tsx" — must show all 3.
    grep -n "navigate.*Settings" "VoltVenture/src/screens/app/AccountScreen.tsx" — must show Settings navigation.
    grep -n "useProfileContext\|memberSince\|navigate.*EditProfile" "VoltVenture/src/screens/app/ProfileScreen.tsx" — must show all 3.
  </verify>
  <acceptance_criteria>
    - AccountNavigator.tsx wraps Stack.Navigator in ProfileProvider
    - AccountNavigator.tsx registers Stack.Screen entries for Profile, EditProfile, Settings, Preferences — all with options={{ headerShown: false }}
    - AccountNavigator.tsx preserves unchanged: AccountMain (headerShown: false), RideHistory (title: 'Ride History'), PaymentMethods (title: 'Payment Methods')
    - AccountScreen.tsx calls useProfileContext() and uses profile.name, profile.email, profile.avatarUri in JSX
    - AccountScreen.tsx profile header TouchableOpacity navigates to 'Profile' on press
    - AccountScreen.tsx Settings menu row navigates to 'Settings' on press, with icon name="cog"
    - AccountScreen.tsx avatar placeholder uses DSColors.primary as backgroundColor and DSColors.textOnPrimary for initials text color (48px circle)
    - ProfileScreen.tsx exists and imports useProfileContext from '../../context/ProfileContext'
    - ProfileScreen.tsx renders profile.name, profile.email, profile.memberSince from context
    - ProfileScreen.tsx "Edit" button navigates to 'EditProfile'
    - ProfileScreen.tsx back button calls navigation.goBack()
    - ProfileScreen.tsx large avatar placeholder (80px) uses DSColors.surface as backgroundColor (not DSColors.primary)
    - ProfileScreen.tsx "MEMBER SINCE" label uses color: DSColors.accent and textTransform: 'uppercase'
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expo-image-picker install | External npm package installed via npx expo install |
| @react-native-async-storage install | External npm package installed via npx expo install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ProfileContext in-memory state | accept | State is mock data; no real PII; resets on app restart |
| T-05-02 | Tampering | npm package install (expo-image-picker) | mitigate | Package verified against npm registry + official Expo v57 docs; [ASSUMED] slopcheck — developer confirms install output is clean |
| T-05-03 | Tampering | npm package install (@react-native-async-storage) | mitigate | Package verified against npm registry + official React Native community org; developer confirms install output |
| T-05-SC | Tampering | npm/expo install commands | mitigate | Both packages tagged [ASSUMED] in legitimacy audit — blocking human checkpoint (see checkpoint below) for verification before install proceeds |
</threat_model>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Package Install Gate: verify expo-image-picker and @react-native-async-storage/async-storage</name>
  <what-built>Task 1 installs two packages via npx expo install. Both packages are [ASSUMED] in the legitimacy audit (slopcheck unavailable on Windows during research).</what-built>
  <how-to-verify>
    1. After Task 1 runs the two install commands, check that npx expo install output shows no unexpected warnings about postinstall scripts or suspicious packages.
    2. Verify: https://www.npmjs.com/package/expo-image-picker — confirm it is listed under the expo org, has weekly downloads in the millions, and the repository links to github.com/expo/expo.
    3. Verify: https://www.npmjs.com/package/@react-native-async-storage/async-storage — confirm it is under the react-native-async-storage org with multi-year history.
    4. Confirm package.json in VoltVenture/ now contains both packages.
  </how-to-verify>
  <resume-signal>Type "packages verified" to proceed with screen implementation, or describe any concerns.</resume-signal>
</task>

<verification>
After both tasks complete:
- Run: npx tsc --noEmit from VoltVenture/ directory (ignore errors about missing EditProfileScreen.tsx, SettingsScreen.tsx, PreferencesScreen.tsx — those are expected until Plans 02 and 03 complete; no other type errors should appear in the files modified by this plan)
- Confirm AccountNavigator.tsx compiles cleanly for the 3 existing routes (ProfileProvider wrapping is valid JSX)
- Confirm ProfileScreen.tsx has no TypeScript errors on useProfileContext() usage
- Confirm userService.ts exports are type-valid
</verification>

<success_criteria>
- expo-image-picker in package.json (non-null version)
- @react-native-async-storage/async-storage in package.json (non-null version)
- AccountStackParamList has 7 routes: AccountMain, RideHistory, PaymentMethods, Profile, EditProfile, Settings, Preferences
- AccountNavigator.tsx renders ProfileProvider > Stack.Navigator with all 7 screens registered
- AccountScreen shows tappable profile header (avatar + name + email) above existing menu rows
- AccountScreen has Settings menu row above Log Out row
- ProfileScreen renders profile.name, profile.email, profile.memberSince from ProfileContext
- ProfileScreen "Edit" button navigates to EditProfile; back button navigates back
- No TypeScript errors in files modified by this plan (excluding unresolved screen imports)
</success_criteria>

<output>
Create `.planning/phases/05-account-profile/05-01-SUMMARY.md` when done.
</output>
