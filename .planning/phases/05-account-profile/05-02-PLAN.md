---
phase: 05-account-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - VoltVenture/src/screens/app/EditProfileScreen.tsx
autonomous: true
requirements:
  - PROF-02

must_haves:
  truths:
    - "User can tap 'Edit' on ProfileScreen and arrive at EditProfileScreen showing their current name and avatar"
    - "User can tap the avatar and open the device photo gallery to select a photo"
    - "Selected photo immediately replaces the initials placeholder on EditProfileScreen"
    - "User can change their display name in the TextInput field"
    - "Tapping 'Save Changes' updates the ProfileContext and navigates back to ProfileScreen showing updated name and avatar"
    - "Tapping back without saving while edits exist shows a 'Discard Changes?' dialog"
    - "Confirming 'Discard' in the dialog navigates back without saving; choosing 'Keep Editing' dismisses the dialog"
  artifacts:
    - path: "VoltVenture/src/screens/app/EditProfileScreen.tsx"
      provides: "Edit profile screen with photo picker and name editing (PROF-02)"
      exports: ["EditProfileScreen (default export)"]
      contains: "ImagePicker.launchImageLibraryAsync"
  key_links:
    - from: "VoltVenture/src/screens/app/EditProfileScreen.tsx"
      to: "VoltVenture/src/context/ProfileContext.tsx"
      via: "updateProfile({ name, avatarUri }) on save"
      pattern: "updateProfile"
    - from: "VoltVenture/src/screens/app/EditProfileScreen.tsx"
      to: "expo-image-picker"
      via: "ImagePicker.launchImageLibraryAsync with mediaTypes: 'images'"
      pattern: "launchImageLibraryAsync"
---

<objective>
Create EditProfileScreen: avatar photo picker via expo-image-picker, display name TextInput with validation, Save Changes PrimaryButton that commits to ProfileContext, and a Discard Changes dialog on back navigation with unsaved edits.

Purpose: Delivers PROF-02 (user can edit display name and photo). Depends on ProfileContext established in Plan 01.

Output:
- EditProfileScreen.tsx (new file) — the only file modified in this plan
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-UI-SPEC.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/05-account-profile/05-01-SUMMARY.md

<interfaces>
From VoltVenture/src/context/ProfileContext.tsx (created in Plan 01):
```typescript
export type UserProfile = {
  name: string;
  email: string;
  avatarUri: string | null;
  memberSince: string;
};

type ProfileContextValue = {
  profile: UserProfile;
  updateProfile: (patch: Partial<UserProfile>) => void;
};

export function useProfileContext(): ProfileContextValue { ... }
```

From VoltVenture/src/types/navigation.ts (AccountStackParamList after Plan 01):
```typescript
export type AccountStackParamList = {
  AccountMain: undefined;
  RideHistory: undefined;
  PaymentMethods: undefined;
  Profile: undefined;
  EditProfile: undefined;
  Settings: undefined;
  Preferences: undefined;
};
```

From VoltVenture/src/components/common/PrimaryButton.tsx (API confirmed):
- Props: label (string), onPress (() => void), loading? (boolean), disabled? (boolean)
- Internal minHeight: 52 applied automatically

From expo-image-picker v57 (verified API):
```typescript
import * as ImagePicker from 'expo-image-picker';

const result = await ImagePicker.launchImageLibraryAsync({
  mediaTypes: 'images',     // string form — NOT MediaTypeOptions.Images (deprecated)
  allowsEditing: true,
  aspect: [1, 1],
  quality: 0.8,
});

// Guard — result.assets may be null on Android permission denial:
if (!result.canceled && result.assets && result.assets.length > 0) {
  const uri = result.assets[0].uri;  // NOT result.uri (removed since SDK ~46)
}
```

From VoltVenture/src/screens/app/AccountScreen.tsx (Portal + Dialog pattern):
```typescript
import { Portal, Dialog, Button } from 'react-native-paper';
// Used in logout confirmation (lines 99-119)
<Portal>
  <Dialog visible={showDialog} onDismiss={() => setShowDialog(false)}>
    <Dialog.Title>...</Dialog.Title>
    <Dialog.Content><Text style={styles.dialogBody}>...</Text></Dialog.Content>
    <Dialog.Actions>
      <Button onPress={() => setShowDialog(false)}>Cancel</Button>
      <Button textColor={DSColors.destructive} onPress={handleConfirm}>Confirm</Button>
    </Dialog.Actions>
  </Dialog>
</Portal>
```

From VoltVenture/src/theme/theme.ts (DSColors tokens):
- background: '#FFFFFF', surface: '#FAFAFA', textPrimary: '#0F0F0F', textSecondary: '#808080'
- border: '#EBEBEB', accent: '#7D9220', primary: '#C6FF2D', textOnPrimary: '#0F0F0F'
- destructive: '#B00020', error: '#D32F2F'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditProfileScreen with photo picker, name input, save, and discard dialog</name>
  <files>VoltVenture/src/screens/app/EditProfileScreen.tsx</files>
  <read_first>
    - VoltVenture/src/context/ProfileContext.tsx — read the useProfileContext hook signature and updateProfile function type before writing the screen; confirm the (patch: Partial<UserProfile>) => void signature
    - VoltVenture/src/screens/app/AccountScreen.tsx — read the Portal > Dialog block (lines 99-119) for exact Discard Changes dialog structure to replicate; read the dialogBody style value
    - VoltVenture/src/components/common/PrimaryButton.tsx — read to confirm: prop names (label, onPress, loading, disabled) and that minHeight: 52 is applied internally
    - VoltVenture/src/types/navigation.ts — confirm EditProfile is in AccountStackParamList with undefined param type
    - VoltVenture/src/theme/theme.ts — read DSColors.error and DSColors.destructive exact values for error text and discard button
    - VoltVenture/src/screens/app/ProfileScreen.tsx — read the custom header pattern (back button + title) established in Plan 01 to match the same header layout
  </read_first>
  <action>
    Create VoltVenture/src/screens/app/EditProfileScreen.tsx as a new file.

    Imports:
      React, useState, useEffect from 'react'
      View, Text, Image, TouchableOpacity, StyleSheet from 'react-native'
      SafeAreaView from 'react-native-safe-area-context'
      Portal, Dialog, Button, TextInput from 'react-native-paper'
      MaterialCommunityIcons from '@expo/vector-icons'
      * as ImagePicker from 'expo-image-picker'
      StackScreenProps from '@react-navigation/stack'
      AccountStackParamList from '../../types/navigation'
      useProfileContext from '../../context/ProfileContext'
      PrimaryButton from '../../components/common/PrimaryButton'
      DSColors from '../../theme/theme'

    Screen prop type: type Props = StackScreenProps<AccountStackParamList, 'EditProfile'>

    getInitials helper (same 5-line function — local copy): splits name by space, takes first char of each part, joins, uppercases, slices to 2.

    State variables:
      const { profile, updateProfile } = useProfileContext()
      const [localName, setLocalName] = useState(profile.name)  — local draft; not committed until Save
      const [localAvatarUri, setLocalAvatarUri] = useState<string | null>(profile.avatarUri)
      const [nameError, setNameError] = useState(false)
      const [isSaving, setIsSaving] = useState(false)
      const [showDiscard, setShowDiscard] = useState(false)

    Derive hasUnsavedChanges: const hasUnsavedChanges = localName !== profile.name || localAvatarUri !== profile.avatarUri

    beforeRemove listener (intercept back navigation):
      useEffect(() => {
        const unsubscribe = navigation.addListener('beforeRemove', (e) => {
          if (!hasUnsavedChanges) return;
          e.preventDefault();
          setShowDiscard(true);
        });
        return unsubscribe;
      }, [navigation, hasUnsavedChanges]);

    handlePickPhoto async function:
      Call ImagePicker.launchImageLibraryAsync with: mediaTypes: 'images' (string, NOT enum), allowsEditing: true, aspect: [1, 1], quality: 0.8.
      Guard: if (!result.canceled && result.assets && result.assets.length > 0) then setLocalAvatarUri(result.assets[0].uri). Do NOT call updateProfile here — photo is staged locally until Save.

    handleSave function:
      Validate: if localName.trim() is empty, setNameError(true) and return early.
      setNameError(false); setIsSaving(true).
      Call updateProfile({ name: localName.trim(), avatarUri: localAvatarUri }).
      setIsSaving(false); navigation.goBack().

    JSX structure:
      SafeAreaView (styles.safeArea) wrapping everything.

      Custom header View (styles.header):
        TouchableOpacity back button: onPress triggers beforeRemove listener naturally by calling navigation.goBack() — which fires the beforeRemove event. So the back button simply calls navigation.goBack(). accessibilityRole="button", accessibilityLabel="Go back".
        Text "Edit Profile" (styles.headerTitle).
        View (width: 40) as spacer to keep title centered (mirror of back button touch target width).

      Avatar section View (styles.avatarSection):
        TouchableOpacity wrapping the avatar: onPress={handlePickPhoto}, accessibilityRole="button", accessibilityLabel="Change profile photo".
        Inside: if localAvatarUri is set, Image (source={{ uri: localAvatarUri }}, styles.avatar — width: 80, height: 80, borderRadius: 40); else View (styles.avatarPlaceholder — width: 80, height: 80, borderRadius: 40, backgroundColor: DSColors.surface) with Text (styles.avatarInitials — fontSize: 24, fontWeight: '600', color: DSColors.textPrimary) showing getInitials(localName).
        Camera badge View (styles.cameraBadge — width: 24, height: 24, borderRadius: 12, backgroundColor: DSColors.surface, position: 'absolute', bottom: 0, right: 0, alignItems: 'center', justifyContent: 'center'): MaterialCommunityIcons name="camera" size={14} color={DSColors.textSecondary}.

      Name input section View (styles.inputSection — paddingHorizontal: 16, marginTop: 24):
        TextInput from react-native-paper: label="Display Name", mode="outlined", value={localName}, onChangeText={(text) => { setLocalName(text); if (text.trim()) setNameError(false); }}, error={nameError}.
        Conditional error Text: if nameError, render Text (styles.errorText — color: DSColors.error, fontSize: 12, marginTop: 4, paddingHorizontal: 4) with "Name can't be empty".

      Spacer View (flex: 1) to push Save button to bottom.

      Save button View (styles.saveSection — paddingHorizontal: 24, paddingBottom: 24):
        PrimaryButton label="Save Changes" onPress={handleSave} loading={isSaving} disabled={isSaving}

      Portal > Dialog (Discard Changes):
        visible={showDiscard}, onDismiss={() => setShowDiscard(false)}.
        Dialog.Title: "Discard Changes?"
        Dialog.Content: Text (styles.dialogBody) "Your edits haven't been saved. Go back without saving?"
        Dialog.Actions:
          Button onPress={() => setShowDiscard(false)}: "Keep Editing"
          Button textColor={DSColors.destructive} onPress={() => { setShowDiscard(false); navigation.goBack(); }}: "Discard"
        NOTE: when Discard is confirmed, call navigation.goBack() WITHOUT removing the beforeRemove listener first — the listener checks hasUnsavedChanges which is still true, so it would re-trigger. Fix: use a ref or set a flag. Implement with a useRef: const discardConfirmed = useRef(false). In the beforeRemove listener: if (discardConfirmed.current) return (allow navigation). In the Discard button handler: set discardConfirmed.current = true then call navigation.goBack().

    Styles (StyleSheet.create):
      safeArea: { flex: 1, backgroundColor: DSColors.background }
      header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 24, paddingTop: 16, paddingBottom: 12 }
      headerTitle: { fontSize: 20, fontWeight: '600', color: DSColors.textPrimary }
      avatarSection: { alignItems: 'center', marginTop: 24 }
      avatar: { width: 80, height: 80, borderRadius: 40 }
      avatarPlaceholder: { width: 80, height: 80, borderRadius: 40, backgroundColor: DSColors.surface, alignItems: 'center', justifyContent: 'center' }
      avatarInitials: { fontSize: 24, fontWeight: '600', color: DSColors.textPrimary }
      cameraBadge: { width: 24, height: 24, borderRadius: 12, backgroundColor: DSColors.surface, position: 'absolute', bottom: 0, right: 0, alignItems: 'center', justifyContent: 'center', borderWidth: 1, borderColor: DSColors.border }
      inputSection: { paddingHorizontal: 16, marginTop: 24 }
      errorText: { color: DSColors.error, fontSize: 12, marginTop: 4, paddingHorizontal: 4 }
      saveSection: { paddingHorizontal: 24, paddingBottom: 24 }
      dialogBody: { fontSize: 16, color: DSColors.textSecondary }
  </action>
  <verify>
    grep -n "launchImageLibraryAsync\|mediaTypes.*images\|assets\[0\]\.uri" "VoltVenture/src/screens/app/EditProfileScreen.tsx"
    All three must appear (confirms correct v57 API usage).
    grep -n "updateProfile\|beforeRemove\|discardConfirmed\|showDiscard" "VoltVenture/src/screens/app/EditProfileScreen.tsx"
    All four must appear (confirms context write, back intercept, discard guard, and dialog visibility state).
    grep -n "nameError\|Name can't be empty\|DSColors\.error" "VoltVenture/src/screens/app/EditProfileScreen.tsx"
    All three must appear (confirms error state implementation).
    grep -n "PrimaryButton\|Save Changes" "VoltVenture/src/screens/app/EditProfileScreen.tsx"
    Both must appear.
  </verify>
  <acceptance_criteria>
    - EditProfileScreen.tsx exists at VoltVenture/src/screens/app/EditProfileScreen.tsx
    - File imports: * as ImagePicker from 'expo-image-picker' (not named import)
    - File calls ImagePicker.launchImageLibraryAsync with mediaTypes: 'images' (string literal — not MediaTypeOptions.Images)
    - File guards image picker result: checks !result.canceled AND result.assets AND result.assets.length > 0 before accessing result.assets[0].uri
    - File uses localAvatarUri state (staged URI) — does NOT call updateProfile in handlePickPhoto; only calls updateProfile in handleSave
    - File calls updateProfile({ name: localName.trim(), avatarUri: localAvatarUri }) in handleSave
    - File implements beforeRemove listener via navigation.addListener('beforeRemove', ...) in a useEffect
    - File uses discardConfirmed ref (or equivalent mechanism) to allow navigation after user confirms Discard
    - File renders Portal > Dialog with visible={showDiscard}, Dialog.Title "Discard Changes?", and two Dialog.Actions buttons ("Keep Editing" and "Discard")
    - Discard button has textColor={DSColors.destructive}
    - File renders TextInput with mode="outlined" and error={nameError}
    - File renders conditional error Text with color DSColors.error and copy "Name can't be empty" when nameError is true
    - File renders PrimaryButton with label="Save Changes" and loading={isSaving}
    - Camera badge View is positioned absolutely over the avatar with MaterialCommunityIcons name="camera"
    - No use of MediaTypeOptions enum anywhere in the file
    - No use of result.uri (legacy shape) anywhere in the file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expo-image-picker result | OS-mediated; returns local URI from device gallery |
| user text input (localName) | Untrusted string from TextInput; must be trimmed and validated before committing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | localName TextInput | mitigate | Validate on save: trim + empty check; show error state if blank; never commit empty name to ProfileContext |
| T-05-05 | Information Disclosure | Local image URI from gallery | accept | URI is local device path only; no network upload; no PII sent anywhere; session-only |
| T-05-06 | Denial of Service | ImagePicker crash on Android permission denial | mitigate | Guard result.assets before access — prevents crash when Android returns null assets after permission denial |
</threat_model>

<verification>
After task completes:
- Run: npx tsc --noEmit from VoltVenture/ directory — EditProfileScreen.tsx should compile without type errors
- Confirm AccountNavigator.tsx now resolves EditProfileScreen import (file exists)
- Manual verification path: Launch app on emulator → Account tab → tap profile header → tap "Edit" → EditProfileScreen opens with correct name pre-filled and avatar section visible
</verification>

<success_criteria>
- EditProfileScreen.tsx compiles without TypeScript errors
- Tapping avatar on EditProfileScreen opens device gallery (expo-image-picker)
- Saving with empty name shows error state "Name can't be empty" and does not navigate back
- Saving with valid name calls updateProfile with trimmed name and staged avatarUri
- After save, ProfileScreen and AccountScreen header reflect updated name and avatar (ProfileContext propagates automatically)
- Tapping back with unsaved edits shows "Discard Changes?" dialog
- Confirming Discard navigates back without saving; "Keep Editing" dismisses dialog
</success_criteria>

<output>
Create `.planning/phases/05-account-profile/05-02-SUMMARY.md` when done.
</output>
