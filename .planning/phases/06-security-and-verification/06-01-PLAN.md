---
phase: 06-security-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VoltVenture/src/types/navigation.ts
  - VoltVenture/src/navigation/AccountNavigator.tsx
  - VoltVenture/src/screens/app/AccountScreen.tsx
  - VoltVenture/src/screens/app/LoginSecurityScreen.tsx
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "User can tap a 'Security' row in AccountScreen and navigate to LoginSecurity"
    - "User can toggle Two-Factor Authentication on and off with Snackbar feedback"
    - "User can see three active session rows with device, location, and timestamp"
    - "User can see ID Scan and Facial Verification rows with Pending badges"
    - "User can see a Security Deposit navigation row at the bottom"
    - "All four new routes are registered in AccountStackParamList and AccountNavigator"
  artifacts:
    - path: "VoltVenture/src/types/navigation.ts"
      provides: "AccountStackParamList with LoginSecurity, IdScan, FacialScan, SecurityDeposit entries"
      contains: "LoginSecurity: undefined"
    - path: "VoltVenture/src/navigation/AccountNavigator.tsx"
      provides: "Four new Stack.Screen registrations with headerShown: false"
      contains: "LoginSecurityScreen"
    - path: "VoltVenture/src/screens/app/AccountScreen.tsx"
      provides: "Security menu row between Settings and Log Out"
      contains: "shield-lock"
    - path: "VoltVenture/src/screens/app/LoginSecurityScreen.tsx"
      provides: "Security hub screen with 2FA, sessions, verification rows, deposit row"
      contains: "twoFAEnabled"
  key_links:
    - from: "AccountScreen.tsx"
      to: "LoginSecurity route"
      via: "navigation.navigate('LoginSecurity')"
      pattern: "navigate.*LoginSecurity"
    - from: "LoginSecurityScreen.tsx"
      to: "IdScan route"
      via: "navigation.navigate('IdScan', { onVerified })"
      pattern: "navigate.*IdScan"
    - from: "LoginSecurityScreen.tsx"
      to: "FacialScan route"
      via: "navigation.navigate('FacialScan', { onVerified })"
      pattern: "navigate.*FacialScan"
---

<objective>
Wire the navigation foundation for Phase 6 and deliver the complete LoginSecurity hub screen.

Purpose: Plans 06-02 and 06-03 cannot register their screens until AccountStackParamList and AccountNavigator are updated. This plan lands all navigation types and the hub screen in Wave 1 so Wave 2 plans can compile.

Output:
- 4 new route entries in AccountStackParamList
- 4 new Stack.Screen registrations in AccountNavigator
- Security menu row added to AccountScreen (between Settings and Log Out)
- LoginSecurityScreen: 2FA toggle + snackbar, 3 session rows, 2 verification rows with badge state, SecurityDeposit nav row
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-and-verification/06-CONTEXT.md
@.planning/phases/06-security-and-verification/06-UI-SPEC.md
@.planning/phases/06-security-and-verification/06-RESEARCH.md

<interfaces>
From VoltVenture/src/types/navigation.ts (current state — must extend, not replace):

  export type AccountStackParamList = {
    AccountMain: undefined;
    RideHistory: undefined;
    PaymentMethods: undefined;
    Profile: undefined;
    EditProfile: undefined;
    Settings: undefined;
    Preferences: undefined;
    // ADD BELOW (per D-02):
    LoginSecurity: undefined;
    IdScan: { onVerified: () => void };
    FacialScan: { onVerified: () => void };
    SecurityDeposit: undefined;
  };

From VoltVenture/src/navigation/AccountNavigator.tsx (current state — add 4 screens inside Stack.Navigator):

  const Stack = createStackNavigator<AccountStackParamList>();
  // existing screens: AccountMain, RideHistory, PaymentMethods, Profile,
  //                   EditProfile, Settings, Preferences
  // ADD (per D-02): LoginSecurity, IdScan, FacialScan, SecurityDeposit
  //   all with options={{ headerShown: false }}

From VoltVenture/src/screens/app/AccountScreen.tsx (insertion point):
  // Security row inserts BEFORE the Log Out block at line 126
  // Uses styles.menuRow (already defined), styles.menuRowLeft, styles.menuRowText
  // MaterialCommunityIcons already imported

From VoltVenture/src/screens/app/PreferencesScreen.tsx (Switch + toggle row pattern):
  import { Switch } from 'react-native-paper';
  // toggleRow, toggleRowLeft, toggleLabelGroup, toggleLabel, toggleDescription styles
  // Switch: trackColor={{ true: DSColors.primary, false: DSColors.border }}, thumbColor={DSColors.background}

From VoltVenture/src/components/common/PrimaryButton.tsx:
  export interface PrimaryButtonProps {
    label: string;
    onPress: () => void;
    loading?: boolean;
    disabled?: boolean;
    testID?: string;
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend navigation types and AccountNavigator with 4 new routes</name>
  <files>
    VoltVenture/src/types/navigation.ts
    VoltVenture/src/navigation/AccountNavigator.tsx
  </files>
  <read_first>
    - VoltVenture/src/types/navigation.ts — read full file; append to AccountStackParamList only
    - VoltVenture/src/navigation/AccountNavigator.tsx — read full file; add 4 imports + 4 Stack.Screen elements
  </read_first>
  <action>
    In navigation.ts: Add four entries to AccountStackParamList. Append them after the existing `Preferences: undefined;` line. The exact entries are: `LoginSecurity: undefined;`, `IdScan: { onVerified: () => void };`, `FacialScan: { onVerified: () => void };`, `SecurityDeposit: undefined;`. Do not remove or reorder existing entries.

    In AccountNavigator.tsx: Add four import statements after the existing screen imports — import LoginSecurityScreen from `'../screens/app/LoginSecurityScreen'`, IdScanScreen from `'../screens/app/IdScanScreen'`, FacialScanScreen from `'../screens/app/FacialScanScreen'`, SecurityDepositScreen from `'../screens/app/SecurityDepositScreen'`. Inside Stack.Navigator, add four Stack.Screen elements after the existing Preferences screen: `name="LoginSecurity"`, `name="IdScan"`, `name="FacialScan"`, `name="SecurityDeposit"` — each with `options={{ headerShown: false }}`. The import paths match files that Plans 06-02 and 06-03 will create; TypeScript compilation completes once those plans land.
  </action>
  <verify>
    Check navigation.ts contains all four new entries: grep -c "LoginSecurity\|IdScan\|FacialScan\|SecurityDeposit" VoltVenture/src/types/navigation.ts (expect 4). Check AccountNavigator.tsx contains the four Stack.Screen registrations: grep -c "LoginSecurity\|IdScan\|FacialScan\|SecurityDeposit" VoltVenture/src/navigation/AccountNavigator.tsx (expect 8 — 4 imports + 4 Stack.Screen name props).
  </verify>
  <acceptance_criteria>
    - navigation.ts AccountStackParamList contains exactly these four new keys: LoginSecurity, IdScan, FacialScan, SecurityDeposit
    - IdScan entry type is `{ onVerified: () => void }` (not undefined)
    - FacialScan entry type is `{ onVerified: () => void }` (not undefined)
    - AccountNavigator.tsx imports LoginSecurityScreen, IdScanScreen, FacialScanScreen, SecurityDepositScreen
    - AccountNavigator.tsx has four Stack.Screen elements with names LoginSecurity, IdScan, FacialScan, SecurityDeposit, each with options={{ headerShown: false }}
    - No existing AccountStackParamList entry is removed or altered
  </acceptance_criteria>
  <done>Both files compile without TypeScript errors on their own (existing screen files remain valid; new screen files may be absent until Plans 06-02/06-03 execute).</done>
</task>

<task type="auto">
  <name>Task 2: Add Security menu row to AccountScreen</name>
  <files>
    VoltVenture/src/screens/app/AccountScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/screens/app/AccountScreen.tsx — read full file to locate the exact insertion point (line ~126, before the Log Out TouchableOpacity block)
  </read_first>
  <action>
    Insert a new TouchableOpacity block immediately before the existing Log Out row (the block beginning with `{/* Log Out row */}`). The new block is the Security row per D-01.

    The Security row uses: `style={styles.menuRow}`, `onPress={() => navigation.navigate('LoginSecurity')}`, `activeOpacity={0.7}`, `accessibilityRole="button"`, `accessibilityLabel="Security"`. Left side: MaterialCommunityIcons `name="shield-lock"` size 20 color DSColors.textPrimary inside a View with style menuRowLeft, Text "Security" with style menuRowText. Right side: MaterialCommunityIcons `name="chevron-right"` size 20 color DSColors.textSecondary. This follows the identical pattern of the existing Settings row at lines 112–124. No other changes to AccountScreen.
  </action>
  <verify>
    grep -n "shield-lock" VoltVenture/src/screens/app/AccountScreen.tsx — must find one match. grep -n "LoginSecurity" VoltVenture/src/screens/app/AccountScreen.tsx — must find one match. Visually confirm the Security row appears between Settings and Log Out by checking line order in the file.
  </verify>
  <acceptance_criteria>
    - AccountScreen.tsx contains a TouchableOpacity with accessibilityLabel="Security"
    - That TouchableOpacity calls navigation.navigate('LoginSecurity') on press
    - MaterialCommunityIcons name="shield-lock" is present in the Security row
    - The Security row appears after the Settings row and before the Log Out row in the JSX (source order confirms this)
    - No existing rows are removed; only one insertion made
  </acceptance_criteria>
  <done>AccountScreen renders: Profile header → Ride History → Payment Methods → Settings → Security → Log Out.</done>
</task>

<task type="auto">
  <name>Task 3: Create LoginSecurityScreen — 2FA toggle, sessions, verification rows, deposit row</name>
  <files>
    VoltVenture/src/screens/app/LoginSecurityScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/screens/app/PreferencesScreen.tsx — read for Switch import, toggleRow/toggleRowLeft/toggleLabelGroup/toggleLabel/toggleDescription style patterns, header pattern
    - VoltVenture/src/screens/app/SettingsScreen.tsx — read for sectionHeader style (fontSize 12, fontWeight '600', color DSColors.accent, textTransform uppercase, letterSpacing 0.8)
    - VoltVenture/src/screens/app/AccountScreen.tsx — read for menuRow, menuRowLeft, menuRowText style definitions
    - VoltVenture/src/theme/theme.ts — confirm DSColors token values
  </read_first>
  <action>
    Create VoltVenture/src/screens/app/LoginSecurityScreen.tsx as a new file. Use `StackScreenProps<AccountStackParamList, 'LoginSecurity'>` for the Props type.

    File-scoped constant at top (before the component): `const AMBER = '#F59E0B';`

    State shape (all useState): `twoFAEnabled` (boolean, false), `idVerified` (boolean, false), `faceVerified` (boolean, false), `snackVisible` (boolean, false), `snackMessage` (string, '').

    Root structure: SafeAreaView (flex:1, backgroundColor: DSColors.background) containing a View (flex:1) with: the custom header at top, then a ScrollView (showsVerticalScrollIndicator: false) containing all four sections.

    Custom header: View with styles.header (flexDirection: row, alignItems: center, justifyContent: space-between, paddingHorizontal: 24, paddingTop: 16, paddingBottom: 12). Left: TouchableOpacity onPress={navigation.goBack} accessibilityRole="button" accessibilityLabel="Go back" containing MaterialCommunityIcons name="arrow-left" size={24} color={DSColors.textPrimary}. Center: Text "Security" with styles.headerTitle (fontSize: 20, fontWeight: '600', color: DSColors.textPrimary). Right: View width={40}.

    Section 1 — ACCOUNT SECURITY: Text "ACCOUNT SECURITY" with styles.sectionHeader. Toggle row: View with styles.toggleRow (same pattern as PreferencesScreen toggleRow — flexDirection: row, alignItems: center, justifyContent: space-between, paddingHorizontal: 24, paddingVertical: 16, backgroundColor: DSColors.surface, borderTopWidth: 1, borderColor: DSColors.border). Left: View styles.toggleRowLeft (flexDirection: row, alignItems: center, flex: 1, gap: 12) containing MaterialCommunityIcons name="shield-account" size={20} color={DSColors.textPrimary} and View styles.toggleLabelGroup (flex: 1) containing Text "Two-Factor Authentication" (styles.toggleLabel: fontSize 16, fontWeight '400', color textPrimary) and Text "Require a code when signing in" (styles.toggleDescription: fontSize 13, fontWeight '400', color textSecondary, marginTop: 2). Right: Switch value={twoFAEnabled} onValueChange={handleTwoFAToggle} trackColor={{ true: DSColors.primary, false: DSColors.border }} thumbColor={DSColors.background} accessibilityLabel={`Two-Factor Authentication, currently ${twoFAEnabled ? 'on' : 'off'}`}.

    handleTwoFAToggle function: sets twoFAEnabled to the new value, then calls setSnackMessage and setSnackVisible(true). Message: "Two-factor authentication enabled" when turning on, "Two-factor authentication disabled" when turning off.

    Section 2 — ACTIVE SESSIONS: Text "ACTIVE SESSIONS" with styles.sectionHeader. Three session rows — each a non-interactive View (not TouchableOpacity) with styles.menuRow style. Left: MaterialCommunityIcons (sizes 24, color textSecondary) — `cellphone` for iPhone 14 Pro, `monitor` for Chrome on Windows, `tablet` for iPad Air. Middle: View flex:1, containing Text for device name (fontSize 15, fontWeight '600', color textPrimary) and a second row with location + timestamp. The three session data items: (1) device "iPhone 14 Pro", location "Bangkok, Thailand", timestamp "Active now" — timestamp color is DSColors.accent; (2) device "Chrome on Windows", location "Chiang Mai, Thailand", timestamp "2 days ago" — timestamp color is DSColors.textSecondary; (3) device "iPad Air", location "Phuket, Thailand", timestamp "5 days ago" — timestamp color is DSColors.textSecondary. Timestamp format: Text "{location} · {timestamp}" or two separate Text elements for location and timestamp. Right side: nothing (no chevron, session rows are non-interactive).

    Section 3 — IDENTITY VERIFICATION: Text "IDENTITY VERIFICATION" with styles.sectionHeader. Two TouchableOpacity rows with styles.menuRow. Row 1: onPress={() => navigation.navigate('IdScan', { onVerified: () => setIdVerified(true) })} accessibilityRole="button" accessibilityLabel={`Verify Identity, status ${idVerified ? 'Verified' : 'Pending'}`}. Left: MaterialCommunityIcons name="fingerprint" size={20} color={DSColors.textPrimary} + Text "Verify Identity (ID Scan)" (styles.menuRowText). Right: View (flexDirection: row, alignItems: center, gap: 8) containing the badge pill and MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary}. Row 2: onPress={() => navigation.navigate('FacialScan', { onVerified: () => setFaceVerified(true) })} accessibilityRole="button" accessibilityLabel={`Facial Verification, status ${faceVerified ? 'Verified' : 'Pending'}`}. Left: MaterialCommunityIcons name="face-recognition" size={20} color={DSColors.textPrimary} + Text "Facial Verification". Right: same badge + chevron pattern.

    Badge pill for each verification row: inline View with style [styles.badge, { backgroundColor: verified ? DSColors.primary : AMBER }] and Text with style [styles.badgeText, { color: verified ? DSColors.textOnPrimary : '#FFFFFF' }] showing 'Verified' or 'Pending'.

    Badge styles: `badge: { paddingHorizontal: 10, paddingVertical: 3, borderRadius: 12 }`, `badgeText: { fontSize: 12, fontWeight: '600' }`.

    Section 4 — SECURITY: Text "SECURITY" with styles.sectionHeader. One TouchableOpacity row with styles.menuRow: onPress={() => navigation.navigate('SecurityDeposit')} accessibilityRole="button" accessibilityLabel="Security Deposit". Left: MaterialCommunityIcons name="bank" size={20} color={DSColors.textPrimary} + Text "Security Deposit" (styles.menuRowText). Right: MaterialCommunityIcons name="chevron-right" size={20} color={DSColors.textSecondary}.

    Snackbar: At the bottom of SafeAreaView (outside ScrollView), render Portal containing Snackbar with visible={snackVisible} onDismiss={() => setSnackVisible(false)} duration={3000} showing {snackMessage}.

    All imports: React, useState from react; View, Text, TouchableOpacity, ScrollView, StyleSheet from react-native; SafeAreaView from react-native-safe-area-context; Switch, Snackbar, Portal from react-native-paper; MaterialCommunityIcons from @expo/vector-icons; StackScreenProps from @react-navigation/stack; AccountStackParamList from ../../types/navigation; DSColors from ../../theme/theme.
  </action>
  <verify>
    npx tsc --noEmit --project VoltVenture/tsconfig.json 2>&1 | grep "LoginSecurityScreen" — expect zero errors on this file. grep -c "AMBER\|twoFAEnabled\|idVerified\|faceVerified\|snackVisible" VoltVenture/src/screens/app/LoginSecurityScreen.tsx (expect 5 matches minimum — one declaration each).
  </verify>
  <acceptance_criteria>
    - File exists at VoltVenture/src/screens/app/LoginSecurityScreen.tsx
    - `const AMBER = '#F59E0B';` is defined as a file-scoped constant before the component function
    - useState declarations present for: twoFAEnabled (false), idVerified (false), faceVerified (false), snackVisible (false), snackMessage ('')
    - Switch imported from 'react-native-paper'; trackColor prop includes `true: DSColors.primary` and `false: DSColors.border`
    - handleTwoFAToggle sets snackMessage to exactly "Two-factor authentication enabled" when turning on, and "Two-factor authentication disabled" when turning off
    - Three session rows use device icons: cellphone, monitor, tablet — in that order
    - "Active now" timestamp text uses color DSColors.accent; other timestamps use DSColors.textSecondary
    - ID Scan row calls navigation.navigate('IdScan', { onVerified: () => setIdVerified(true) }) on press
    - FacialScan row calls navigation.navigate('FacialScan', { onVerified: () => setFaceVerified(true) }) on press
    - SecurityDeposit row calls navigation.navigate('SecurityDeposit') on press
    - Badge pill uses DSColors.primary background + DSColors.textOnPrimary text when verified=true; AMBER background + '#FFFFFF' text when verified=false
    - Portal + Snackbar renders with duration={3000} and onDismiss closing snackVisible
    - SafeAreaView is the root element with flex:1 and backgroundColor: DSColors.background
    - No NativeWind className props anywhere in the file
    - No AsyncStorage import or usage
  </acceptance_criteria>
  <done>LoginSecurity screen renders all four sections. Toggling 2FA produces correct snackbar message. Tapping IdScan or FacialScan rows navigates with onVerified callback. Tapping SecurityDeposit row navigates to SecurityDeposit. Session rows are non-interactive and display correct mock data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → state | 2FA toggle and Refund button write only to in-memory React state — no external sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | LoginSecurity navigation params (onVerified callback) | accept | Callbacks are in-memory function refs within a single stack; no cross-origin call. Project has no deep linking. No user-supplied values flow to privileged operations. |
| T-06-02 | Information Disclosure | Mock session data (device names, locations) | accept | Data is hardcoded mock strings — no real PII in source. Acceptable for frontend-only MVP scope. |
| T-06-SC | Tampering | npm installs | accept | No new packages installed in Phase 6 — all dependencies are already in package.json and previously verified. |
</threat_model>

<verification>
After all three tasks complete:
1. `npx tsc --noEmit --project VoltVenture/tsconfig.json` — zero errors
2. AccountStackParamList has exactly 11 entries (7 existing + 4 new): grep -c "undefined\|onVerified" VoltVenture/src/types/navigation.ts
3. AccountNavigator has 11 Stack.Screen elements (7 existing + 4 new): grep -c "Stack.Screen" VoltVenture/src/navigation/AccountNavigator.tsx
4. Security row appears between Settings and Log Out in AccountScreen source order
5. LoginSecurityScreen.tsx contains all five state variables and the AMBER constant
</verification>

<success_criteria>
- User can open AccountScreen and see Security row between Settings and Log Out
- Tapping Security row navigates to LoginSecurity hub
- 2FA toggle produces correct snackbar messages ("Two-factor authentication enabled" / "...disabled")
- Three session rows show correct device/location/timestamp with Active now in accent color
- Verification rows navigate to IdScan and FacialScan with onVerified callbacks
- SecurityDeposit row navigates to SecurityDeposit screen
- TypeScript compiles with zero errors
</success_criteria>

<output>
Create `.planning/phases/06-security-and-verification/06-01-SUMMARY.md` when done.
</output>
