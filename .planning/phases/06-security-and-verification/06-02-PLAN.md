---
phase: 06-security-and-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - VoltVenture/src/screens/app/IdScanScreen.tsx
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "User sees a dark viewfinder with landscape rectangle and corner brackets in idle state"
    - "User sees instructional text 'Position your ID document within the frame.'"
    - "Tapping Capture triggers a 1500ms ActivityIndicator overlay with 'Verifying...' text"
    - "After 1500ms, the viewfinder area is replaced with a green checkmark and 'Identity Verified' text"
    - "Tapping Continue calls onVerified and returns to LoginSecurity with the ID Scan badge updated to Verified"
  artifacts:
    - path: "VoltVenture/src/screens/app/IdScanScreen.tsx"
      provides: "ID document mock viewfinder screen with 3-state state machine"
      contains: "ScanState"
  key_links:
    - from: "IdScanScreen.tsx"
      to: "LoginSecurityScreen.tsx"
      via: "route.params.onVerified() then navigation.goBack()"
      pattern: "onVerified.*goBack\|goBack.*onVerified"
---

<objective>
Deliver the IdScan screen: a mock camera viewfinder for ID document scanning with a 3-state flow (idle → verifying → success) and a callback to update LoginSecurity's badge on completion.

Purpose: Satisfies SEC-02 — user can scan an ID document and see a mock verified result. Depends on Plan 06-01 having registered the IdScan route and navigation types.

Output:
- IdScanScreen.tsx: landscape viewfinder, corner brackets, capture state machine, success state, Continue callback
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-security-and-verification/06-CONTEXT.md
@.planning/phases/06-security-and-verification/06-UI-SPEC.md
@.planning/phases/06-security-and-verification/06-RESEARCH.md

<interfaces>
From VoltVenture/src/types/navigation.ts (as updated by Plan 06-01):

  AccountStackParamList includes:
    IdScan: { onVerified: () => void };

Screen props type:
  type Props = StackScreenProps<AccountStackParamList, 'IdScan'>;
  // gives: navigation (stack nav prop) + route (with params.onVerified)

From VoltVenture/src/components/common/PrimaryButton.tsx:
  export interface PrimaryButtonProps {
    label: string;
    onPress: () => void;
    loading?: boolean;
    disabled?: boolean;
    testID?: string;
  }
  // Use: PrimaryButton label="Capture" onPress={startCapture}
  // Use: PrimaryButton label="Continue" onPress={handleContinue}
  // Wrap in View with paddingHorizontal: 24, paddingTop: 24 for correct width context

From VoltVenture/src/screens/app/SettingsScreen.tsx (custom header pattern):
  styles.header = { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between',
    paddingHorizontal: 24, paddingTop: 16, paddingBottom: 12 }
  styles.headerTitle = { fontSize: 20, fontWeight: '600', color: DSColors.textPrimary }
  Back button: MaterialCommunityIcons name="arrow-left" size={24} color={DSColors.textPrimary}
  Spacer: View style={{ width: 40 }}

State machine type (define in file):
  type ScanState = 'idle' | 'verifying' | 'success';

Viewfinder constants (define in file):
  const VIEWFINDER_BG = DSColors.textPrimary;  // #0F0F0F — dark camera bg
  const WHITE = '#FFFFFF';                      // corner brackets, overlay text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IdScanScreen with viewfinder mock and capture state machine</name>
  <files>
    VoltVenture/src/screens/app/IdScanScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/screens/app/SettingsScreen.tsx — read for the exact custom header implementation (lines 53–66) and header/headerTitle style definitions
    - VoltVenture/src/screens/app/AccountScreen.tsx — read for SafeAreaView root pattern and DSColors import path
    - VoltVenture/src/components/common/PrimaryButton.tsx — read for props interface (label, onPress, disabled)
    - VoltVenture/src/theme/theme.ts — read for DSColors.primary, DSColors.textPrimary, DSColors.textSecondary, DSColors.background token values
  </read_first>
  <action>
    Create VoltVenture/src/screens/app/IdScanScreen.tsx as a new file.

    File-scoped constants (before the component): `const VIEWFINDER_BG = DSColors.textPrimary;` and `const WHITE = '#FFFFFF';`. Note: DSColors must be imported before these constants are used — define them inside the module after imports, not at module top-level before imports.

    State: `const [scanState, setScanState] = useState<ScanState>('idle');` where `type ScanState = 'idle' | 'verifying' | 'success';` is declared before the component.

    Props type: `type Props = StackScreenProps<AccountStackParamList, 'IdScan'>;`

    startCapture function: calls setScanState('verifying'), then setTimeout(() => setScanState('success'), 1500).

    handleContinue function: calls `route.params.onVerified?.()` then `navigation.goBack()`.

    Root layout: SafeAreaView (flex:1, backgroundColor: DSColors.background) containing a View (flex:1). Inside that View: custom header, then instruction text, then viewfinder area (conditional on scanState), then button area.

    Custom header: identical pattern to SettingsScreen — View styles.header, TouchableOpacity with goBack, Text "ID Verification" with styles.headerTitle, View width={40}.

    Instruction text (shown when scanState !== 'success'): Text "Position your ID document within the frame." — style: fontSize 15, fontWeight '400', color DSColors.textSecondary, textAlign 'center', paddingHorizontal 24, paddingVertical 16.

    Viewfinder (shown when scanState === 'idle' or 'verifying'): View with styles.viewfinder: backgroundColor: VIEWFINDER_BG, width: '100%', aspectRatio: 1.6, position: 'relative' (sets a containing block for absolute children). Inside: four corner bracket Views (each with position: 'absolute', width: 20, height: 20, borderColor: WHITE, borderWidth: 0 as base). Top-left: top:12, left:12, borderTopWidth:3, borderLeftWidth:3. Top-right: top:12, right:12, borderTopWidth:3, borderRightWidth:3. Bottom-left: bottom:12, left:12, borderBottomWidth:3, borderLeftWidth:3. Bottom-right: bottom:12, right:12, borderBottomWidth:3, borderRightWidth:3. When scanState === 'verifying', additionally render an overlay View inside the viewfinder with style: { ...StyleSheet.absoluteFillObject, backgroundColor: 'rgba(0,0,0,0.6)', alignItems: 'center', justifyContent: 'center', gap: 12 } containing ActivityIndicator size="large" color={DSColors.primary} and Text "Verifying..." (fontSize 15, fontWeight '400', color WHITE).

    Success card (shown when scanState === 'success'): replace viewfinder with View styles.successCard: alignItems 'center', justifyContent 'center', paddingVertical 48, gap 16. Contains: MaterialCommunityIcons name="check-circle" size={72} color={DSColors.primary}, Text "Identity Verified" (fontSize 20, fontWeight '700', color DSColors.textPrimary).

    Button area (paddingHorizontal: 24, paddingTop: 24): When scanState === 'idle', render PrimaryButton label="Capture" onPress={startCapture}. When scanState === 'success', render PrimaryButton label="Continue" onPress={handleContinue}. When scanState === 'verifying', render nothing (button hidden during processing).

    All imports: React, useState from react; View, Text, TouchableOpacity, StyleSheet, ActivityIndicator from react-native; SafeAreaView from react-native-safe-area-context; MaterialCommunityIcons from @expo/vector-icons; StackScreenProps from @react-navigation/stack; AccountStackParamList from ../../types/navigation; DSColors from ../../theme/theme; PrimaryButton from ../../components/common/PrimaryButton.

    Do NOT import expo-camera, Camera, or any camera library. The viewfinder is a styled View only.
  </action>
  <verify>
    File exists: test -f VoltVenture/src/screens/app/IdScanScreen.tsx and echo "EXISTS". TypeScript check: npx tsc --noEmit --project VoltVenture/tsconfig.json 2>&1 | grep "IdScanScreen" — expect zero errors. Check state machine type: grep -c "ScanState\|verifying\|success" VoltVenture/src/screens/app/IdScanScreen.tsx (expect at minimum 3 matches). Check no camera import: grep "expo-camera\|Camera" VoltVenture/src/screens/app/IdScanScreen.tsx — expect zero matches.
  </verify>
  <acceptance_criteria>
    - File exists at VoltVenture/src/screens/app/IdScanScreen.tsx
    - `type ScanState = 'idle' | 'verifying' | 'success'` is defined
    - `const VIEWFINDER_BG = DSColors.textPrimary` is defined at module scope (after imports)
    - `const WHITE = '#FFFFFF'` is defined at module scope (after imports)
    - startCapture sets scanState to 'verifying' then calls setTimeout with 1500ms delay to set 'success'
    - handleContinue calls `route.params.onVerified?.()` before `navigation.goBack()`
    - Viewfinder View has aspectRatio: 1.6 and backgroundColor: VIEWFINDER_BG
    - Four corner bracket Views are present with borderColor: WHITE and correct selective borderWidth properties (borderTopWidth/borderLeftWidth for top-left, etc.)
    - Verifying overlay uses StyleSheet.absoluteFillObject spread (not referenced directly as a style value)
    - ActivityIndicator has color={DSColors.primary} and size="large"
    - "Verifying..." text color is WHITE (not DSColors)
    - Success card contains MaterialCommunityIcons name="check-circle" size={72} color={DSColors.primary}
    - Success text "Identity Verified" has fontSize 20, fontWeight '700', color DSColors.textPrimary
    - Button area shows "Capture" in idle state, nothing in verifying state, "Continue" in success state
    - Instruction text "Position your ID document within the frame." is present and hidden in success state
    - No expo-camera import exists in the file
    - No NativeWind className props
    - No AsyncStorage usage
    - SafeAreaView is root element with backgroundColor: DSColors.background
    - Header title text is exactly "ID Verification"
  </acceptance_criteria>
  <done>IdScan screen renders correctly in all three states. Tapping Capture shows ActivityIndicator for ~1.5s then shows success state. Tapping Continue calls onVerified and returns to LoginSecurity where the ID Scan badge updates to Verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.params.onVerified | Function reference passed from LoginSecurity via navigation params — in-memory only, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Spoofing | onVerified callback invocation | accept | Callback originates from LoginSecurity in the same React tree. No user-controlled data flows into the call. The optional chaining `?.()` prevents crashes if somehow undefined. |
| T-06-02-02 | Denial of Service | setTimeout(1500ms) | accept | Single 1500ms timeout in local state. Cannot be externally triggered repeatedly; setScanState('verifying') is idempotent. No memory leak risk — component unmounts cleanly. |
| T-06-SC | Tampering | npm installs | accept | No new packages installed — all dependencies pre-existing. |
</threat_model>

<verification>
After this plan completes:
1. `npx tsc --noEmit --project VoltVenture/tsconfig.json` — zero errors on IdScanScreen.tsx
2. `grep "expo-camera\|Camera" VoltVenture/src/screens/app/IdScanScreen.tsx` — zero matches
3. `grep "aspectRatio: 1.6" VoltVenture/src/screens/app/IdScanScreen.tsx` — one match
4. `grep "Identity Verified" VoltVenture/src/screens/app/IdScanScreen.tsx` — one match
5. `grep "onVerified" VoltVenture/src/screens/app/IdScanScreen.tsx` — at least two matches (param extraction + call)
</verification>

<success_criteria>
- IdScanScreen.tsx exists and compiles with zero TypeScript errors
- Viewfinder is dark View with 1.6 aspect ratio and four white corner brackets
- Capture button triggers 1500ms mock delay with ActivityIndicator overlay
- Success state shows green check-circle icon and "Identity Verified" text
- Continue button calls onVerified callback then navigates back
- After returning, LoginSecurity shows "Verified" (green) badge on the ID Scan row
</success_criteria>

<output>
Create `.planning/phases/06-security-and-verification/06-02-SUMMARY.md` when done.
</output>
