---
phase: 06-security-and-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - VoltVenture/src/screens/app/FacialScanScreen.tsx
  - VoltVenture/src/screens/app/SecurityDepositScreen.tsx
autonomous: true
requirements:
  - SEC-03
  - SEC-04

must_haves:
  truths:
    - "User sees a dark square viewfinder with a dashed oval face guide in idle state"
    - "User sees instructional text 'Center your face within the frame and hold still.'"
    - "Tapping Capture triggers a 1500ms ActivityIndicator overlay then success state"
    - "FacialScan success state shows 'Face Verified' and Continue returns to LoginSecurity with Facial badge updated to Verified"
    - "User can view SecurityDeposit screen showing '$150.00' in a status card with 'Active Hold' badge"
    - "Tapping 'Request Refund' disables the button and shows the refund snackbar message"
  artifacts:
    - path: "VoltVenture/src/screens/app/FacialScanScreen.tsx"
      provides: "Facial liveness mock viewfinder screen with oval guide and 3-state machine"
      contains: "Face Verified"
    - path: "VoltVenture/src/screens/app/SecurityDepositScreen.tsx"
      provides: "Security deposit status card with refund CTA"
      contains: "150.00"
  key_links:
    - from: "FacialScanScreen.tsx"
      to: "LoginSecurityScreen.tsx"
      via: "route.params.onVerified() then navigation.goBack()"
      pattern: "onVerified.*goBack\|goBack.*onVerified"
    - from: "SecurityDepositScreen.tsx"
      to: "Snackbar"
      via: "refundRequested state toggles snackVisible"
      pattern: "refundRequested\|setSnackVisible"
---

<objective>
Deliver FacialScanScreen (oval viewfinder, same state machine as IdScan, FacialScan-specific copy) and SecurityDepositScreen (status card with $150.00, Active Hold badge, refund CTA with Snackbar feedback). Completes Phase 6 security & verification requirements.

Purpose: Satisfies SEC-03 (facial scan flow) and SEC-04 (security deposit view). Both screens depend on the navigation types and AccountNavigator updates from Plan 06-01 (Wave 1). No dependency on Plan 06-02 — these can execute in parallel with IdScan if desired, but are placed in Wave 2 alongside it.

Output:
- FacialScanScreen.tsx: square viewfinder with dashed oval, identical state machine to IdScan, FacialScan copy
- SecurityDepositScreen.tsx: status card, explanation section, Request Refund button with disabled state + Snackbar
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-security-and-verification/06-CONTEXT.md
@.planning/phases/06-security-and-verification/06-UI-SPEC.md
@.planning/phases/06-security-and-verification/06-RESEARCH.md

<interfaces>
From VoltVenture/src/types/navigation.ts (as updated by Plan 06-01):

  AccountStackParamList includes:
    FacialScan: { onVerified: () => void };
    SecurityDeposit: undefined;

Screen props types:
  type FacialProps = StackScreenProps<AccountStackParamList, 'FacialScan'>;
  type DepositProps = StackScreenProps<AccountStackParamList, 'SecurityDeposit'>;

From VoltVenture/src/components/common/PrimaryButton.tsx:
  export interface PrimaryButtonProps {
    label: string;
    onPress: () => void;
    loading?: boolean;
    disabled?: boolean;
  }

From Plan 06-02 (IdScanScreen — reference for identical state machine):
  type ScanState = 'idle' | 'verifying' | 'success';
  const VIEWFINDER_BG = DSColors.textPrimary;  // #0F0F0F
  const WHITE = '#FFFFFF';
  startCapture: setScanState('verifying') → setTimeout(1500, () => setScanState('success'))
  handleContinue: route.params.onVerified?.() → navigation.goBack()

FacialScan viewfinder difference from IdScan:
  viewfinder aspectRatio: 1 (square, not 1.6)
  oval inside viewfinder: width: 200, height: 240, borderRadius: 120,
    borderWidth: 2, borderColor: WHITE, borderStyle: 'dashed' (or 'solid' fallback)
  viewfinder uses alignItems: 'center', justifyContent: 'center' to center oval

FacialScan copy differences from IdScan:
  Header title: "Facial Verification"
  Instruction text: "Center your face within the frame and hold still."
  Success text: "Face Verified" (not "Identity Verified")
  Capture/Continue buttons: same labels

DSColors tokens for SecurityDeposit:
  DSColors.surface   (#FAFAFA) — card background, Active Hold badge background
  DSColors.border    (#EBEBEB) — card border, Active Hold badge border
  DSColors.textPrimary   (#0F0F0F) — card heading, amount, "What is this?" label
  DSColors.textSecondary (#808080) — card label overline, refund estimate, paragraph body
  DSColors.background    (#FFFFFF) — screen background
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FacialScanScreen with oval viewfinder and FacialScan-specific copy</name>
  <files>
    VoltVenture/src/screens/app/FacialScanScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/screens/app/IdScanScreen.tsx — read full file once Plan 06-02 creates it; replicate the identical structure, changing only: aspectRatio (1 not 1.6), adding oval child View inside viewfinder, instruction text, success text, and header title. If IdScanScreen.tsx does not yet exist (Plans running in parallel), use the interface contract in this plan's context block as the authoritative spec.
    - VoltVenture/src/screens/app/SettingsScreen.tsx — custom header pattern if IdScanScreen.tsx is unavailable
    - VoltVenture/src/theme/theme.ts — DSColors token values
  </read_first>
  <action>
    Create VoltVenture/src/screens/app/FacialScanScreen.tsx as a new file. The implementation is identical to IdScanScreen.tsx with the following specific differences:

    1. Props type: `type Props = StackScreenProps<AccountStackParamList, 'FacialScan'>;`

    2. File-scoped constants (after imports): `const VIEWFINDER_BG = DSColors.textPrimary;` and `const WHITE = '#FFFFFF';`

    3. State machine: identical — `type ScanState = 'idle' | 'verifying' | 'success';` and useState('idle'). startCapture and handleContinue functions are the same — handleContinue calls `route.params.onVerified?.()` then `navigation.goBack()`.

    4. Header title text: "Facial Verification" (not "ID Verification").

    5. Instruction text: "Center your face within the frame and hold still." (not the ID scan instruction). Instruction is shown when scanState !== 'success', hidden in success state.

    6. Viewfinder: View with aspectRatio: 1 (square, not 1.6), backgroundColor: VIEWFINDER_BG, width: '100%', alignItems: 'center', justifyContent: 'center'. No corner brackets. Instead, a single child View (the oval) with: width: 200, height: 240, borderRadius: 120, borderWidth: 2, borderColor: WHITE, borderStyle: 'solid'. Use 'solid' not 'dashed' to avoid Android rendering inconsistency (per RESEARCH.md Pitfall 2 — dashed is unreliable on Android; solid is the accepted fallback). This oval is only shown when scanState === 'idle'. When scanState === 'verifying', the verifying overlay (same absoluteFillObject overlay with ActivityIndicator and "Verifying..." text) appears over the entire viewfinder. The oval is hidden during verifying state; the overlay covers it.

    7. Success state: View styles.successCard (alignItems center, justifyContent center, paddingVertical 48, gap 16) containing MaterialCommunityIcons name="check-circle" size={72} color={DSColors.primary} and Text "Face Verified" (fontSize 20, fontWeight '700', color DSColors.textPrimary). Note: "Face Verified" not "Identity Verified".

    8. Button area (View paddingHorizontal: 24, paddingTop: 24): PrimaryButton label="Capture" in idle state; nothing in verifying state; PrimaryButton label="Continue" onPress={handleContinue} in success state.

    All imports: React, useState from react; View, Text, TouchableOpacity, StyleSheet, ActivityIndicator from react-native; SafeAreaView from react-native-safe-area-context; MaterialCommunityIcons from @expo/vector-icons; StackScreenProps from @react-navigation/stack; AccountStackParamList from ../../types/navigation; DSColors from ../../theme/theme; PrimaryButton from ../../components/common/PrimaryButton.

    Do NOT import expo-camera, Camera, or any camera library.
  </action>
  <verify>
    File exists: test -f VoltVenture/src/screens/app/FacialScanScreen.tsx && echo "EXISTS". Type check: npx tsc --noEmit --project VoltVenture/tsconfig.json 2>&1 | grep "FacialScanScreen" — expect zero errors. Check oval: grep "borderRadius: 120" VoltVenture/src/screens/app/FacialScanScreen.tsx — expect one match. Check no corner brackets: grep "borderTopWidth\|borderBottomWidth" VoltVenture/src/screens/app/FacialScanScreen.tsx — expect zero matches. Check copy: grep "Face Verified" VoltVenture/src/screens/app/FacialScanScreen.tsx — expect one match.
  </verify>
  <acceptance_criteria>
    - File exists at VoltVenture/src/screens/app/FacialScanScreen.tsx
    - `type ScanState = 'idle' | 'verifying' | 'success'` defined
    - `const VIEWFINDER_BG = DSColors.textPrimary` defined after imports
    - `const WHITE = '#FFFFFF'` defined after imports
    - Viewfinder View has aspectRatio: 1 (not 1.6)
    - Oval View has width: 200, height: 240, borderRadius: 120, borderWidth: 2, borderColor: WHITE, borderStyle: 'solid'
    - No corner bracket Views (no borderTopWidth/borderLeftWidth selective border pattern)
    - startCapture calls setScanState('verifying') then setTimeout(callback, 1500)
    - handleContinue calls route.params.onVerified?.() before navigation.goBack()
    - Instruction text is exactly "Center your face within the frame and hold still."
    - Success text is exactly "Face Verified"
    - Header title is exactly "Facial Verification"
    - Success card icon is MaterialCommunityIcons name="check-circle" size={72} color={DSColors.primary}
    - No expo-camera import
    - No NativeWind className props
    - SafeAreaView is root element with backgroundColor: DSColors.background
  </acceptance_criteria>
  <done>FacialScan screen renders dark square viewfinder with centered oval in idle state. Capture triggers 1500ms mock delay then success state showing "Face Verified". Continue calls onVerified and returns to LoginSecurity with the Facial Verification badge updated to Verified (green pill).</done>
</task>

<task type="auto">
  <name>Task 2: Create SecurityDepositScreen with status card and refund CTA</name>
  <files>
    VoltVenture/src/screens/app/SecurityDepositScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/screens/app/SettingsScreen.tsx — custom header pattern (header, headerTitle styles)
    - VoltVenture/src/screens/app/AccountScreen.tsx — Portal usage pattern; DSColors import
    - VoltVenture/src/theme/theme.ts — DSColors.surface, DSColors.border, DSColors.textPrimary, DSColors.textSecondary, DSColors.background values
    - VoltVenture/src/components/common/PrimaryButton.tsx — props interface (label, onPress, disabled)
  </read_first>
  <action>
    Create VoltVenture/src/screens/app/SecurityDepositScreen.tsx as a new file.

    Props type: `type Props = StackScreenProps<AccountStackParamList, 'SecurityDeposit'>;`

    State: `const [refundRequested, setRefundRequested] = useState(false);` and `const [snackVisible, setSnackVisible] = useState(false);`

    handleRefundRequest function: sets refundRequested(true) and setSnackVisible(true).

    Root layout: SafeAreaView (flex:1, backgroundColor: DSColors.background). Inside: custom header, then ScrollView (showsVerticalScrollIndicator: false, flex:1) containing the status card and explanation section, then the button area outside ScrollView (paddingHorizontal: 24, paddingTop: 24, paddingBottom: 24). Portal + Snackbar at the bottom of SafeAreaView.

    Custom header: identical pattern to SettingsScreen — View styles.header, back button with arrow-left icon, Text "Security Deposit" with styles.headerTitle, spacer View width={40}.

    Status card: View with styles.card — margin: 24, padding: 20, backgroundColor: DSColors.surface, borderWidth: 1, borderColor: DSColors.border, borderRadius: 12, alignItems: 'center', gap: 12. Contains in order:
    1. Text "SECURITY DEPOSIT" — fontSize 13, fontWeight '600', color DSColors.textSecondary, textTransform 'uppercase', letterSpacing 0.8, textAlign 'center'.
    2. Status badge inline View: backgroundColor: DSColors.surface, borderWidth: 1, borderColor: DSColors.border, borderRadius: 12, paddingHorizontal: 12, paddingVertical: 4 — containing Text "Active Hold" (fontSize 12, fontWeight '600', color DSColors.textSecondary).
    3. Text "$150.00" — fontSize 32, fontWeight '700', color DSColors.textPrimary.
    4. Text "Estimated refund: 7 days after your last active ride." — fontSize 13, fontWeight '400', color DSColors.textSecondary, textAlign 'center'.

    Explanation section: View with styles.explanationSection — paddingHorizontal: 24, paddingTop: 8, gap: 8. Contains:
    1. Text "What is this?" — fontSize 15, fontWeight '600', color DSColors.textPrimary.
    2. Text "Your deposit is held to cover any outstanding charges or damage. It's automatically refunded after your account is in good standing for 7 days following your last ride." — fontSize 15, fontWeight '400', color DSColors.textSecondary, lineHeight: 22.

    Button area (View paddingHorizontal: 24, paddingTop: 24, paddingBottom: 24, outside ScrollView): PrimaryButton label="Request Refund" onPress={handleRefundRequest} disabled={refundRequested}.

    Portal + Snackbar (inside SafeAreaView, after the button area): visible={snackVisible} onDismiss={() => setSnackVisible(false)} duration={3000} showing "Refund request submitted. Processing may take 5–7 business days."

    All imports: React, useState from react; View, Text, TouchableOpacity, ScrollView, StyleSheet from react-native; SafeAreaView from react-native-safe-area-context; Snackbar, Portal from react-native-paper; MaterialCommunityIcons from @expo/vector-icons; StackScreenProps from @react-navigation/stack; AccountStackParamList from ../../types/navigation; DSColors from ../../theme/theme; PrimaryButton from ../../components/common/PrimaryButton.
  </action>
  <verify>
    File exists: test -f VoltVenture/src/screens/app/SecurityDepositScreen.tsx && echo "EXISTS". Type check: npx tsc --noEmit --project VoltVenture/tsconfig.json 2>&1 | grep "SecurityDepositScreen" — expect zero errors. Check amount: grep "150.00" VoltVenture/src/screens/app/SecurityDepositScreen.tsx — expect one match. Check refund state: grep "refundRequested" VoltVenture/src/screens/app/SecurityDepositScreen.tsx — expect at least 3 matches (declaration, setter call, disabled prop). Check snackbar message: grep "5.7 business days\|5–7 business days" VoltVenture/src/screens/app/SecurityDepositScreen.tsx — expect one match.
  </verify>
  <acceptance_criteria>
    - File exists at VoltVenture/src/screens/app/SecurityDepositScreen.tsx
    - useState declarations: refundRequested (false), snackVisible (false)
    - handleRefundRequest sets both refundRequested(true) and snackVisible(true) in one call
    - Status card View has borderRadius: 12, borderWidth: 1, borderColor: DSColors.border, backgroundColor: DSColors.surface, alignItems: 'center', margin: 24, padding: 20
    - Amount text "$150.00" has fontSize: 32 and fontWeight: '700'
    - "Active Hold" badge has borderRadius: 12 and backgroundColor: DSColors.surface
    - Refund estimate text is exactly "Estimated refund: 7 days after your last active ride."
    - Explanation label is exactly "What is this?"
    - Explanation body starts with "Your deposit is held to cover any outstanding charges"
    - PrimaryButton has disabled={refundRequested} — button grays out after first tap
    - Portal + Snackbar renders with duration={3000}
    - Snackbar message is exactly "Refund request submitted. Processing may take 5–7 business days."
    - Header title is exactly "Security Deposit"
    - No AsyncStorage usage
    - No NativeWind className props
    - SafeAreaView is root element with backgroundColor: DSColors.background
  </acceptance_criteria>
  <done>SecurityDeposit screen shows status card with $150.00, Active Hold badge, and refund estimate. Tapping Request Refund disables the button and shows a snackbar that auto-dismisses after 3 seconds. Button cannot be tapped again (disabled state persists in-memory).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.params.onVerified (FacialScan) | In-memory callback from LoginSecurity — same trust model as IdScan |
| User tap → refundRequested state | Write to in-memory boolean; no external API call, no data exfiltration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Spoofing | FacialScan onVerified callback | accept | Same analysis as T-06-02-01 — in-memory function ref, no user-controlled data flows into the call. Optional chaining `?.()` prevents crash if undefined. |
| T-06-03-02 | Denial of Service | Request Refund double-tap | accept | `disabled={refundRequested}` prevents re-submission after first tap. PrimaryButton internally also disables on the `disabled` prop. In-memory state — no backend call to protect. |
| T-06-03-03 | Information Disclosure | "$150.00" hardcoded amount | accept | Static mock data — not real financial data. No PII. Acceptable for frontend-only MVP. |
| T-06-SC | Tampering | npm installs | accept | No new packages installed — all dependencies pre-existing. |
</threat_model>

<verification>
After this plan completes, run the full Phase 6 verification:
1. `npx tsc --noEmit --project VoltVenture/tsconfig.json` — zero errors across all Phase 6 files
2. `grep "expo-camera\|Camera" VoltVenture/src/screens/app/FacialScanScreen.tsx` — zero matches
3. `grep "borderRadius: 120" VoltVenture/src/screens/app/FacialScanScreen.tsx` — one match (oval)
4. `grep "Face Verified" VoltVenture/src/screens/app/FacialScanScreen.tsx` — one match
5. `grep "150.00" VoltVenture/src/screens/app/SecurityDepositScreen.tsx` — one match
6. `grep "refundRequested" VoltVenture/src/screens/app/SecurityDepositScreen.tsx` — three or more matches
7. `grep "5.7 business\|5–7 business" VoltVenture/src/screens/app/SecurityDepositScreen.tsx` — one match
8. All five new screen files exist: AccountScreen (updated), LoginSecurityScreen, IdScanScreen, FacialScanScreen, SecurityDepositScreen
</verification>

<success_criteria>
Phase 6 is complete when:
1. User can tap Security in AccountScreen and reach LoginSecurity hub
2. 2FA toggle shows correct snackbar messages (SEC-01)
3. Tapping ID Scan row navigates to IdScan — Capture → 1.5s delay → "Identity Verified" → Continue updates badge to Verified (SEC-02)
4. Tapping Facial Verification row navigates to FacialScan — same flow → "Face Verified" → badge updates (SEC-03)
5. Tapping Security Deposit row shows status card with $150.00, Active Hold, and refund estimate; Request Refund disables button and shows snackbar (SEC-04)
6. TypeScript compiles with zero errors across all Phase 6 files
7. No NativeWind className props on any Phase 6 screen
8. No expo-camera import in any file
</success_criteria>

<output>
Create `.planning/phases/06-security-and-verification/06-03-SUMMARY.md` when done.
</output>
