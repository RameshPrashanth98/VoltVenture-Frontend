---
phase: 07-navigation-ride-extras
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VoltVenture/src/types/navigation.ts
  - VoltVenture/src/navigation/NavNavigator.tsx
  - VoltVenture/src/navigation/RootNavigator.tsx
  - VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx
  - VoltVenture/src/screens/navigation/WalkingDirectionsScreen.tsx
  - VoltVenture/src/components/map/BikeDetailSheet.tsx
  - VoltVenture/src/screens/app/MapScreen.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "NavStackParamList and ChargeStackParamList are exported from navigation.ts"
    - "RideStackParamList includes SafetyMount as first entry"
    - "RootStackParamList includes NavStack and ChargeStack"
    - "NavNavigator registers NavigateToBike and WalkingDirections screens"
    - "NavStack is registered in RootNavigator with presentation: 'modal'"
    - "Tapping Get Directions in BikeDetailSheet navigates to NavStack/NavigateToBike"
    - "NavigateToBike shows full-screen MapView with polyline, ETA card, and View Turn-by-Turn button"
    - "WalkingDirections shows 5 mock steps in a FlatList with direction icons and distances"
    - "TypeScript compiles with zero errors after this plan"
  artifacts:
    - path: "VoltVenture/src/types/navigation.ts"
      provides: "NavStackParamList, ChargeStackParamList, updated RideStackParamList, updated RootStackParamList"
      contains: "NavStackParamList"
    - path: "VoltVenture/src/navigation/NavNavigator.tsx"
      provides: "NavStack navigator (NavigateToBike + WalkingDirections)"
      exports: ["default NavNavigator"]
    - path: "VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx"
      provides: "Full-screen map + ETA card + polyline for walking nav"
      exports: ["default NavigateToBikeScreen"]
    - path: "VoltVenture/src/screens/navigation/WalkingDirectionsScreen.tsx"
      provides: "Turn-by-turn walking step list"
      exports: ["default WalkingDirectionsScreen"]
  key_links:
    - from: "VoltVenture/src/components/map/BikeDetailSheet.tsx"
      to: "VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx"
      via: "onGetDirections callback → MapScreen.tsx → navigation.navigate('NavStack', { screen: 'NavigateToBike', params: { bike } })"
      pattern: "onGetDirections"
    - from: "VoltVenture/src/navigation/RootNavigator.tsx"
      to: "VoltVenture/src/navigation/NavNavigator.tsx"
      via: "Stack.Screen name='NavStack' component={NavNavigator}"
      pattern: "NavStack"
    - from: "VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx"
      to: "VoltVenture/src/screens/navigation/WalkingDirectionsScreen.tsx"
      via: "navigation.push('WalkingDirections', { bike })"
      pattern: "navigation.push"
---

<objective>
Add the NavStack modal (NavigateToBike + WalkingDirections), update navigation types for all phase-7 stacks, wire BikeDetailSheet's "Get Directions" button, and create both navigation screens.

Purpose: Delivers NAV-01 (walking map + ETA) and NAV-02 (turn-by-turn directions). Also plants ChargeStackParamList and SafetyMount type additions that 07-02 and 07-03 depend on — ensuring TypeScript is clean before those plans run.

Output: 2 new screen files, 1 new navigator file, 4 modified files. Phase-7 navigation contract fully typed.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-UI-SPEC.md

<interfaces>
<!-- Extracted from VoltVenture/src/types/navigation.ts — current state before this plan's edits -->

Current RootStackParamList (lines 49-54):
  AuthStack, AppTabs, BookingStack, RideStack — add NavStack + ChargeStack

Current RideStackParamList (lines 17-21):
  ActiveRide: { bike: Bike }
  PaymentSummary: { rideSummary: RideSummary }
  RideReceipt: { paymentResult: PaymentResult; rideSummary: RideSummary }
  — add SafetyMount: { bike: Bike } as FIRST entry

New types to add:
  NavStackParamList = { NavigateToBike: { bike: Bike }; WalkingDirections: { bike: Bike } }
  ChargeStackParamList = { EndRideFindCharging: undefined; RidingToCharging: { chargerName: string; location: { latitude: number; longitude: number } } }

Bike type already imported on line 4 — no new imports needed in navigation.ts.

<!-- Extracted from VoltVenture/src/components/map/BikeDetailSheet.tsx — current interface -->

Current BikeDetailSheetProps (lines 9-12):
  interface BikeDetailSheetProps { bike: Bike | null; onReserve: () => void; }

Add: onGetDirections: () => void

<!-- MapScreen.tsx caller — current BikeDetailSheet usage (lines 164-172) -->

Current: <BikeDetailSheet bike={selectedBike} onReserve={() => { ... }} />
Add onGetDirections callback that calls bikeDetailRef.current?.dismiss() then navigation.navigate('NavStack', { screen: 'NavigateToBike', params: { bike: selectedBike } })

<!-- haversineKm function body — must be INLINED, NOT imported (no export in MapScreen.tsx) -->

function haversineKm(lat1, lon1, lat2, lon2): number {
  R=6371, dLat/dLon in radians, haversine formula, returns R * 2 * atan2(sqrt(a), sqrt(1-a))
}
Full 10-line body is in 07-PATTERNS.md NavigateToBikeScreen section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation types — add NavStackParamList, ChargeStackParamList, SafetyMount to RideStack, NavStack+ChargeStack to RootStack</name>
  <files>VoltVenture/src/types/navigation.ts</files>

  <read_first>
    - VoltVenture/src/types/navigation.ts — read the full file first; understand exact current structure before editing
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — section "src/types/navigation.ts" for the exact before/after type definitions
  </read_first>

  <action>
    Read navigation.ts in full. Make four targeted edits:

    1. In RideStackParamList (currently lines 17-21): add SafetyMount: { bike: Bike } as the FIRST entry before ActiveRide. The updated type must have SafetyMount first, then ActiveRide, PaymentSummary, RideReceipt.

    2. After the existing RideStackParamList closing brace and before RootStackParamList, add two new exported types:
       - NavStackParamList with NavigateToBike: { bike: Bike } and WalkingDirections: { bike: Bike }
       - ChargeStackParamList with EndRideFindCharging: undefined and RidingToCharging: { chargerName: string; location: { latitude: number; longitude: number } }

    3. In RootStackParamList (currently lines 49-54): append two entries after RideStack:
       - NavStack: NavigatorScreenParams<NavStackParamList>
       - ChargeStack: NavigatorScreenParams<ChargeStackParamList>

    No new imports needed — Bike is already imported. NavigatorScreenParams is already imported. Do not add duplicate imports.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - navigation.ts contains `NavStackParamList` as an exported type
    - navigation.ts contains `ChargeStackParamList` as an exported type
    - navigation.ts contains `SafetyMount: { bike: Bike }` within RideStackParamList
    - navigation.ts contains `NavStack: NavigatorScreenParams<NavStackParamList>` within RootStackParamList
    - navigation.ts contains `ChargeStack: NavigatorScreenParams<ChargeStackParamList>` within RootStackParamList
    - SafetyMount entry appears BEFORE ActiveRide in RideStackParamList
    - `npx tsc --noEmit` produces zero errors from navigation.ts changes (other existing errors, if any, are unchanged)
  </acceptance_criteria>

  <done>navigation.ts exports NavStackParamList, ChargeStackParamList; RideStackParamList has SafetyMount first; RootStackParamList has NavStack and ChargeStack. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create NavNavigator + NavigateToBikeScreen + WalkingDirectionsScreen + update RootNavigator</name>
  <files>
    VoltVenture/src/navigation/NavNavigator.tsx,
    VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx,
    VoltVenture/src/screens/navigation/WalkingDirectionsScreen.tsx,
    VoltVenture/src/navigation/RootNavigator.tsx
  </files>

  <read_first>
    - VoltVenture/src/navigation/BookingNavigator.tsx — exact navigator pattern to copy for NavNavigator
    - VoltVenture/src/navigation/RootNavigator.tsx — current file before edit; identify where to add NavStack and ChargeStack registrations
    - VoltVenture/src/screens/ride/ActiveRideScreen.tsx — full-screen map + overlay card pattern for NavigateToBikeScreen
    - VoltVenture/src/screens/app/LoginSecurityScreen.tsx — custom header + SafeAreaView + FlatList pattern for WalkingDirectionsScreen
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — NavigateToBikeScreen section, WalkingDirectionsScreen section, NavNavigator section
    - .planning/phases/07-navigation-ride-extras/07-UI-SPEC.md — screens 1 and 2 for exact layout, colors, copy
  </read_first>

  <action>
    Create the `src/screens/navigation/` folder and its two screen files, then create NavNavigator.tsx, then update RootNavigator.

    --- NavNavigator.tsx ---
    New file at VoltVenture/src/navigation/NavNavigator.tsx. Mirror BookingNavigator.tsx exactly:
    - Import createStackNavigator from @react-navigation/stack
    - Import NavStackParamList from ../types/navigation
    - Import NavigateToBikeScreen from ../screens/navigation/NavigateToBikeScreen
    - Import WalkingDirectionsScreen from ../screens/navigation/WalkingDirectionsScreen
    - const Stack = createStackNavigator<NavStackParamList>()
    - screenOptions={{ headerShown: false }}
    - Two Stack.Screen: name="NavigateToBike" component={NavigateToBikeScreen}, name="WalkingDirections" component={WalkingDirectionsScreen}

    --- NavigateToBikeScreen.tsx ---
    New file at VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx. Uses full-screen map pattern from ActiveRideScreen.

    Imports: React, { useState, useEffect } from 'react'; View, Text, TouchableOpacity, StyleSheet, ActivityIndicator from 'react-native'; MapView, { Marker, Polyline } from 'react-native-maps'; useSafeAreaInsets from 'react-native-safe-area-context'; MaterialCommunityIcons from '@expo/vector-icons'; StackScreenProps from '@react-navigation/stack'; NavStackParamList from '../../types/navigation'; DSColors, DSTypography from '../../theme/theme'.

    Props type: StackScreenProps<NavStackParamList, 'NavigateToBike'>. Destructure { route, navigation }.

    Inline haversineKm function (10-line body — copy from 07-PATTERNS.md, NOT imported from MapScreen).

    Constants: USER_LAT = 52.3676, USER_LON = 4.9041. Compute at module level or inside component:
    - distanceKm = haversineKm(USER_LAT, USER_LON, bike.latitude, bike.longitude)
    - etaMin = Math.round(distanceKm / 5 * 60)
    - distanceM = Math.round(distanceKm * 1000)

    useState loading (boolean, starts true). useEffect: resolve mock location, set loading false after 500ms (simulates async resolve).

    Loading state: returns View with ActivityIndicator size="large" color={DSColors.primary} centered on white background.

    Ready state JSX:
    - Root: View style={StyleSheet.absoluteFill}
    - MapView style={StyleSheet.absoluteFill}, scrollEnabled={false}, zoomEnabled={true}, rotateEnabled={false}, pitchEnabled={false}, initialRegion centered between user and bike (average lat/lon), latitudeDelta/longitudeDelta: 0.05
    - Inside MapView children: Marker for user (View style={styles.userMarker}), Marker for bike destination (MaterialCommunityIcons name="bicycle" size={24} color={DSColors.primary}), Polyline with 4 coordinates — [USER_LAT/USER_LON, {52.3690, 4.9020}, {52.3710, 4.9005}, bike coords], strokeColor={DSColors.primary} strokeWidth={4}
    - ETA overlay card: View style={[styles.overlayCard, { top: insets.top + 8 }]} — contains Text bike.name in headingMd/white, Text "{etaMin} min walk — {distanceM} m" in label/white, TouchableOpacity "View Turn-by-Turn" button (borderWidth:1, borderColor:'rgba(255,255,255,0.3)', borderRadius:8, paddingVertical:8, alignItems:'center', marginTop:12) with Text label fontSize:15 color white, onPress={() => navigation.push('WalkingDirections', { bike })}

    StyleSheet.create: overlayCard (position:'absolute', left:16, right:16, backgroundColor:'rgba(15,15,15,0.85)', borderRadius:16, paddingHorizontal:24, paddingVertical:16, zIndex:10), userMarker (width:16, height:16, borderRadius:8, backgroundColor:'#FFFFFF', borderWidth:3, borderColor:DSColors.primary), loadingContainer (flex:1, justifyContent:'center', alignItems:'center', backgroundColor:DSColors.background).

    CRITICAL: Do NOT use NativeWind className on any element. StyleSheet.create only.

    --- WalkingDirectionsScreen.tsx ---
    New file at VoltVenture/src/screens/navigation/WalkingDirectionsScreen.tsx. Uses SafeAreaView + custom header + FlatList pattern from LoginSecurityScreen.

    Imports: React from 'react'; View, Text, TouchableOpacity, FlatList, StyleSheet from 'react-native'; SafeAreaView from 'react-native-safe-area-context'; MaterialCommunityIcons from '@expo/vector-icons'; StackScreenProps from '@react-navigation/stack'; NavStackParamList from '../../types/navigation'; DSColors, DSTypography from '../../theme/theme'.

    Props type: StackScreenProps<NavStackParamList, 'WalkingDirections'>.

    Above component, define:
    - type Step = { id: string; icon: string; text: string; distance: string; isArrival?: boolean }
    - MOCK_STEPS: Step[] = 5 items per UI-SPEC: (1) id:'1', icon:'arrow-up', text:'Head north on Damrak', distance:'150 m'; (2) id:'2', icon:'arrow-left', text:'Turn left onto Nieuwendijk', distance:'200 m'; (3) id:'3', icon:'arrow-up', text:'Continue straight for 200 m', distance:'200 m'; (4) id:'4', icon:'arrow-right', text:'Turn right onto Warmoesstraat', distance:'120 m'; (5) id:'5', icon:'flag-checkered', text:'Arrive at your bike — right side of the street', distance:'', isArrival:true

    JSX: SafeAreaView (flex:1, backgroundColor:DSColors.background) > custom header View (flexDirection:'row', alignItems:'center', justifyContent:'space-between', paddingHorizontal:16, paddingVertical:12, borderBottomWidth:1, borderBottomColor:DSColors.border, height:56) with TouchableOpacity back (accessibilityRole='button', accessibilityLabel='Go back') + MaterialCommunityIcons 'arrow-left' 24dp DSColors.textPrimary + Text 'Walking Directions' headingMd/textPrimary + View width:40 spacer.

    FlatList: data={MOCK_STEPS}, keyExtractor={item => item.id}, contentContainerStyle={{ paddingHorizontal:16, paddingTop:8 }}.

    renderItem: View (flexDirection:'row', alignItems:'center', gap:12, paddingVertical:16, borderBottomWidth:1, borderBottomColor:DSColors.border) > View width:32 containing MaterialCommunityIcons name={item.icon} size={24} color={item.isArrival ? DSColors.primary : DSColors.textPrimary} + Text style body/textPrimary flex:1 > Text style label/textSecondary minWidth:48 textAlign:'right' (omit if distance is empty string).

    --- RootNavigator.tsx update ---
    Read the current file. Add two imports at the top: NavNavigator from './NavNavigator', ChargeNavigator from './ChargeNavigator'. Add two Stack.Screen registrations after the existing RideStack entry, before the closing Stack.Navigator tag:
    - name="NavStack" component={NavNavigator} options={{ presentation:'modal', headerShown:false }}
    - name="ChargeStack" component={ChargeNavigator} options={{ presentation:'modal', headerShown:false }}

    Note: ChargeNavigator does not exist yet (created in 07-03). The import and Stack.Screen registration must still be added here. The file will have a TypeScript "cannot find module" error until 07-03 creates ChargeNavigator — this is acceptable; 07-03 resolves it.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | grep -v "ChargeNavigator\|cannot find module" | head -30</automated>
  </verify>

  <acceptance_criteria>
    - VoltVenture/src/navigation/NavNavigator.tsx exists and default-exports NavNavigator
    - VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx exists and default-exports NavigateToBikeScreen
    - VoltVenture/src/screens/navigation/WalkingDirectionsScreen.tsx exists and default-exports WalkingDirectionsScreen
    - NavigateToBikeScreen.tsx contains `haversineKm` function defined locally (not imported)
    - NavigateToBikeScreen.tsx contains `<Polyline` as a child of `<MapView` (not outside it)
    - NavigateToBikeScreen.tsx contains `navigation.push('WalkingDirections'`
    - NavigateToBikeScreen.tsx does NOT contain `className=` (no NativeWind)
    - WalkingDirectionsScreen.tsx contains `MOCK_STEPS` array with 5 items
    - WalkingDirectionsScreen.tsx contains `flag-checkered` (arrival step icon)
    - RootNavigator.tsx contains `name="NavStack"` Stack.Screen
    - RootNavigator.tsx contains `name="ChargeStack"` Stack.Screen
    - NavNavigator.tsx contains both `NavigateToBike` and `WalkingDirections` as Stack.Screen names
    - TypeScript errors, if any, are limited to the missing ChargeNavigator module (07-03 resolves this)
  </acceptance_criteria>

  <done>NavNavigator created; NavigateToBikeScreen (map + ETA + polyline + View Turn-by-Turn) and WalkingDirectionsScreen (5-step FlatList + custom header) created; RootNavigator registers NavStack and ChargeStack modal stacks.</done>
</task>

<task type="auto">
  <name>Task 3: Update BikeDetailSheet (add onGetDirections prop + secondary button) and MapScreen (pass onGetDirections callback)</name>
  <files>
    VoltVenture/src/components/map/BikeDetailSheet.tsx,
    VoltVenture/src/screens/app/MapScreen.tsx
  </files>

  <read_first>
    - VoltVenture/src/components/map/BikeDetailSheet.tsx — full file; note current interface (lines 9-12), JSX structure, existing styles
    - VoltVenture/src/screens/app/MapScreen.tsx — full file; locate BikeDetailSheet usage (lines 164-172), existing navigation.navigate calls, bikeDetailRef usage
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — BikeDetailSheet section and MapScreen caller update section
    - .planning/phases/07-navigation-ride-extras/07-UI-SPEC.md — Integration Point Changes: BikeDetailSheet
  </read_first>

  <action>
    --- BikeDetailSheet.tsx ---
    Read the current file. Make four changes:

    1. Add TouchableOpacity to the React Native import on line 2 (alongside existing View, Text, StyleSheet imports).

    2. Add onGetDirections: () => void to BikeDetailSheetProps interface (below onReserve).

    3. Add onGetDirections to the function destructure parameter: { bike, onReserve, onGetDirections }.

    4. Add secondary button JSX immediately after the existing PrimaryButton (label="Reserve" onPress={onReserve}):
       - TouchableOpacity with style={styles.getDirectionsButton} onPress={onGetDirections} activeOpacity={0.7}
       - Inner Text with style={styles.getDirectionsLabel}: "Get Directions"

    5. Add two new styles to StyleSheet.create:
       - getDirectionsButton: { borderWidth:1, borderColor:DSColors.border, borderRadius:8, paddingVertical:12, alignItems:'center', marginTop:8 }
       - getDirectionsLabel: { fontSize:15, fontWeight:'600', color:DSColors.textPrimary }

    --- MapScreen.tsx ---
    Read the current file. Locate the BikeDetailSheet JSX (around lines 164-172). Add the onGetDirections prop with its callback:

    onGetDirections={() => {
      if (!selectedBike) return;
      bikeDetailRef.current?.dismiss();
      navigation.navigate('NavStack', { screen: 'NavigateToBike', params: { bike: selectedBike } });
    }}

    The navigation object in MapScreen already has access to RootStackParamList (confirm by reading current navigation hook usage). If MapScreen uses useNavigation, ensure the type parameter is StackNavigationProp<RootStackParamList> or NavigationProp<RootStackParamList> — check existing usage and match it. Do not change the navigation hook type if it already covers RootStackParamList; just add 'NavStack' which is now in the type from Task 1.

    Do not modify any other MapScreen logic. Do not add NativeWind.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | grep -v "ChargeNavigator\|cannot find module" | head -30</automated>
  </verify>

  <acceptance_criteria>
    - BikeDetailSheet.tsx contains `onGetDirections: () => void` in the interface
    - BikeDetailSheet.tsx contains `onGetDirections` in the function parameter destructure
    - BikeDetailSheet.tsx contains a `TouchableOpacity` with text "Get Directions" below the PrimaryButton
    - BikeDetailSheet.tsx contains `getDirectionsButton` in StyleSheet.create
    - BikeDetailSheet.tsx TouchableOpacity calls `onGetDirections` on press (not direct navigation)
    - MapScreen.tsx contains `onGetDirections` prop passed to BikeDetailSheet
    - MapScreen.tsx onGetDirections callback contains `bikeDetailRef.current?.dismiss()`
    - MapScreen.tsx onGetDirections callback contains `navigation.navigate('NavStack'`
    - TypeScript produces zero errors related to BikeDetailSheet or MapScreen (excluding ChargeNavigator missing module)
  </acceptance_criteria>

  <done>BikeDetailSheet has onGetDirections callback prop and "Get Directions" secondary outlined button. MapScreen passes the callback that dismisses the bottom sheet and navigates to NavStack/NavigateToBike.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.params → screen | NavStackParamList params (bike object) passed from BikeDetailSheet via MapScreen |
| navigation.getParent() → RootNavigator | Cross-stack navigation calls originate from child stack screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | NavigateToBikeScreen route.params.bike | accept | Frontend-only phase; bike object comes from existing MapScreen mock data, same trust level as MapScreen. No user-supplied input. |
| T-07-01-02 | Denial of Service | haversineKm inlined in screen | accept | Pure math function on mock coordinates; no external calls, no infinite loops. Division by walking speed (5) is a constant. |
| T-07-01-03 | Information Disclosure | navigation.navigate('NavStack', { bike }) | accept | Bike data is mock/public info (location, name). No PII transmitted via navigation params. |
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in this plan. All dependencies verified from existing package.json. |
</threat_model>

<verification>
After all three tasks complete, run the full TypeScript check:

```
cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit
```

Expected: Zero errors (or only the ChargeNavigator missing module error that 07-03 will resolve).

Manual smoke-check (on emulator after 07-02 and 07-03 complete): Tap "Get Directions" on a bike detail sheet — NavStack modal slides up, NavigateToBike map appears with green polyline and ETA card. Tap "View Turn-by-Turn" — WalkingDirections screen pushes with 5 Amsterdam steps. Tap back — returns to NavigateToBike.
</verification>

<success_criteria>
1. navigation.ts exports NavStackParamList, ChargeStackParamList; RideStackParamList has SafetyMount first; RootStackParamList has NavStack and ChargeStack
2. NavNavigator.tsx created and wired into RootNavigator as a modal stack
3. NavigateToBikeScreen shows full-screen MapView with Electric Green polyline, user dot marker, bike destination marker, ETA card (bike name + "X min walk — Y m"), and "View Turn-by-Turn" button
4. WalkingDirectionsScreen shows custom header "Walking Directions" + 5 FlatList steps with direction icons (including flag-checkered arrival in Electric Green) and distances
5. BikeDetailSheet has "Get Directions" outlined secondary button below "Book Bike"
6. TypeScript compiles clean (zero errors except pending ChargeNavigator)
</success_criteria>

<output>
Create `.planning/phases/07-navigation-ride-extras/07-01-SUMMARY.md` when done.
</output>
