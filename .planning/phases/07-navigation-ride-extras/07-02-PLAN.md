---
phase: 07-navigation-ride-extras
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - VoltVenture/src/screens/ride/SafetyMountScreen.tsx
  - VoltVenture/src/navigation/RideNavigator.tsx
  - VoltVenture/src/screens/booking/UnlockSuccessScreen.tsx
autonomous: true
requirements:
  - RIDE-05

must_haves:
  truths:
    - "SafetyMountScreen shows 4 interactive checklist items with icons"
    - "Start Ride button is disabled until all 4 items are checked"
    - "Tapping all items enables the Start Ride button and navigates to ActiveRide"
    - "SafetyMount is the first screen in RideStack (RideNavigator)"
    - "UnlockSuccessScreen Start Ride navigates to SafetyMount instead of ActiveRide"
    - "TypeScript compiles with zero errors after this plan"
  artifacts:
    - path: "VoltVenture/src/screens/ride/SafetyMountScreen.tsx"
      provides: "4-item safety checklist screen with disabled PrimaryButton"
      exports: ["default SafetyMountScreen"]
    - path: "VoltVenture/src/navigation/RideNavigator.tsx"
      provides: "SafetyMount as first RideStack screen"
      contains: "SafetyMount"
  key_links:
    - from: "VoltVenture/src/screens/booking/UnlockSuccessScreen.tsx"
      to: "VoltVenture/src/screens/ride/SafetyMountScreen.tsx"
      via: "navigation.getParent<any>()?.navigate('RideStack', { screen: 'SafetyMount', params: { bike } })"
      pattern: "SafetyMount"
    - from: "VoltVenture/src/navigation/RideNavigator.tsx"
      to: "VoltVenture/src/screens/ride/SafetyMountScreen.tsx"
      via: "Stack.Screen name='SafetyMount' component={SafetyMountScreen} — must be first Screen"
      pattern: "SafetyMount"
    - from: "VoltVenture/src/screens/ride/SafetyMountScreen.tsx"
      to: "VoltVenture/src/screens/ride/ActiveRideScreen.tsx"
      via: "navigation.navigate('ActiveRide', { bike }) when allChecked"
      pattern: "navigation.navigate"
---

<objective>
Create SafetyMountScreen (4-item interactive checklist with disabled PrimaryButton), insert it as the first screen in RideNavigator, and update UnlockSuccessScreen to route to SafetyMount instead of ActiveRide.

Purpose: Delivers RIDE-05 — the safety checklist gate before a ride starts. The PrimaryButton remains disabled until all 4 items are tapped, preventing accidental ride start.

Output: 1 new screen file, 2 modified files. RideStack flow is now: SafetyMount → ActiveRide → PaymentSummary → RideReceipt.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-UI-SPEC.md

<interfaces>
<!-- RideStackParamList — current state AFTER 07-01 edits -->

RideStackParamList = {
  SafetyMount: { bike: Bike };    // first entry — added by 07-01
  ActiveRide: { bike: Bike };
  PaymentSummary: { rideSummary: RideSummary };
  RideReceipt: { paymentResult: PaymentResult; rideSummary: RideSummary };
}

Screen props type: StackScreenProps<RideStackParamList, 'SafetyMount'>

<!-- PrimaryButton disabled prop — from VoltVenture/src/components/common/PrimaryButton.tsx -->

PrimaryButton accepts: label: string, onPress: () => void, disabled?: boolean
When disabled=true, Paper Button applies opacity internally (no custom disabled style needed).

<!-- UnlockSuccessScreen exact change — from 07-RESEARCH.md Critical Finding #6 -->

Line 45 change only:
  BEFORE: onPress={() => navigation.getParent<any>()?.navigate('RideStack', { screen: 'ActiveRide', params: { bike } })}
  AFTER:  onPress={() => navigation.getParent<any>()?.navigate('RideStack', { screen: 'SafetyMount', params: { bike } })}

No other changes to UnlockSuccessScreen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SafetyMountScreen with 4-item checklist and disabled PrimaryButton</name>
  <files>VoltVenture/src/screens/ride/SafetyMountScreen.tsx</files>

  <read_first>
    - VoltVenture/src/screens/app/LoginSecurityScreen.tsx — custom header + SafeAreaView + ScrollView + TouchableOpacity row pattern (analog)
    - VoltVenture/src/components/common/PrimaryButton.tsx — confirm disabled prop signature
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — SafetyMountScreen section for full import list, checklist state pattern, row JSX, StyleSheet values
    - .planning/phases/07-navigation-ride-extras/07-UI-SPEC.md — screen 3 (SafetyMount) for exact copy, colors, spacing, checklist items
  </read_first>

  <action>
    Create new file at VoltVenture/src/screens/ride/SafetyMountScreen.tsx. This is a light-surface screen — use StyleSheet.create + DSColors (NOT NativeWind).

    Imports: React, { useState } from 'react'; View, Text, TouchableOpacity, ScrollView, StyleSheet from 'react-native'; SafeAreaView from 'react-native-safe-area-context'; MaterialCommunityIcons from '@expo/vector-icons'; StackScreenProps from '@react-navigation/stack'; RideStackParamList from '../../types/navigation'; DSColors, DSTypography from '../../theme/theme'; PrimaryButton from '../../components/common/PrimaryButton'.

    Props type: StackScreenProps<RideStackParamList, 'SafetyMount'>. Destructure { route, navigation }.

    Above the component, define:
    - type ChecklistItem = { id: number; label: string; icon: string }
    - CHECKLIST_ITEMS: ChecklistItem[] (4 items): id:1 label:'Helmet secured' icon:'helmet'; id:2 label:'Brakes tested' icon:'car-brake-hold'; id:3 label:'Lights working' icon:'lightbulb-on'; id:4 label:'App tracking active' icon:'map-marker-check'

    State: const [checked, setChecked] = useState<Set<number>>(new Set())
    Toggle: toggleItem function that uses Set immutable update (create new Set from prev, add or delete id, return next set)
    allChecked: checked.size === CHECKLIST_ITEMS.length

    JSX structure:
    - SafeAreaView (flex:1, backgroundColor:DSColors.background)
      - Custom header View (flexDirection:'row', alignItems:'center', justifyContent:'space-between', paddingHorizontal:16, paddingVertical:12, borderBottomWidth:1, borderBottomColor:DSColors.border, height:56)
        - TouchableOpacity onPress={() => navigation.goBack()} accessibilityRole='button' accessibilityLabel='Go back'
          - MaterialCommunityIcons name='arrow-left' size={24} color={DSColors.textPrimary}
        - Text style headingMd/textPrimary: 'Safety Check'
        - View width:40 spacer
      - ScrollView contentContainerStyle={{ paddingHorizontal:16, paddingTop:16, paddingBottom:32 }}
        - Text style body/textSecondary marginBottom:24: 'Complete all checks before starting your ride'
        - Map over CHECKLIST_ITEMS: TouchableOpacity (style=checkRow, onPress={() => toggleItem(item.id), activeOpacity:0.7, accessibilityRole='checkbox', accessibilityState={{ checked: checked.has(item.id) }}, accessibilityLabel={item.label})
          - MaterialCommunityIcons name={item.icon} size={24} color={checked.has(item.id) ? DSColors.accent : DSColors.textSecondary}
          - Text style={styles.checkLabel}: {item.label}
          - MaterialCommunityIcons name={checked.has(item.id) ? 'check-circle' : 'circle-outline'} size={24} color={checked.has(item.id) ? DSColors.accent : DSColors.border}
        - PrimaryButton label='Start Ride' disabled={!allChecked} onPress={() => navigation.navigate('ActiveRide', { bike })} style={{ marginTop:32 }}

    StyleSheet.create values:
    - safeArea: { flex:1, backgroundColor:DSColors.background }
    - header: { flexDirection:'row', alignItems:'center', justifyContent:'space-between', paddingHorizontal:16, paddingVertical:12, borderBottomWidth:1, borderBottomColor:DSColors.border, height:56 }
    - headerTitle: { ...DSTypography.headingMd, color:DSColors.textPrimary }
    - subtitle: { ...DSTypography.body, color:DSColors.textSecondary, marginBottom:24 }
    - checkRow: { flexDirection:'row', alignItems:'center', paddingVertical:16, gap:12, borderBottomWidth:1, borderBottomColor:DSColors.border }
    - checkLabel: { ...DSTypography.body, color:DSColors.textPrimary, flex:1 }

    Note on PrimaryButton disabled style: PrimaryButton already handles opacity via React Native Paper's disabled state — do NOT add custom opacity wrapping. Pass disabled={!allChecked} prop directly.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | grep "SafetyMount" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - VoltVenture/src/screens/ride/SafetyMountScreen.tsx exists and default-exports SafetyMountScreen
    - File contains CHECKLIST_ITEMS array with exactly 4 items (ids 1-4)
    - File contains icons: 'helmet', 'car-brake-hold', 'lightbulb-on', 'map-marker-check'
    - File contains `useState<Set<number>>(new Set())`
    - File contains `allChecked` or `checked.size === CHECKLIST_ITEMS.length` guard for the button
    - File contains `disabled={!allChecked}` on PrimaryButton (or equivalent `disabled={checked.size < 4}`)
    - File contains `navigation.navigate('ActiveRide', { bike })` on PrimaryButton press
    - File contains `accessibilityRole='checkbox'` on checklist row TouchableOpacity
    - File contains `check-circle` and `circle-outline` MaterialCommunityIcons for checked/unchecked state
    - File does NOT contain `className=` (no NativeWind)
    - `npx tsc --noEmit` produces zero SafetyMountScreen-related errors
  </acceptance_criteria>

  <done>SafetyMountScreen created with 4-item interactive checklist; Start Ride PrimaryButton is disabled until all 4 items checked; tapping all enables the button and navigates to ActiveRide.</done>
</task>

<task type="auto">
  <name>Task 2: Update RideNavigator (prepend SafetyMount) and UnlockSuccessScreen (change target from ActiveRide to SafetyMount)</name>
  <files>
    VoltVenture/src/navigation/RideNavigator.tsx,
    VoltVenture/src/screens/booking/UnlockSuccessScreen.tsx
  </files>

  <read_first>
    - VoltVenture/src/navigation/RideNavigator.tsx — full file; identify current import list and Stack.Screen order
    - VoltVenture/src/screens/booking/UnlockSuccessScreen.tsx — full file; find line 45 (the "Start Ride" onPress)
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — RideNavigator.tsx section and UnlockSuccessScreen section for exact changes
    - .planning/phases/07-navigation-ride-extras/07-RESEARCH.md — Critical Finding #5 (RideNavigator change) and Critical Finding #6 (UnlockSuccessScreen line 45)
  </read_first>

  <action>
    --- RideNavigator.tsx ---
    Read the current file. Make two changes:

    1. Add import for SafetyMountScreen: import SafetyMountScreen from '../screens/ride/SafetyMountScreen'; — place it before or after the existing ActiveRideScreen import, keeping the existing import order style.

    2. Add Stack.Screen as the FIRST child of Stack.Navigator (before the existing ActiveRide screen):
       `<Stack.Screen name="SafetyMount" component={SafetyMountScreen} />`

    The resulting Stack.Navigator children order must be: SafetyMount, ActiveRide, PaymentSummary, RideReceipt. SafetyMount MUST be first — it is the initial screen when RideStack is opened.

    Do not change screenOptions or any other existing configuration.

    --- UnlockSuccessScreen.tsx ---
    Read the current file. Find the onPress handler on the "Start Ride" PrimaryButton (currently line 45, per RESEARCH.md Critical Finding #6). Change only the screen name from 'ActiveRide' to 'SafetyMount' in the navigate call:

    BEFORE: navigate('RideStack', { screen: 'ActiveRide', params: { bike } })
    AFTER:  navigate('RideStack', { screen: 'SafetyMount', params: { bike } })

    Do not change any other code in UnlockSuccessScreen — no visual changes, no new imports, no style changes.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | grep -v "ChargeNavigator\|cannot find module" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - RideNavigator.tsx contains `import SafetyMountScreen from '../screens/ride/SafetyMountScreen'`
    - RideNavigator.tsx contains `<Stack.Screen name="SafetyMount" component={SafetyMountScreen}`
    - SafetyMount Stack.Screen appears BEFORE the ActiveRide Stack.Screen in the JSX (first child of Stack.Navigator)
    - UnlockSuccessScreen.tsx contains `screen: 'SafetyMount'` in the navigate call (not 'ActiveRide')
    - UnlockSuccessScreen.tsx does NOT contain `screen: 'ActiveRide'` in any navigate call (the old target is fully replaced)
    - `npx tsc --noEmit` produces zero errors from RideNavigator or UnlockSuccessScreen (excluding pending ChargeNavigator)
  </acceptance_criteria>

  <done>RideNavigator registers SafetyMount as first screen before ActiveRide. UnlockSuccessScreen Start Ride now routes to SafetyMount. RideStack flow is SafetyMount → ActiveRide → PaymentSummary → RideReceipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.params → SafetyMountScreen | bike object passed from UnlockSuccessScreen via RideStack |
| checklist state → PrimaryButton disabled | Client-side UI state; gate is cosmetic only (no server enforcement needed for frontend-only phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | SafetyMountScreen checklist state | accept | State is UI-local (useState); no persistence, no server call. Frontend-only phase — the checklist is a UX gate, not a security gate. |
| T-07-02-02 | Spoofing | navigation.navigate('ActiveRide') from SafetyMount | accept | Navigation is within the same RideStack. No external input influences the target screen. |
| T-07-02-03 | Denial of Service | Back navigation from SafetyMount dismisses RideStack | accept | Correct and intended behavior (D-05 / RESEARCH.md Pitfall 5). User presses back → RideStack dismisses → returns to BookingStack/UnlockSuccess. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in this plan. |
</threat_model>

<verification>
After both tasks complete:

```
cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit
```

Expected: Zero errors (or only the ChargeNavigator missing module from 07-01 — resolved by 07-03).

Manual flow to verify on emulator after full phase execution:
- Tap "Unlock" → complete booking flow → UnlockSuccessScreen → "Start Ride"
- SafetyMountScreen slides up as first RideStack screen
- All 4 checklist items unchecked → "Start Ride" button is disabled (opacity 0.4)
- Tap all 4 items → "Start Ride" button becomes active (Electric Green background)
- Tap "Start Ride" → ActiveRideScreen opens
</verification>

<success_criteria>
1. SafetyMountScreen shows "Safety Check" header, subtitle, 4 checklist rows with correct icons (helmet, car-brake-hold, lightbulb-on, map-marker-check)
2. Tapping a checklist row toggles the icon to check-circle in DSColors.accent (#7D9220) and the left icon color changes to accent
3. "Start Ride" PrimaryButton is disabled until all 4 items checked; enabled state shows Electric Green background
4. Tapping "Start Ride" when all checked navigates to ActiveRide within RideStack
5. RideNavigator lists SafetyMount as the first Stack.Screen (initial route)
6. UnlockSuccessScreen "Start Ride" navigates to RideStack/SafetyMount (not ActiveRide)
7. TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/07-navigation-ride-extras/07-02-SUMMARY.md` when done.
</output>
