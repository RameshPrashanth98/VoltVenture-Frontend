---
phase: 07-navigation-ride-extras
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx
  - VoltVenture/src/screens/charging/RidingToChargingScreen.tsx
  - VoltVenture/src/navigation/ChargeNavigator.tsx
  - VoltVenture/src/screens/ride/RideReceiptScreen.tsx
autonomous: true
requirements:
  - RIDE-06
  - RIDE-07

must_haves:
  truths:
    - "ChargeNavigator registers EndRideFindCharging and RidingToCharging screens"
    - "RootNavigator ChargeStack entry (added in 07-01) resolves — ChargeNavigator module now exists"
    - "EndRideFindCharging shows MapView with 5 mock charger pins and a close button"
    - "Tapping a charger pin shows an info card with station name, distance, and Navigate Here CTA"
    - "Tapping Navigate Here pushes RidingToCharging with charger name and coordinates"
    - "RidingToCharging shows full-screen MapView with ETA card and Electric Green polyline to charger"
    - "RideReceiptScreen has a Find a Charging Station text-only button below Done"
    - "Tapping Find a Charging Station dismisses RideStack then opens ChargeStack modal"
    - "TypeScript compiles with zero errors after this plan"
  artifacts:
    - path: "VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx"
      provides: "MapView with 5 charger pins, info card on pin tap, Navigate Here CTA"
      exports: ["default EndRideFindChargingScreen"]
    - path: "VoltVenture/src/screens/charging/RidingToChargingScreen.tsx"
      provides: "Full-screen map with ETA card and polyline to charger"
      exports: ["default RidingToChargingScreen"]
    - path: "VoltVenture/src/navigation/ChargeNavigator.tsx"
      provides: "ChargeStack navigator (EndRideFindCharging + RidingToCharging)"
      exports: ["default ChargeNavigator"]
  key_links:
    - from: "VoltVenture/src/screens/ride/RideReceiptScreen.tsx"
      to: "VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx"
      via: "const parent = navigation.getParent(); parent?.goBack(); setTimeout(() => parent?.navigate('ChargeStack', { screen: 'EndRideFindCharging' }), 300)"
      pattern: "ChargeStack"
    - from: "VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx"
      to: "VoltVenture/src/screens/charging/RidingToChargingScreen.tsx"
      via: "navigation.push('RidingToCharging', { chargerName, location })"
      pattern: "RidingToCharging"
    - from: "VoltVenture/src/navigation/RootNavigator.tsx"
      to: "VoltVenture/src/navigation/ChargeNavigator.tsx"
      via: "Stack.Screen name='ChargeStack' component={ChargeNavigator} — registered in 07-01"
      pattern: "ChargeNavigator"
---

<objective>
Create ChargeNavigator, EndRideFindChargingScreen, RidingToChargingScreen, and add the "Find a Charging Station" secondary CTA to RideReceiptScreen. This resolves the ChargeNavigator missing module error from 07-01 and delivers RIDE-06 and RIDE-07.

Purpose: Completes the post-ride charging flow. After ending a ride, the user can find 5 mock charging stations on a map and navigate to one with a live walking route.

Output: 2 new screen files, 1 new navigator file, 1 modified file. ChargeStack becomes fully functional.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/07-navigation-ride-extras/07-UI-SPEC.md

<interfaces>
<!-- ChargeStackParamList — defined in navigation.ts by 07-01 -->

ChargeStackParamList = {
  EndRideFindCharging: undefined;
  RidingToCharging: { chargerName: string; location: { latitude: number; longitude: number } };
}

Screen props types:
  StackScreenProps<ChargeStackParamList, 'EndRideFindCharging'>
  StackScreenProps<ChargeStackParamList, 'RidingToCharging'>

<!-- RideReceiptScreen current Done button — from 07-PATTERNS.md RideReceiptScreen section -->

Current Done button (lines 79-82):
  <PrimaryButton label="Done" onPress={() => navigation.getParent()?.goBack()} />

Add below it: TouchableOpacity for "Find a Charging Station" with the parent-capture pattern.

CRITICAL — parent capture pattern (from 07-RESEARCH.md Critical Finding #7):
  const parent = navigation.getParent();
  parent?.goBack();                          // dismiss RideStack
  setTimeout(() => {
    parent?.navigate('ChargeStack', { screen: 'EndRideFindCharging' });
  }, 300);

The parent must be captured BEFORE goBack() is called. After RideStack dismisses, navigation prop is invalid. The captured parent (RootNavigator) persists.

<!-- haversineKm — must be INLINED in both new map screens (not exported from MapScreen.tsx) -->

10-line function body available in 07-PATTERNS.md NavigateToBikeScreen section.

<!-- Mock charger data — from 07-UI-SPEC.md and 07-PATTERNS.md -->

MOCK_CHARGERS: ChargerStation[] = [
  { name: 'VoltHub Central', latitude: 52.3731, longitude: 4.8936 },
  { name: 'Dam Square Charger', latitude: 52.3728, longitude: 4.8936 },
  { name: 'Waterlooplein Station', latitude: 52.3678, longitude: 4.9006 },
  { name: 'Leidseplein EV Point', latitude: 52.3638, longitude: 4.8831 },
  { name: 'Vondelpark Charge Bay', latitude: 52.3580, longitude: 4.8688 },
]
USER_LAT = 52.3676, USER_LON = 4.9041

<!-- Charger pin marker spec — from 07-UI-SPEC.md screen 4 -->

Charger pin: View (40x40dp circle, backgroundColor:DSColors.background, borderWidth:2, borderColor:DSColors.primary, borderRadius:20, justifyContent:'center', alignItems:'center')
  MaterialCommunityIcons name='ev-station' size={28} color={DSColors.primary}
tracksViewChanges={false} on every Marker with custom View child (prevents Android perf issue)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChargeNavigator + EndRideFindChargingScreen + RidingToChargingScreen</name>
  <files>
    VoltVenture/src/navigation/ChargeNavigator.tsx,
    VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx,
    VoltVenture/src/screens/charging/RidingToChargingScreen.tsx
  </files>

  <read_first>
    - VoltVenture/src/navigation/BookingNavigator.tsx — exact navigator pattern to copy for ChargeNavigator
    - VoltVenture/src/screens/ride/ActiveRideScreen.tsx — full-screen map + overlay card pattern for both new screens
    - VoltVenture/src/screens/app/MapScreen.tsx — Marker onPress + selectedBike state pattern (analog for selectedCharger in EndRideFindCharging)
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — EndRideFindChargingScreen section, RidingToChargingScreen section, ChargeNavigator section
    - .planning/phases/07-navigation-ride-extras/07-UI-SPEC.md — screens 4 and 5 for exact layout, charger pin design, info card anatomy, close button
  </read_first>

  <action>
    Create the `src/screens/charging/` folder and its two screen files, then create ChargeNavigator.

    --- ChargeNavigator.tsx ---
    New file at VoltVenture/src/navigation/ChargeNavigator.tsx. Mirror BookingNavigator.tsx exactly:
    - Import createStackNavigator from @react-navigation/stack
    - Import ChargeStackParamList from ../types/navigation
    - Import EndRideFindChargingScreen from ../screens/charging/EndRideFindChargingScreen
    - Import RidingToChargingScreen from ../screens/charging/RidingToChargingScreen
    - const Stack = createStackNavigator<ChargeStackParamList>()
    - screenOptions={{ headerShown: false }}
    - Two Stack.Screen: name="EndRideFindCharging" component={EndRideFindChargingScreen}, name="RidingToCharging" component={RidingToChargingScreen}

    --- EndRideFindChargingScreen.tsx ---
    New file at VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx. Composite of ActiveRideScreen (absoluteFill) and MapScreen (Marker onPress + state).

    Imports: React, { useState } from 'react'; View, Text, TouchableOpacity, StyleSheet from 'react-native'; MapView, { Marker } from 'react-native-maps'; useSafeAreaInsets from 'react-native-safe-area-context'; MaterialCommunityIcons from '@expo/vector-icons'; StackScreenProps from '@react-navigation/stack'; ChargeStackParamList from '../../types/navigation'; DSColors, DSTypography from '../../theme/theme'; PrimaryButton from '../../components/common/PrimaryButton'.

    Props type: StackScreenProps<ChargeStackParamList, 'EndRideFindCharging'>. Destructure { navigation }.

    Above component, define:
    - type ChargerStation = { name: string; latitude: number; longitude: number }
    - MOCK_CHARGERS array with the 5 Amsterdam stations (exact values from interfaces block above)
    - USER_LAT = 52.3676, USER_LON = 4.9041

    Inline haversineKm function body (10 lines — from 07-PATTERNS.md, NOT imported).

    State: const [selectedCharger, setSelectedCharger] = useState<ChargerStation | null>(null)
    Compute distance for info card: when selectedCharger is not null: Math.round(haversineKm(USER_LAT, USER_LON, selectedCharger.latitude, selectedCharger.longitude) * 1000)

    JSX structure:
    - Root: View style={StyleSheet.absoluteFill}
      - MapView style={StyleSheet.absoluteFill}, scrollEnabled={true}, zoomEnabled={true}, rotateEnabled={false}, pitchEnabled={false}, onPress={() => setSelectedCharger(null)}, initialRegion: { latitude:USER_LAT, longitude:USER_LON, latitudeDelta:0.02, longitudeDelta:0.02 }
        - MOCK_CHARGERS.map: Marker key={charger.name} coordinate={{ latitude:charger.latitude, longitude:charger.longitude }} onPress={() => setSelectedCharger(charger)} tracksViewChanges={false}
          - View style={styles.chargerPin}
            - MaterialCommunityIcons name='ev-station' size={28} color={DSColors.primary}
      - Close button: TouchableOpacity style={[styles.closeButton, { top: insets.top + 8 }]} onPress={() => navigation.getParent()?.goBack()} accessibilityLabel='Close'
        - MaterialCommunityIcons name='close' size={24} color={DSColors.textPrimary}
      - Conditional info card (selectedCharger !== null): View style={[styles.infoCard, { bottom: insets.bottom + 16 }]}
        - Text style={styles.chargerName}: {selectedCharger.name}
        - Text style={styles.chargerDistance}: {distanceM} m away
        - PrimaryButton label='Navigate Here' onPress={() => navigation.push('RidingToCharging', { chargerName: selectedCharger.name, location: { latitude: selectedCharger.latitude, longitude: selectedCharger.longitude } })}

    StyleSheet.create:
    - chargerPin: { width:40, height:40, borderRadius:20, backgroundColor:DSColors.background, borderWidth:2, borderColor:DSColors.primary, justifyContent:'center', alignItems:'center' }
    - closeButton: { position:'absolute', left:16, backgroundColor:'rgba(255,255,255,0.9)', borderRadius:20, padding:8, zIndex:10 }
    - infoCard: { position:'absolute', left:16, right:16, backgroundColor:DSColors.background, borderRadius:16, padding:16, borderWidth:1, borderColor:DSColors.border, elevation:4, shadowColor:'#000', shadowOffset:{ width:0, height:2 }, shadowOpacity:0.1, shadowRadius:4, zIndex:10 }
    - chargerName: { ...DSTypography.headingMd, color:DSColors.textPrimary, marginBottom:4 }
    - chargerDistance: { ...DSTypography.label, color:DSColors.textSecondary, marginBottom:12 }

    CRITICAL: Do NOT use NativeWind className. Do NOT place charger pins outside the MapView. Do NOT call navigation.push without 'RidingToCharging' being the exact screen name from ChargeStackParamList.

    --- RidingToChargingScreen.tsx ---
    New file at VoltVenture/src/screens/charging/RidingToChargingScreen.tsx. Identical pattern to NavigateToBikeScreen — same absoluteFill structure, same overlay card, same haversineKm ETA formula — but with charger destination instead of bike.

    Imports: React from 'react'; View, Text, StyleSheet from 'react-native'; MapView, { Marker, Polyline } from 'react-native-maps'; useSafeAreaInsets from 'react-native-safe-area-context'; MaterialCommunityIcons from '@expo/vector-icons'; StackScreenProps from '@react-navigation/stack'; ChargeStackParamList from '../../types/navigation'; DSColors, DSTypography from '../../theme/theme'.

    Props type: StackScreenProps<ChargeStackParamList, 'RidingToCharging'>. Destructure { route }. Extract chargerName and location from route.params.

    Inline haversineKm function body (same 10-line copy as EndRideFindCharging and NavigateToBike).

    Constants and ETA derived values:
    - USER_LAT = 52.3676, USER_LON = 4.9041
    - distanceKm = haversineKm(USER_LAT, USER_LON, location.latitude, location.longitude)
    - etaMin = Math.round(distanceKm / 5 * 60)
    - distanceM = Math.round(distanceKm * 1000)

    JSX structure:
    - Root: View style={StyleSheet.absoluteFill}
      - MapView style={StyleSheet.absoluteFill}, scrollEnabled={false}, zoomEnabled={true}, rotateEnabled={false}, pitchEnabled={false}, initialRegion: { latitude:(USER_LAT + location.latitude)/2, longitude:(USER_LON + location.longitude)/2, latitudeDelta:0.03, longitudeDelta:0.03 }
        - Marker (user dot): coordinate={{ latitude:USER_LAT, longitude:USER_LON }} — View style={styles.userMarker}
        - Marker (charger destination): coordinate={{ latitude:location.latitude, longitude:location.longitude }} — MaterialCommunityIcons name='ev-station' size={28} color={DSColors.primary}
        - Polyline: coordinates=[{ latitude:USER_LAT, longitude:USER_LON }, { latitude:52.3690, longitude:4.8970 }, { latitude:52.3700, longitude:4.8950 }, { latitude:location.latitude, longitude:location.longitude }] strokeColor={DSColors.primary} strokeWidth={4}
      - ETA overlay card: View style={[styles.overlayCard, { top: insets.top + 8 }]}
        - Text style={styles.chargerName}: {chargerName}
        - Text style={styles.etaLabel}: {etaMin} min walk — {distanceM} m

    Note: RidingToCharging has NO secondary action button in the ETA card (unlike NavigateToBike which has "View Turn-by-Turn"). The charger is the final destination — no deeper drill.

    StyleSheet.create:
    - overlayCard: { position:'absolute', left:16, right:16, backgroundColor:'rgba(15,15,15,0.85)', borderRadius:16, paddingHorizontal:24, paddingVertical:16, zIndex:10 }
    - chargerName: { ...DSTypography.headingMd, color:'#FFFFFF' }
    - etaLabel: { ...DSTypography.label, color:'#FFFFFF', marginTop:4 }
    - userMarker: { width:16, height:16, borderRadius:8, backgroundColor:'#FFFFFF', borderWidth:3, borderColor:DSColors.primary }

    CRITICAL: Polyline must be a direct JSX child of MapView, NOT in an overlay View. Do NOT use NativeWind.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - VoltVenture/src/navigation/ChargeNavigator.tsx exists and default-exports ChargeNavigator
    - VoltVenture/src/screens/charging/EndRideFindChargingScreen.tsx exists and default-exports EndRideFindChargingScreen
    - VoltVenture/src/screens/charging/RidingToChargingScreen.tsx exists and default-exports RidingToChargingScreen
    - ChargeNavigator.tsx contains both `EndRideFindCharging` and `RidingToCharging` as Stack.Screen names
    - EndRideFindChargingScreen.tsx contains `MOCK_CHARGERS` array with exactly 5 entries
    - EndRideFindChargingScreen.tsx contains `tracksViewChanges={false}` on Marker
    - EndRideFindChargingScreen.tsx contains `setSelectedCharger(null)` on MapView onPress (info card dismiss)
    - EndRideFindChargingScreen.tsx contains `navigation.push('RidingToCharging'`
    - EndRideFindChargingScreen.tsx contains `navigation.getParent()?.goBack()` on close button
    - EndRideFindChargingScreen.tsx contains `haversineKm` defined locally (not imported)
    - RidingToChargingScreen.tsx contains `<Polyline` as child of `<MapView` (not outside it)
    - RidingToChargingScreen.tsx contains `haversineKm` defined locally (not imported)
    - RidingToChargingScreen.tsx does NOT contain a "View Turn-by-Turn" button (no secondary action)
    - Neither new screen file contains `className=` (no NativeWind)
    - `npx tsc --noEmit` produces zero errors (ChargeNavigator module is now resolved)
  </acceptance_criteria>

  <done>ChargeNavigator created; EndRideFindChargingScreen shows 5 charger pins on map with selectable info card and Navigate Here CTA; RidingToChargingScreen shows walking route polyline + ETA card to charger. TypeScript clean — ChargeNavigator missing module error from 07-01 is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Add Find a Charging Station secondary CTA to RideReceiptScreen</name>
  <files>VoltVenture/src/screens/ride/RideReceiptScreen.tsx</files>

  <read_first>
    - VoltVenture/src/screens/ride/RideReceiptScreen.tsx — full file; locate current Done button (lines 79-82), current RN imports, current StyleSheet
    - .planning/phases/07-navigation-ride-extras/07-PATTERNS.md — RideReceiptScreen section for exact add-below pattern and parent-capture navigation code
    - .planning/phases/07-navigation-ride-extras/07-RESEARCH.md — Critical Finding #7 (parent capture requirement and timing)
    - .planning/phases/07-navigation-ride-extras/07-UI-SPEC.md — Integration Point Changes: RideReceiptScreen for button spec (text-only, textSecondary style)
  </read_first>

  <action>
    Read RideReceiptScreen.tsx in full. Make three targeted changes:

    1. Add TouchableOpacity to the React Native import on line 2 alongside existing imports (View, Text, StyleSheet, etc. — check exact current imports and add TouchableOpacity without removing others).

    2. Add the "Find a Charging Station" TouchableOpacity immediately BELOW the existing "Done" PrimaryButton:
       - Component: TouchableOpacity
       - style={styles.findChargingButton} onPress handler (see below)
       - Inner Text style={styles.findChargingLabel}: 'Find a Charging Station'

       The onPress handler MUST use the parent-capture pattern:
         const parent = navigation.getParent();
         parent?.goBack();
         setTimeout(() => { parent?.navigate('ChargeStack', { screen: 'EndRideFindCharging' }); }, 300);

       This is an inline arrow function on the onPress prop. Capture parent BEFORE calling goBack(). After goBack() is called, the navigation prop is no longer valid for cross-stack navigation — only the captured parent reference (pointing to RootNavigator) persists.

    3. Add two new style entries to StyleSheet.create:
       - findChargingButton: { marginTop:12, paddingVertical:12, alignItems:'center' }
       - findChargingLabel: { fontSize:15, fontWeight:'400', color:DSColors.textSecondary }

    Do not change the Done button, any existing functionality, any existing styles, or any other part of the file. The "Find a Charging Station" button is a ghost/text-only link — no border, no background.
  </action>

  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - RideReceiptScreen.tsx contains `TouchableOpacity` in its React Native import
    - RideReceiptScreen.tsx contains a TouchableOpacity with text 'Find a Charging Station'
    - The "Find a Charging Station" TouchableOpacity appears AFTER the Done PrimaryButton in the JSX
    - RideReceiptScreen.tsx contains `const parent = navigation.getParent()` inside the onPress handler
    - RideReceiptScreen.tsx contains `parent?.goBack()` before the setTimeout call
    - RideReceiptScreen.tsx contains `setTimeout` with 300ms delay before `parent?.navigate('ChargeStack'`
    - RideReceiptScreen.tsx contains `screen: 'EndRideFindCharging'` inside the ChargeStack navigate call
    - RideReceiptScreen.tsx contains `findChargingButton` in StyleSheet.create
    - findChargingLabel style uses `color: DSColors.textSecondary` (NOT textPrimary, NOT primary)
    - `npx tsc --noEmit` produces zero errors
  </acceptance_criteria>

  <done>RideReceiptScreen has "Find a Charging Station" ghost text button below "Done". Tapping it captures parent (RootNavigator), dismisses RideStack via goBack(), then navigates to ChargeStack/EndRideFindCharging after 300ms settle delay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.params → RidingToChargingScreen | chargerName (string) and location (lat/lon object) from EndRideFindCharging navigation call |
| navigation.getParent() → RootNavigator | Cross-stack dismiss+navigate from RideReceiptScreen into ChargeStack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | RidingToCharging route.params.location | accept | Location coordinates come from MOCK_CHARGERS constant in EndRideFindCharging — no user-supplied input, no external data source. haversineKm is a pure math function; extreme coord values produce large but finite ETA numbers, not crashes. |
| T-07-03-02 | Tampering | RidingToCharging route.params.chargerName | accept | chargerName is a display string from MOCK_CHARGERS. No SQL/HTML injection surface (rendered via React Native Text only). No sanitization needed for mock frontend data. |
| T-07-03-03 | Denial of Service | setTimeout 300ms in RideReceiptScreen | accept | Standard dismiss-animation settle delay. Fixed 300ms — no infinite loop, no user-controlled delay. |
| T-07-03-04 | Spoofing | navigation.getParent() in EndRideFindCharging close button | accept | Calls goBack() on the parent RootNavigator to dismiss ChargeStack. Correct and intended behavior. No external navigation target. |
| T-07-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in this plan. |
</threat_model>

<verification>
After both tasks complete, run the full TypeScript check:

```
cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture" && npx tsc --noEmit
```

Expected: Zero errors. ChargeNavigator module is now resolved (created in Task 1), closing the pending TS error from 07-01.

Manual flow to verify on emulator after all three plans execute:
1. Complete a ride through to RideReceiptScreen
2. Tap "Find a Charging Station" — RideStack dismisses, then ChargeStack modal slides up
3. EndRideFindCharging shows map centered on Amsterdam with 5 Electric Green charger pins
4. Tap a charger pin → info card appears at bottom with station name, "X m away", "Navigate Here" button
5. Tap "Navigate Here" → RidingToCharging pushes with ETA card showing charger name + walk time and Electric Green polyline
6. Tap close (X) on EndRideFindCharging → ChargeStack dismisses
</verification>

<success_criteria>
1. ChargeNavigator created and resolves the ChargeStack registration in RootNavigator (TypeScript clean, no missing module)
2. EndRideFindCharging shows full-screen MapView with 5 charger pins (ev-station icon, Electric Green border, white background)
3. Tapping a charger pin shows info card with name, distance in meters ("Y m away"), and "Navigate Here" PrimaryButton
4. Tapping map empty area clears the selected charger info card
5. Close button (top-left) dismisses the ChargeStack modal
6. Navigate Here navigates to RidingToCharging with correct chargerName and location params
7. RidingToCharging shows full-screen map with Electric Green polyline (strokeWidth:4), user dot, ev-station destination marker, and floating ETA card showing charger name + "X min walk — Y m"
8. RideReceiptScreen has "Find a Charging Station" text-only link below "Done" button
9. Tapping "Find a Charging Station" dismisses RideStack then opens ChargeStack after 300ms
10. TypeScript compiles with zero errors across all 07-* files
</success_criteria>

<output>
Create `.planning/phases/07-navigation-ride-extras/07-03-SUMMARY.md` when done.
</output>
