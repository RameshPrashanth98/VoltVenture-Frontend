---
phase: 08-payments-and-rewards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VoltVenture/src/types/payment.ts
  - VoltVenture/src/services/paymentService.ts
  - VoltVenture/src/types/navigation.ts
  - VoltVenture/src/navigation/AccountNavigator.tsx
  - VoltVenture/src/navigation/RideNavigator.tsx
autonomous: true
requirements: [PAY-05, PAY-06]

must_haves:
  truths:
    - "paymentService exports getSavedCards(), addCard(), setDefault(), getDefault() alongside untouched processPayment()"
    - "SavedCard interface is exported from types/payment.ts and consumed by paymentService"
    - "AccountStackParamList has AddPaymentMethod: undefined and VoltCoins: undefined entries"
    - "RideStackParamList has SelectPaymentMethod: undefined entry"
    - "AccountNavigator registers AddPaymentMethodScreen and VoltCoinsRewardsScreen (placeholder imports accepted for Wave 2)"
    - "RideNavigator registers SelectPaymentMethodScreen (placeholder import accepted for Wave 2)"
  artifacts:
    - path: "VoltVenture/src/types/payment.ts"
      provides: "SavedCard interface + PaymentResult"
      contains: "export interface SavedCard"
    - path: "VoltVenture/src/services/paymentService.ts"
      provides: "Extended payment service with in-memory card state"
      exports: ["paymentService", "PaymentService", "SavedCard"]
    - path: "VoltVenture/src/types/navigation.ts"
      provides: "Updated param lists for all new routes"
      contains: "AddPaymentMethod"
    - path: "VoltVenture/src/navigation/AccountNavigator.tsx"
      provides: "Registered AddPaymentMethod + VoltCoins screens"
      contains: "AddPaymentMethod"
    - path: "VoltVenture/src/navigation/RideNavigator.tsx"
      provides: "Registered SelectPaymentMethod screen"
      contains: "SelectPaymentMethod"
  key_links:
    - from: "VoltVenture/src/services/paymentService.ts"
      to: "VoltVenture/src/types/payment.ts"
      via: "SavedCard import"
      pattern: "import.*SavedCard.*from.*types/payment"
    - from: "VoltVenture/src/navigation/AccountNavigator.tsx"
      to: "VoltVenture/src/screens/app/AddPaymentMethodScreen.tsx"
      via: "Stack.Screen registration"
      pattern: "AddPaymentMethod.*component"
---

<objective>
Lay the service and navigation foundation that all Phase 8 screens depend on. This plan extends paymentService with in-memory card state, adds SavedCard to payment types, registers three new routes in navigation param lists, and wires screen registrations in AccountNavigator and RideNavigator.

Purpose: Wave 2 plans (08-02, 08-03) cannot be written without these contracts existing first. All screens reading/writing card data call paymentService — establishing it here ensures a single shared module-level state.
Output: Extended paymentService, SavedCard type, updated navigation param lists, updated navigators.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/08-payments-and-rewards/08-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/08-payments-and-rewards/08-RESEARCH.md

<interfaces>
<!-- Current state of files being modified — read before changing anything -->

From VoltVenture/src/types/payment.ts (full file, 6 lines):
  export interface PaymentResult { id: string; amount: number; method: string; timestamp: string; }

From VoltVenture/src/services/paymentService.ts (full file, 22 lines):
  export interface PaymentService { processPayment(summary: RideSummary): Promise<PaymentResult>; }
  const mockPaymentService: PaymentService = { async processPayment(summary) { ... } };
  export const paymentService: PaymentService = mockPaymentService;

From VoltVenture/src/types/navigation.ts — AccountStackParamList currently has:
  AccountMain, RideHistory, PaymentMethods, Profile, EditProfile, Settings,
  Preferences, LoginSecurity, IdScan, FacialScan, SecurityDeposit
  — MISSING: AddPaymentMethod, VoltCoins

From VoltVenture/src/types/navigation.ts — RideStackParamList currently has:
  SafetyMount, ActiveRide, PaymentSummary, RideReceipt
  — MISSING: SelectPaymentMethod

From VoltVenture/src/navigation/AccountNavigator.tsx (full file, 37 lines):
  Registers: AccountMain, RideHistory, PaymentMethods, Profile, EditProfile,
  Settings, Preferences, LoginSecurity, IdScan, FacialScan, SecurityDeposit
  — MISSING registrations: AddPaymentMethod, VoltCoins

From VoltVenture/src/navigation/RideNavigator.tsx (full file, 20 lines):
  Registers: SafetyMount, ActiveRide, PaymentSummary, RideReceipt
  — MISSING registration: SelectPaymentMethod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SavedCard interface and extend paymentService</name>
  <files>
    VoltVenture/src/types/payment.ts,
    VoltVenture/src/services/paymentService.ts
  </files>
  <read_first>
    - VoltVenture/src/types/payment.ts — current PaymentResult interface (full file)
    - VoltVenture/src/services/paymentService.ts — current PaymentService interface + mockPaymentService (full file)
    - VoltVenture/src/types/ride.ts — RideSummary type used by processPayment
    - .planning/phases/08-payments-and-rewards/08-RESEARCH.md — Pattern 1: paymentService Extension, Pitfall 4, Security Domain note
  </read_first>
  <action>
    In types/payment.ts: add the SavedCard interface directly below the existing PaymentResult export. SavedCard has fields: id: string, last4: string, brand: string, expiry: string (MM/YY), cardholderName: string, isDefault: boolean.

    In services/paymentService.ts:
    1. Add import for SavedCard from '../types/payment'.
    2. Widen the PaymentService interface: add getSavedCards(): SavedCard[], addCard(card: Omit<SavedCard, 'id' | 'isDefault'>): SavedCard, setDefault(cardId: string): void, getDefault(): SavedCard | undefined. Keep processPayment() unchanged.
    3. Above the mockPaymentService object, declare a module-level mutable array: const savedCards: SavedCard[] = [{ id: 'card-seed', last4: '4242', brand: 'Visa', expiry: '12/26', cardholderName: 'Test User', isDefault: true }].
    4. Implement the four new methods in mockPaymentService:
       - getSavedCards: returns [...savedCards] (copy)
       - addCard: generates id via 'card-' + Date.now(), sets isDefault: false, pushes to savedCards, returns the new card. Store only last4 (last 4 chars of input last4 field) — do NOT store or log raw card numbers.
       - setDefault: finds current default card and sets isDefault: false, then sets the target card isDefault: true. If cardId not found, no-op.
       - getDefault: returns savedCards.find(c => c.isDefault) ?? savedCards[0].
    5. Keep processPayment() exactly as-is (no changes to its implementation or signature).
    6. The export line remains: export const paymentService: PaymentService = mockPaymentService.
  </action>
  <verify>
    Run: cd VoltVenture && npx tsc --noEmit
    Expected: zero TypeScript errors in payment.ts and paymentService.ts
  </verify>
  <acceptance_criteria>
    - VoltVenture/src/types/payment.ts contains `export interface SavedCard` with fields id, last4, brand, expiry, cardholderName, isDefault
    - VoltVenture/src/services/paymentService.ts contains `getSavedCards(): SavedCard[]` in the PaymentService interface
    - VoltVenture/src/services/paymentService.ts contains `setDefault(cardId: string): void` in the PaymentService interface
    - VoltVenture/src/services/paymentService.ts contains `const savedCards: SavedCard[] = [` with the seed Visa 4242 card
    - VoltVenture/src/services/paymentService.ts does NOT contain any reference to storing a full card number (only last4 is stored)
    - `npx tsc --noEmit` exits 0 (or reports only pre-existing errors not in these two files)
  </acceptance_criteria>
  <done>SavedCard type is exported from payment.ts. paymentService exposes getSavedCards, addCard, setDefault, getDefault alongside untouched processPayment. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation param lists and register screens in navigators</name>
  <files>
    VoltVenture/src/types/navigation.ts,
    VoltVenture/src/navigation/AccountNavigator.tsx,
    VoltVenture/src/navigation/RideNavigator.tsx
  </files>
  <read_first>
    - VoltVenture/src/types/navigation.ts — full file (68 lines) — current param lists
    - VoltVenture/src/navigation/AccountNavigator.tsx — full file (37 lines) — current Stack.Screen registrations
    - VoltVenture/src/navigation/RideNavigator.tsx — full file (20 lines) — current Stack.Screen registrations
    - .planning/phases/08-payments-and-rewards/08-RESEARCH.md — Pitfall 5 (AddPaymentMethod missing from both navigator AND types causes runtime crash)
    - .planning/phases/08-payments-and-rewards/08-CONTEXT.md — D-05 (SelectPaymentMethod in RideStackParamList), D-13 (VoltCoins in AccountStackParamList)
  </read_first>
  <action>
    In types/navigation.ts:
    - In AccountStackParamList: add `AddPaymentMethod: undefined` after the PaymentMethods entry, and `VoltCoins: undefined` after AddPaymentMethod.
    - In RideStackParamList: add `SelectPaymentMethod: undefined` after the PaymentSummary entry (before RideReceipt). No params required per D-05.

    In navigation/AccountNavigator.tsx:
    - Add two import statements near the top (after existing screen imports): import AddPaymentMethodScreen from '../screens/app/AddPaymentMethodScreen' and import VoltCoinsRewardsScreen from '../screens/app/VoltCoinsRewardsScreen'. These screens will be created in Wave 2 (08-02 and 08-03) — the TypeScript import is valid once the files exist. Note: if running tsc before Wave 2 plans execute, these will produce "Cannot find module" errors — that is expected and will resolve after Wave 2.
    - Inside the Stack.Navigator, after the PaymentMethods Stack.Screen, add: Stack.Screen name="AddPaymentMethod" component={AddPaymentMethodScreen} options={{ headerShown: false }}
    - After the SecurityDeposit Stack.Screen, add: Stack.Screen name="VoltCoins" component={VoltCoinsRewardsScreen} options={{ headerShown: false }}

    In navigation/RideNavigator.tsx:
    - Add import for SelectPaymentMethodScreen from '../screens/ride/SelectPaymentMethodScreen'. (Wave 2 will create the file.)
    - Inside the Stack.Navigator, after PaymentSummary Stack.Screen, add: Stack.Screen name="SelectPaymentMethod" component={SelectPaymentMethodScreen} (no options override needed — screenOptions already sets headerShown: false globally).
  </action>
  <verify>
    After Wave 2 plans complete: cd VoltVenture && npx tsc --noEmit — zero errors.
    Before Wave 2 (this wave only): grep -c "AddPaymentMethod" VoltVenture/src/types/navigation.ts should return at least 1. grep -c "SelectPaymentMethod" VoltVenture/src/types/navigation.ts should return at least 1. grep -c "VoltCoins" VoltVenture/src/types/navigation.ts should return at least 1.
  </verify>
  <acceptance_criteria>
    - VoltVenture/src/types/navigation.ts AccountStackParamList contains `AddPaymentMethod: undefined`
    - VoltVenture/src/types/navigation.ts AccountStackParamList contains `VoltCoins: undefined`
    - VoltVenture/src/types/navigation.ts RideStackParamList contains `SelectPaymentMethod: undefined`
    - VoltVenture/src/navigation/AccountNavigator.tsx contains `Stack.Screen name="AddPaymentMethod"`
    - VoltVenture/src/navigation/AccountNavigator.tsx contains `Stack.Screen name="VoltCoins"`
    - VoltVenture/src/navigation/RideNavigator.tsx contains `Stack.Screen name="SelectPaymentMethod"`
  </acceptance_criteria>
  <done>Three new routes are registered: AddPaymentMethod and VoltCoins in AccountStack, SelectPaymentMethod in RideStack. Both navigation.ts param lists and navigator files updated atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → paymentService.addCard() | Unsanitized card form input; only last4 stored — raw number never persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | paymentService.addCard() | mitigate | Store only last4 of card number; never log or store raw card number string |
| T-08-02 | Tampering | Module-level savedCards array | accept | In-memory mock only; no persistence, no real card data; acceptable for frontend mock phase |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
After this plan executes:
- types/payment.ts exports SavedCard interface
- paymentService exports getSavedCards, addCard, setDefault, getDefault, processPayment
- navigation.ts RideStackParamList includes SelectPaymentMethod
- navigation.ts AccountStackParamList includes AddPaymentMethod and VoltCoins
- AccountNavigator.tsx registers AddPaymentMethod and VoltCoins screens
- RideNavigator.tsx registers SelectPaymentMethod screen
</verification>

<success_criteria>
Foundation layer complete: SavedCard type, extended paymentService with module-level card state seeded with Visa 4242, and all three new routes registered in navigation param lists and navigator components. Wave 2 plans (08-02 and 08-03) can now create their screens without touching navigation or service files.
</success_criteria>

<output>
Create .planning/phases/08-payments-and-rewards/08-01-SUMMARY.md when done.
</output>
