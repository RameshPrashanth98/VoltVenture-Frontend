---
phase: 08-payments-and-rewards
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx
  - VoltVenture/src/screens/app/AccountScreen.tsx
  - VoltVenture/src/screens/app/RideHistoryScreen.tsx
autonomous: true
requirements: [REW-01, HIST-01]

must_haves:
  truths:
    - "A VoltCoins Rewards row appears in AccountScreen between Payment Methods and Settings"
    - "VoltCoinsRewardsScreen shows the total VoltCoins balance computed as sum(Math.floor(ride.costEur × 10)) from rideService.getRideHistory()"
    - "Earn history FlatList shows one row per ride with bike name + date on left and +{coins} VoltCoins on right; empty state shown when no rides"
    - "Available Rewards section shows 3–4 static reward cards; tapping Redeem shows Snackbar 'Rewards redemption coming soon'"
    - "RideHistoryScreen stats header is always visible (even with zero rides) showing Total Rides, Distance, Total Spend, CO2 Saved tiles"
    - "Stats values for empty history show: 0 rides, 0.0 km, €0.00, 0.0 kg CO2"
    - "CO2 formula: totalDistanceKm × 0.21 displayed as X.X kg CO2 (per D-09)"
  artifacts:
    - path: "VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx"
      provides: "VoltCoins balance + earn history + rewards catalog"
      exports: ["default VoltCoinsRewardsScreen"]
    - path: "VoltVenture/src/screens/app/AccountScreen.tsx"
      provides: "VoltCoins Rewards menu row between Payment Methods and Settings"
      contains: "VoltCoins"
    - path: "VoltVenture/src/screens/app/RideHistoryScreen.tsx"
      provides: "Stats header via ListHeaderComponent with 4 stat tiles"
      contains: "ListHeaderComponent"
  key_links:
    - from: "VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx"
      to: "VoltVenture/src/services/rideService.ts"
      via: "rideService.getRideHistory()"
      pattern: "rideService\\.getRideHistory"
    - from: "VoltVenture/src/screens/app/RideHistoryScreen.tsx"
      to: "ListHeaderComponent stats block"
      via: "rides.reduce() aggregations"
      pattern: "ListHeaderComponent"
    - from: "VoltVenture/src/screens/app/AccountScreen.tsx"
      to: "VoltCoinsRewardsScreen"
      via: "navigation.navigate('VoltCoins')"
      pattern: "navigate.*VoltCoins"
---

## Phase Goal

**As a** tourist, **I want to** see my VoltCoins rewards balance and my aggregate ride statistics, **so that** I can track my loyalty progress and understand my riding habits at a glance.

<objective>
Deliver the rewards and stats vertical slice: VoltCoinsRewardsScreen (balance + earn history + rewards catalog) accessible from AccountScreen, plus a stats summary header added to the existing RideHistoryScreen.

Purpose: REW-01 (VoltCoins balance and earn history) and HIST-01 (aggregate ride stats) both derive their data from the same rideService.getRideHistory() — no service changes needed, pure computation in-screen.
Output: New VoltCoinsRewardsScreen, AccountScreen VoltCoins row, RideHistoryScreen stats header.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/VoltVenture/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/08-payments-and-rewards/08-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/08-payments-and-rewards/08-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/7.Frontend/.planning/phases/08-payments-and-rewards/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From VoltVenture/src/services/rideService.ts (read-only):
  interface RideService { getRideHistory(): RideSummary[]; }
  export const rideService: RideService;
  RideSummary fields used: id, bikeName, startTime, costEur, distanceKm, durationMin

From VoltVenture/src/types/navigation.ts (after 08-01):
  AccountStackParamList includes: VoltCoins: undefined

From VoltVenture/src/screens/app/AccountScreen.tsx (current, 282 lines):
  - Payment Methods row ends at approximately line 109
  - Settings row starts at approximately line 111
  - INSERT VoltCoins row BETWEEN these two (between Payment Methods block end and Settings block start)
  - menuRow style: flexDirection:'row', alignItems:'center', justifyContent:'space-between',
    paddingHorizontal:24, paddingVertical:16, backgroundColor:DSColors.surface,
    borderTopWidth:1, borderColor:DSColors.border
  - menuRowLeft style: flexDirection:'row', alignItems:'center', gap:12
  - menuRowText style: fontSize:16, fontWeight:'400', color:DSColors.textPrimary
  - Icons used: MaterialCommunityIcons, already imported

From VoltVenture/src/screens/app/RideHistoryScreen.tsx (current, 111 lines):
  - FlatList at lines 55–61: data={rides}, keyExtractor, renderItem, ListEmptyComponent, ItemSeparatorComponent
  - rides = rideService.getRideHistory() at line 28
  - formatDate(isoString) helper at lines 13–17
  - All styles: safeArea, emptyContainer, emptyText, rideRow, rideBikeName, rideMeta, rideCost, separator

From VoltVenture/src/screens/app/PaymentMethodsScreen.tsx — sectionHeader style (reuse in VoltCoinsRewards):
  fontSize:12, fontWeight:'600', color:DSColors.textSecondary, textTransform:'uppercase',
  letterSpacing:0.8, paddingHorizontal:24, paddingTop:20, paddingBottom:8

From VoltVenture/src/screens/app/SecurityDepositScreen.tsx — Snackbar in Portal pattern:
  Portal > Snackbar visible={snackVisible} onDismiss={...} duration={3000} > message text

Custom header pattern (from SecurityDepositScreen):
  View row: TouchableOpacity (arrow-left icon) + Text (title) + View (width:40)
  header style: flexDirection:'row', alignItems:'center', justifyContent:'space-between',
    paddingHorizontal:24, paddingTop:16, paddingBottom:12
  headerTitle style: fontSize:20, fontWeight:'600', color:DSColors.textPrimary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VoltCoinsRewardsScreen + add VoltCoins row to AccountScreen</name>
  <files>
    VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx,
    VoltVenture/src/screens/app/AccountScreen.tsx
  </files>
  <read_first>
    - VoltVenture/src/screens/app/AccountScreen.tsx — full file (282 lines) — locate Payment Methods row end and Settings row start for insertion point
    - VoltVenture/src/screens/app/RideHistoryScreen.tsx — formatDate helper (lines 13–17) to reuse in earn history
    - VoltVenture/src/services/rideService.ts — getRideHistory() and RideSummary shape
    - VoltVenture/src/screens/app/SecurityDepositScreen.tsx — Snackbar in Portal pattern, custom header pattern
    - VoltVenture/src/screens/app/PaymentMethodsScreen.tsx — sectionHeader style values to match
    - .planning/phases/08-payments-and-rewards/08-CONTEXT.md — D-10 (balance formula), D-11 (earn history rows), D-12 (static rewards), D-13 (route name VoltCoins, position between PaymentMethods and Settings)
    - .planning/phases/08-payments-and-rewards/08-RESEARCH.md — Pitfall 6 (NaN guard), Anti-Patterns (Snackbar outside Portal), specifics (balance display, reward names and coin costs)
  </read_first>
  <action>
    CREATE VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx:
    - Prop type: StackScreenProps<AccountStackParamList, 'VoltCoins'>
    - Imports: React, useState, View, Text, FlatList, TouchableOpacity, StyleSheet, SafeAreaView from react-native-safe-area-context, Portal + Snackbar from react-native-paper, MaterialCommunityIcons, StackScreenProps, AccountStackParamList, DSColors, rideService, RideSummary from '../../types/ride'
    - Data: const rides = rideService.getRideHistory() (called once at render)
    - VoltCoins balance: const voltCoins = rides.reduce((acc, r) => acc + Math.floor((r.costEur ?? 0) * 10), 0)
    - State: snackVisible: boolean (false)

    formatDate helper (inline, same logic as RideHistoryScreen): converts ISO string to 'Mon D' format using toLocaleDateString('en-US', { month: 'short', day: 'numeric' })

    Static rewards array (module-level const, not in component state):
      const REWARDS = [
        { id: 'r1', name: '€1 off next ride', coins: 100 },
        { id: 'r2', name: 'Free 10-min ride', coins: 500 },
        { id: 'r3', name: 'Partner café voucher', coins: 250 },
        { id: 'r4', name: 'Priority bike hold', coins: 75 },
      ]

    Layout (ScrollView root — NOT FlatList, because earn history list is short and we need rewards below it; use ScrollView with nested map, not nested FlatList):
    - SafeAreaView edges={['bottom']}
    - Custom header: arrow-left goBack + "VoltCoins Rewards" title + width:40 spacer
    - Balance card section: centered View with MaterialCommunityIcons name="coins" size={48} color={DSColors.primary} + large Text showing voltCoins (fontSize:48, fontWeight:'700', color:DSColors.textPrimary) + Text "VoltCoins" below (fontSize:16, color:DSColors.textSecondary) + paddingVertical:32 paddingHorizontal:24 backgroundColor:DSColors.surface marginBottom:16
    - Section header "EARN HISTORY" (matches PaymentMethodsScreen sectionHeader style)
    - If rides.length === 0: empty state View with MaterialCommunityIcons name="history" size={32} color={DSColors.textSecondary} + Text "No rides yet — earn VoltCoins on your next ride." (matching RideHistoryScreen emptyText style)
    - If rides.length > 0: map over rides — each row is a View (earnRow) with flexDirection:'row', justifyContent:'space-between', alignItems:'center', paddingHorizontal:24, paddingVertical:14. Left: Text "{ride.bikeName}" (fontSize:15, fontWeight:'500', color:DSColors.textPrimary) + Text formatDate(ride.startTime) (fontSize:13, color:DSColors.textSecondary, marginTop:2). Right: Text "+{Math.floor((ride.costEur ?? 0) * 10)} VoltCoins" (fontSize:14, fontWeight:'600', color:DSColors.primary). Between rows: View height:1 backgroundColor:DSColors.border marginHorizontal:24.
    - Section header "AVAILABLE REWARDS" below earn history
    - Map over REWARDS — each reward card: View with backgroundColor:DSColors.surface, borderRadius:12, borderWidth:1, borderColor:DSColors.border, padding:16, marginHorizontal:24, marginBottom:12. Inside: Text reward.name (fontSize:15, fontWeight:'600', color:DSColors.textPrimary) + Text "{reward.coins} VoltCoins" (fontSize:13, color:DSColors.textSecondary, marginTop:4) + TouchableOpacity "Redeem" button (marginTop:12, alignSelf:'flex-start', paddingVertical:8, paddingHorizontal:16, backgroundColor:DSColors.surface, borderWidth:1, borderColor:DSColors.border, borderRadius:8) with Text "Redeem" (fontSize:13, fontWeight:'500', color:DSColors.textPrimary) onPress: setSnackVisible(true)
    - Portal > Snackbar visible={snackVisible} onDismiss={() => setSnackVisible(false)} duration={3000}: "Rewards redemption coming soon"

    UPDATE VoltVenture/src/screens/app/AccountScreen.tsx:
    - After the Payment Methods TouchableOpacity block (ends around line 109) and before the Settings TouchableOpacity block (starts around line 111), insert a new TouchableOpacity using the exact same menuRow + menuRowLeft + menuRowText styles:
      icon: MaterialCommunityIcons name="star-circle" size={20} color={DSColors.textPrimary}
      text: "VoltCoins Rewards"
      onPress: navigation.navigate('VoltCoins')
      accessibilityRole="button" accessibilityLabel="VoltCoins Rewards"
      chevron-right icon on right
    - Do not change any other rows, styles, or logic in AccountScreen
  </action>
  <verify>
    cd VoltVenture && npx tsc --noEmit
    Behavioral:
    - AccountScreen shows "VoltCoins Rewards" row between "Payment Methods" and "Settings"
    - Tapping it opens VoltCoinsRewardsScreen
    - Balance shows 0 VoltCoins when no rides, or computed sum when rides exist
    - 4 reward cards shown; tapping Redeem shows Snackbar "Rewards redemption coming soon"
    - Earn history shows ride rows or empty state depending on rideService data
  </verify>
  <acceptance_criteria>
    - VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx exists with default export
    - VoltCoinsRewardsScreen contains `rideService.getRideHistory()`
    - VoltCoinsRewardsScreen contains `Math.floor((r.costEur ?? 0) * 10)` in reduce for balance
    - VoltCoinsRewardsScreen contains `Portal` wrapping `Snackbar`
    - VoltCoinsRewardsScreen contains the REWARDS static array with at least 3 entries
    - VoltCoinsRewardsScreen contains "Rewards redemption coming soon" string
    - VoltVenture/src/screens/app/AccountScreen.tsx contains `navigation.navigate('VoltCoins')`
    - AccountScreen VoltCoins row appears in JSX between the PaymentMethods TouchableOpacity and the Settings TouchableOpacity
    - `npx tsc --noEmit` exits 0 (or pre-existing errors only)
  </acceptance_criteria>
  <done>VoltCoinsRewardsScreen delivers balance (computed live), earn history list (per-ride rows), and static rewards catalog with Snackbar feedback. AccountScreen exposes the new screen via a menu row between Payment Methods and Settings.</done>
</task>

<task type="auto">
  <name>Task 2: Add stats summary header to RideHistoryScreen</name>
  <files>VoltVenture/src/screens/app/RideHistoryScreen.tsx</files>
  <read_first>
    - VoltVenture/src/screens/app/RideHistoryScreen.tsx — full file (111 lines) — current FlatList props, rides state, all StyleSheet styles
    - .planning/phases/08-payments-and-rewards/08-CONTEXT.md — D-07 (ListHeaderComponent, above FlatList), D-08 (always visible, zeroed values when empty, 2×2 grid layout), D-09 (CO2 = totalDistanceKm × 0.21)
    - .planning/phases/08-payments-and-rewards/08-RESEARCH.md — Pattern 2 (FlatList ListHeaderComponent), Pitfall 3 (no ScrollView wrapping), code example "FlatList with Empty State", specifics (2×2 grid — bike icon, map marker, cash, leaf)
  </read_first>
  <action>
    Modify VoltVenture/src/screens/app/RideHistoryScreen.tsx:

    Add imports: MaterialCommunityIcons is already imported. No new package imports needed.

    Derive stats from the existing rides variable (already fetched on line 28):
    - totalRides = rides.length
    - totalKm = rides.reduce((acc, r) => acc + r.distanceKm, 0)
    - totalEur = rides.reduce((acc, r) => acc + r.costEur, 0)
    - co2Kg = rides.reduce((acc, r) => acc + r.distanceKm, 0) * 0.21

    Define a StatsHeader component INSIDE the function component (or as a local const before return):
    - Outer View: flexDirection:'row', flexWrap:'wrap', paddingHorizontal:16, paddingTop:16, paddingBottom:8, backgroundColor:DSColors.background
    - 4 stat tiles arranged in a 2×2 grid: each tile is a View with width:'50%', padding:8
    - Each tile inner card: backgroundColor:DSColors.surface, borderRadius:12, borderWidth:1, borderColor:DSColors.border, padding:12, alignItems:'center'
    - Tile 1: MaterialCommunityIcons name="bike" size={22} color={DSColors.primary} + Text totalRides.toString() (fontSize:24, fontWeight:'700', color:DSColors.textPrimary) + Text "Total Rides" (fontSize:12, color:DSColors.textSecondary, marginTop:4)
    - Tile 2: MaterialCommunityIcons name="map-marker-distance" size={22} color={DSColors.primary} + Text totalKm.toFixed(1) + " km" (fontSize:24, fontWeight:'700') + Text "Distance" (fontSize:12, color:DSColors.textSecondary)
    - Tile 3: MaterialCommunityIcons name="cash" size={22} color={DSColors.primary} + Text "€" + totalEur.toFixed(2) (fontSize:24, fontWeight:'700') + Text "Total Spend" (fontSize:12, color:DSColors.textSecondary)
    - Tile 4: MaterialCommunityIcons name="leaf" size={22} color={DSColors.primary} + Text co2Kg.toFixed(1) + " kg" (fontSize:24, fontWeight:'700') + Text "CO2 Saved" (fontSize:12, color:DSColors.textSecondary)
    - Below the 2×2 grid: Text "Ride History" section label (same sectionHeader style as PaymentMethodsScreen: fontSize:12, fontWeight:'600', color:DSColors.textSecondary, textTransform:'uppercase', letterSpacing:0.8, paddingHorizontal:24, paddingTop:12, paddingBottom:8) to visually separate stats from the ride list below

    On the existing FlatList (lines 55–61), add prop: ListHeaderComponent={StatsHeader}
    Do NOT wrap the FlatList in a ScrollView — pass StatsHeader directly as the ListHeaderComponent prop.
    All other FlatList props (data, keyExtractor, renderItem, ListEmptyComponent, ItemSeparatorComponent) remain unchanged.

    Add the necessary StyleSheet entries for: statsTile, statsTileCard, statValue, statLabel — do not rename or remove any existing styles.
  </action>
  <verify>
    cd VoltVenture && npx tsc --noEmit
    Behavioral:
    - RideHistoryScreen shows 4 stat tiles before the ride list — visible even when rides array is empty
    - Empty state: tiles show "0", "0.0 km", "€0.00", "0.0 kg"
    - After completing a ride: tiles update to reflect the new totals
    - No "VirtualizedLists should never be nested" warning in Metro console
  </verify>
  <acceptance_criteria>
    - VoltVenture/src/screens/app/RideHistoryScreen.tsx contains `ListHeaderComponent=`
    - File contains `totalRides` derived from `rides.length`
    - File contains `co2Kg` derived from `rides.reduce` multiplied by `0.21` (per D-09)
    - File contains `"CO2 Saved"` label text
    - File contains `"Total Rides"` label text
    - File does NOT contain any ScrollView wrapping the FlatList
    - `npx tsc --noEmit` exits 0 (or pre-existing errors only)
  </acceptance_criteria>
  <done>RideHistoryScreen stats header always visible above the ride list, showing 4 tiles (Total Rides, Distance, Total Spend, CO2 Saved) in a 2×2 grid computed from rideService data via ListHeaderComponent. Empty state shows zero values without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rideService.getRideHistory() → screen | Read-only in-memory data; no user-controlled input in this plan |
| Redeem CTA → Snackbar | No state mutation; purely presentational feedback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | VoltCoins balance computation | accept | Derived from mock ride data only; no real financial data; no persistence |
| T-08-06 | Tampering | Static REWARDS array | accept | Hardcoded in module scope; no user mutation path; Redeem is a stub |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
After this plan:
- AccountScreen has VoltCoins Rewards row between Payment Methods and Settings
- VoltCoinsRewardsScreen shows live balance (0 or computed), earn history rows, 4 reward cards, Snackbar on Redeem
- RideHistoryScreen shows stats header via ListHeaderComponent — always visible, 4 tiles in 2×2 grid, CO2 = distanceKm × 0.21
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
REW-01 and HIST-01 delivered as a complete vertical slice: user can navigate to VoltCoins Rewards from Account screen, see balance + earn history + rewards catalog, and see aggregate ride stats at the top of Ride History. All per D-07 through D-13.
</success_criteria>

<output>
Create .planning/phases/08-payments-and-rewards/08-03-SUMMARY.md when done.
</output>
