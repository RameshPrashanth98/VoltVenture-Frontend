---
phase: 09-discovery-and-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VoltVenture/src/types/navigation.ts
  - VoltVenture/src/navigation/AppTabs.tsx
  - VoltVenture/src/navigation/DiscoverNavigator.tsx
  - VoltVenture/src/navigation/NavNavigator.tsx
  - VoltVenture/src/components/map/CafeMarker.tsx
  - VoltVenture/src/components/map/CafeDetailSheet.tsx
  - VoltVenture/src/screens/app/MapScreen.tsx
autonomous: true
requirements:
  - DISC-05
  - CONT-01

must_haves:
  truths:
    - "Discover tab appears as the 2nd tab (Map | Discover | Account) with compass icon"
    - "Tapping a white café pin on MapScreen opens a bottom sheet with name, hours, distance, and Get Directions CTA"
    - "Get Directions in CafeDetailSheet navigates to NavStack/NavigateToPoi with café name and coordinates"
    - "NavigateToPoi screen exists and is registered in NavNavigator"
    - "TypeScript compiles without errors after all type additions"
  artifacts:
    - path: "VoltVenture/src/types/navigation.ts"
      provides: "DiscoverStackParamList, NavigateToPoi in NavStackParamList, Discover in AppTabParamList"
      contains: "DiscoverStackParamList"
    - path: "VoltVenture/src/navigation/DiscoverNavigator.tsx"
      provides: "Stack navigator for Discover tab with 6 screens"
      exports: ["default DiscoverNavigator"]
    - path: "VoltVenture/src/components/map/CafeMarker.tsx"
      provides: "White circle + coffee icon marker component"
      exports: ["default CafeMarker"]
    - path: "VoltVenture/src/components/map/CafeDetailSheet.tsx"
      provides: "BottomSheetModal content with café info and Get Directions CTA"
      exports: ["default CafeDetailSheet"]
  key_links:
    - from: "VoltVenture/src/navigation/AppTabs.tsx"
      to: "VoltVenture/src/navigation/DiscoverNavigator.tsx"
      via: "Tab.Screen name=Discover component=DiscoverNavigator"
      pattern: "Discover.*DiscoverNavigator"
    - from: "VoltVenture/src/screens/app/MapScreen.tsx"
      to: "VoltVenture/src/components/map/CafeDetailSheet.tsx"
      via: "cafeDetailRef + BottomSheetModal"
      pattern: "cafeDetailRef|CafeDetailSheet"
    - from: "VoltVenture/src/screens/app/MapScreen.tsx"
      to: "NavStack/NavigateToPoi"
      via: "navigation.navigate in onGetDirections"
      pattern: "NavigateToPoi"
---

<objective>
Establish the navigation foundation for Phase 9: add DiscoverStackParamList and NavigateToPoi to the type system, register the Discover tab in AppTabs with its DiscoverNavigator, create the DiscoverNavigator stack, and wire café map integration (CafeMarker component, CafeDetailSheet component, MapScreen café data + ref + marker rendering).

Purpose: Every Wave 2 plan depends on these types and navigation routes existing. MapScreen café integration must ship in Wave 1 because CafeDetailSheet navigates to NavigateToPoi which lives in the NavStack (already exists from Phase 7) — this cross-stack navigation must be verified before screens consuming it are built.

Output: Working Discover tab (renders placeholder until DiscoverScreen exists), café pins on MapScreen that open a bottom sheet with Get Directions CTA routing to NavigateToPoi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-discovery-and-content/09-CONTEXT.md
@.planning/phases/09-discovery-and-content/09-UI-SPEC.md
@.planning/phases/09-discovery-and-content/09-RESEARCH.md
@.planning/phases/09-discovery-and-content/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation type system + DiscoverNavigator + NavNavigator update</name>

  <read_first>
    VoltVenture/src/types/navigation.ts — read full file to see current AppTabParamList, NavStackParamList, AccountStackParamList, and NavigatorScreenParams import location before making any changes
    VoltVenture/src/navigation/AppTabs.tsx — read full file to see current Tab.Screen order, icon format, and MaterialCommunityIcons import before inserting Discover tab
    VoltVenture/src/navigation/NavNavigator.tsx — read full file to see current Stack.Screen registrations and import pattern before adding NavigateToPoi
    VoltVenture/src/navigation/AccountNavigator.tsx — read as the exact pattern to replicate for DiscoverNavigator (stack navigator structure, headerShown options)
    VoltVenture/src/screens/discover/ — this directory does not exist yet; DiscoverNavigator imports screen files that will be created in Wave 2, so use placeholder component references or stub imports with a TODO comment to avoid import errors during Wave 1
  </read_first>

  <action>
    STEP 1 — Update navigation.ts (must happen before AppTabs.tsx to avoid TypeScript errors per RESEARCH.md Pitfall 1):

    Add DiscoverStackParamList after AccountStackParamList:
    - DiscoverMain: undefined
    - CuratedRoutes: undefined
    - VipHubs: undefined
    - Support: undefined
    - PrivacyPolicy: undefined
    - TermsOfService: undefined

    Update AppTabParamList to add Discover between Map and Account:
    - Map: undefined
    - Discover: NavigatorScreenParams<DiscoverStackParamList>
    - Account: NavigatorScreenParams<AccountStackParamList>
    (NavigatorScreenParams is already imported — no new import needed)

    Update NavStackParamList to add NavigateToPoi:
    - NavigateToBike: { bike: Bike }
    - WalkingDirections: { bike: Bike }
    - NavigateToPoi: { name: string; location: { latitude: number; longitude: number } }

    STEP 2 — Create VoltVenture/src/navigation/DiscoverNavigator.tsx:
    Mirror AccountNavigator.tsx exactly. Use createStackNavigator<DiscoverStackParamList>(). Register 6 screens: DiscoverMain, CuratedRoutes, VipHubs, Support, PrivacyPolicy, TermsOfService — all with headerShown: false. Because the screen files do not yet exist, create stub placeholder components inline at the top of DiscoverNavigator.tsx (one line each: export const PlaceholderScreen = () => null) and use them as temporary component references. Label them with // TODO: replace with actual screen import in Wave 2. No ProfileProvider wrapper.

    STEP 3 — Update VoltVenture/src/navigation/AppTabs.tsx:
    Import DiscoverNavigator. Insert Discover Tab.Screen between Map and Account:
    - name: "Discover"
    - component: DiscoverNavigator
    - tabBarLabel: "Discover"
    - tabBarIcon: MaterialCommunityIcons name={focused ? 'compass' : 'compass-outline'} color={color} size={size}

    STEP 4 — Update VoltVenture/src/navigation/NavNavigator.tsx:
    Import NavigateToPoiScreen from '../screens/navigation/NavigateToPoiScreen' (file will be created in Plan 09-03). To avoid import error, create a one-line stub file at VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx with: import React from 'react'; import { View } from 'react-native'; export default function NavigateToPoiScreen() { return <View />; }
    Then register: Stack.Screen name="NavigateToPoi" component={NavigateToPoiScreen}
  </action>

  <verify>
    VoltVenture/src/types/navigation.ts contains "DiscoverStackParamList"
    VoltVenture/src/types/navigation.ts contains "NavigateToPoi"
    VoltVenture/src/types/navigation.ts contains "Discover: NavigatorScreenParams"
    VoltVenture/src/navigation/AppTabs.tsx contains "Discover" and "compass"
    VoltVenture/src/navigation/DiscoverNavigator.tsx exists and contains "createStackNavigator"
    VoltVenture/src/navigation/NavNavigator.tsx contains "NavigateToPoi"
  </verify>

  <acceptance_criteria>
    - navigation.ts: DiscoverStackParamList type exported with exactly 6 routes (DiscoverMain, CuratedRoutes, VipHubs, Support, PrivacyPolicy, TermsOfService)
    - navigation.ts: AppTabParamList contains "Discover: NavigatorScreenParams<DiscoverStackParamList>"
    - navigation.ts: NavStackParamList contains "NavigateToPoi: { name: string; location: { latitude: number; longitude: number } }"
    - DiscoverNavigator.tsx: file exists, imports createStackNavigator, registers exactly 6 Stack.Screen entries with headerShown: false
    - AppTabs.tsx: Tab.Screen name="Discover" appears between Map and Account screens, uses compass/compass-outline icons
    - NavNavigator.tsx: Stack.Screen name="NavigateToPoi" registered
    - A stub NavigateToPoiScreen.tsx exists so NavNavigator.tsx compiles without import errors
  </acceptance_criteria>

  <done>Navigation type system and navigator files updated. Discover tab renders in AppTabs (with placeholder screens). NavigateToPoi route registered in NavStack. No TypeScript errors on these files.</done>
</task>

<task type="auto">
  <name>Task 2: CafeMarker + CafeDetailSheet components + MapScreen café integration</name>

  <read_first>
    VoltVenture/src/components/map/BikeMarker.tsx — read full file; CafeMarker is a direct copy-and-adapt (4 color/icon value changes per PATTERNS.md)
    VoltVenture/src/components/map/BikeDetailSheet.tsx — read full file; CafeDetailSheet replicates this exactly (different props interface and content)
    VoltVenture/src/screens/app/MapScreen.tsx — read full file to understand: current import list (lines 1-20), haversineKm function location (lines 18-29), selectedBike/bikeDetailRef/bikeSnapPoints state pattern (lines 34-42), renderBackdrop definition (lines 100-103), Marker render loop in MapView (lines 128-137), BottomSheetModal placement (lines 158-177)
    VoltVenture/src/components/common/PrimaryButton.tsx — read for props interface (label, onPress)
    VoltVenture/src/theme/theme.ts — read DSColors for: background, border, textPrimary, textSecondary, primary, textOnPrimary, surface
  </read_first>

  <action>
    STEP 1 — Create VoltVenture/src/components/map/CafeMarker.tsx:
    Exact copy of BikeMarker.tsx with these 4 changes:
    - circle.backgroundColor: DSColors.primary → DSColors.background (white)
    - circle.borderColor: DSColors.textOnPrimary → DSColors.border (#EBEBEB border)
    - tail.borderTopColor: DSColors.primary → DSColors.background (white tail)
    - Icon: name="lightning-bolt" → name="coffee", color: DSColors.textOnPrimary → DSColors.textPrimary
    No other changes. Component takes no props.

    STEP 2 — Create VoltVenture/src/components/map/CafeDetailSheet.tsx:
    Declare Cafe type locally: { id: string; name: string; hours: string; latitude: number; longitude: number }
    Props interface CafeDetailSheetProps: { cafe: Cafe | null; userLocation: { latitude: number; longitude: number } | null; onGetDirections: () => void }
    Null guard at top: if (!cafe) return null
    Include inline haversineKm function (copy from MapScreen — same function body, comment "Inline copy from MapScreen.tsx — haversineKm is not exported")
    Compute distanceKm: userLocation ? haversineKm(userLocation.latitude, userLocation.longitude, cafe.latitude, cafe.longitude) : null
    Root element: BottomSheetView from @gorhom/bottom-sheet, style container (paddingHorizontal: 24, paddingBottom: 32)
    Content order per UI-SPEC §5:
    1. Drag handle: View width 40, height 4, borderRadius 2, backgroundColor DSColors.border, alignSelf center, marginTop 8, marginBottom 16
    2. Photo placeholder: View height 160, backgroundColor DSColors.surface, borderRadius 8, marginBottom 16, overflow hidden, alignItems center, justifyContent center — contains MaterialCommunityIcons name="coffee" size={48} color={DSColors.textSecondary}
    3. Café name: Text DSTypography.heading, color DSColors.textPrimary, marginBottom 4
    4. Hours row: View flexDirection row gap 6, marginBottom 4 — clock-outline icon size 14 DSColors.textSecondary + Text 15px DSColors.textSecondary
    5. Distance row: View flexDirection row gap 6, marginBottom 20 — map-marker icon size 14 DSColors.textSecondary + Text 15px DSColors.textSecondary showing distanceKm.toFixed(1) + " km away" or "—"
    6. PrimaryButton label="Get Directions" onPress={onGetDirections}
    Styles: StyleSheet.create + DSColors only (no NativeWind).

    STEP 3 — Modify VoltVenture/src/screens/app/MapScreen.tsx:
    a. Imports: add CafeMarker from '../../components/map/CafeMarker' and CafeDetailSheet from '../../components/map/CafeDetailSheet' alongside existing BikeMarker/BikeDetailSheet imports.
    b. After haversineKm function, add:
       - Cafe type (same definition as in CafeDetailSheet)
       - MOCK_CAFES const array with exactly 5 entries from UI-SPEC §5:
         { id: 'c1', name: 'Café de Jaren', hours: 'Mon–Sun 9:00–23:00', latitude: 52.3684, longitude: 4.8960 }
         { id: 'c2', name: 'Screaming Beans', hours: 'Mon–Fri 8:00–17:00, Sat–Sun 9:00–17:00', latitude: 52.3637, longitude: 4.8833 }
         { id: 'c3', name: 'Lot Sixty One Coffee', hours: 'Mon–Fri 8:00–17:00, Sat 9:00–17:00', latitude: 52.3612, longitude: 4.8745 }
         { id: 'c4', name: 'Headfirst Coffee', hours: 'Mon–Sun 8:00–18:00', latitude: 52.3703, longitude: 4.9014 }
         { id: 'c5', name: 'Black Gold Coffee', hours: 'Mon–Sun 8:30–18:00', latitude: 52.3671, longitude: 4.9044 }
    c. Inside the component function, parallel to selectedBike and bikeDetailRef, add:
       - const [selectedCafe, setSelectedCafe] = useState<Cafe | null>(null)
       - const cafeDetailRef = useRef<BottomSheetModal>(null)
       - const cafeSnapPoints = useMemo(() => ['55%'], [])
    d. Add handleCafeMarkerPress callback (parallel to handleMarkerPress):
       const handleCafeMarkerPress = useCallback((cafe: Cafe) => { setSelectedCafe(cafe); cafeDetailRef.current?.present(); }, [])
    e. Inside MapView JSX, after the existing bike Marker render loop, add the café Marker render loop:
       MOCK_CAFES.map(cafe => Marker key={cafe.id} coordinate={{latitude, longitude}} onPress={() => handleCafeMarkerPress(cafe)} tracksViewChanges={false} — child: CafeMarker component
    f. After the existing BikeDetailSheet BottomSheetModal in JSX, add CafeDetailSheet BottomSheetModal:
       - ref={cafeDetailRef}, snapPoints={cafeSnapPoints}, enablePanDownToClose, backdropComponent={renderBackdrop} (reuse existing renderBackdrop — no second callback needed per RESEARCH.md Pitfall 2)
       - Child: CafeDetailSheet cafe={selectedCafe} userLocation={userLocation} onGetDirections callback:
         if (!selectedCafe) return; cafeDetailRef.current?.dismiss(); then navigation.navigate('NavStack', { screen: 'NavigateToPoi', params: { name: selectedCafe.name, location: { latitude: selectedCafe.latitude, longitude: selectedCafe.longitude } } })
  </action>

  <verify>
    VoltVenture/src/components/map/CafeMarker.tsx contains "DSColors.background" and "coffee"
    VoltVenture/src/components/map/CafeDetailSheet.tsx contains "BottomSheetView" and "haversineKm" and "if (!cafe) return null"
    VoltVenture/src/screens/app/MapScreen.tsx contains "MOCK_CAFES" and "cafeDetailRef" and "CafeDetailSheet" and "NavigateToPoi"
    VoltVenture/src/screens/app/MapScreen.tsx contains "Café de Jaren" (confirms mock data present)
  </verify>

  <acceptance_criteria>
    - CafeMarker.tsx: circle backgroundColor uses DSColors.background (NOT DSColors.primary); icon name is "coffee" (NOT "lightning-bolt"); tail borderTopColor uses DSColors.background; component takes no props
    - CafeDetailSheet.tsx: contains null guard "if (!cafe) return null"; contains haversineKm inline function with comment; photo placeholder height is 160 (per UI-SPEC, not 120 from PATTERNS.md — UI-SPEC takes precedence); PrimaryButton label is "Get Directions"
    - MapScreen.tsx: MOCK_CAFES array has exactly 5 items including "Café de Jaren" and "Black Gold Coffee"; cafeDetailRef useRef<BottomSheetModal> declared; cafeSnapPoints = ['55%']; CafeMarker rendered inside Marker with tracksViewChanges={false}; CafeDetailSheet BottomSheetModal placed after BikeDetailSheet BottomSheetModal; onGetDirections navigates to NavStack screen NavigateToPoi with name and location params
    - MapScreen.tsx uses the existing shared renderBackdrop callback for CafeDetailSheet (no duplicate useCallback)
  </acceptance_criteria>

  <done>CafeMarker and CafeDetailSheet components exist. MapScreen renders white café pins alongside bike pins. Tapping a café pin opens CafeDetailSheet. Get Directions navigates to NavStack/NavigateToPoi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock data → UI | Hardcoded café coordinates and hours render in MapView — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | MapScreen MOCK_CAFES | accept | Mock data only — no PII, no real café data, frontend-only |
| T-09-SC | Tampering | npm installs | accept | No new packages in Phase 9 — all libraries previously vetted |
</threat_model>

<verification>
After both tasks complete:
1. TypeScript: navigation.ts exports DiscoverStackParamList with 6 routes
2. TypeScript: AppTabParamList includes Discover tab; NavStackParamList includes NavigateToPoi
3. AppTabs.tsx registers Discover tab between Map and Account
4. MapScreen.tsx: 5 café Markers render in MapView using CafeMarker component
5. Tapping a café marker opens CafeDetailSheet (55% snap point)
6. CafeDetailSheet shows café name, hours, distance, and Get Directions button
7. Get Directions dismisses sheet and navigates to NavStack/NavigateToPoi
</verification>

<success_criteria>
- Discover tab visible as 2nd tab in app with compass icon (Map | Discover | Account)
- White café pins visible on MapScreen alongside Electric Green bike pins
- Café bottom sheet opens on pin tap with all required content fields
- Get Directions navigates to NavigateToPoi (stub screen) without crashing
- No TypeScript errors in modified files
</success_criteria>

<output>
Create `.planning/phases/09-discovery-and-content/09-01-SUMMARY.md` when done
</output>
