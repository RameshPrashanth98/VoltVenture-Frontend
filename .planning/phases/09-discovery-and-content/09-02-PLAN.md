---
phase: 09-discovery-and-content
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - VoltVenture/src/navigation/DiscoverNavigator.tsx
  - VoltVenture/src/screens/discover/DiscoverScreen.tsx
  - VoltVenture/src/screens/discover/CuratedRoutesScreen.tsx
  - VoltVenture/src/screens/discover/VipHubsScreen.tsx
autonomous: true
requirements:
  - DISC-05
  - DISC-06
  - DISC-07

must_haves:
  truths:
    - "Discover tab home screen shows two sections (EXPLORE and INFO) with menu rows navigating to child screens"
    - "Curated Routes screen shows a FlatList of 5 route cards with name, distance, difficulty badge, and highlight tags"
    - "Tapping a route card shows a Snackbar 'Route details coming soon' (no navigation)"
    - "VIP Hubs screen shows a fixed-height MapView (top 45%) with 5 hub pins and a FlatList (lower 55%) with inline-expanding hub cards"
    - "Tapping a hub card expands it to reveal description, amenities, hours, and Get Directions CTA navigating to NavigateToPoi"
    - "Tapping a hub map pin scrolls the FlatList to that hub's card and expands it"
  artifacts:
    - path: "VoltVenture/src/screens/discover/DiscoverScreen.tsx"
      provides: "Discover home screen with EXPLORE and INFO section rows"
      exports: ["default DiscoverScreen"]
    - path: "VoltVenture/src/screens/discover/CuratedRoutesScreen.tsx"
      provides: "FlatList of 5 mock route cards with difficulty badges and Snackbar"
      exports: ["default CuratedRoutesScreen"]
    - path: "VoltVenture/src/screens/discover/VipHubsScreen.tsx"
      provides: "Split map+list screen with inline-expanding hub cards"
      exports: ["default VipHubsScreen"]
  key_links:
    - from: "VoltVenture/src/screens/discover/DiscoverScreen.tsx"
      to: "VoltVenture/src/navigation/DiscoverNavigator.tsx"
      via: "navigation.navigate('CuratedRoutes'), navigation.navigate('VipHubs')"
      pattern: "navigate.*CuratedRoutes|navigate.*VipHubs"
    - from: "VoltVenture/src/screens/discover/VipHubsScreen.tsx"
      to: "NavStack/NavigateToPoi"
      via: "navigation.navigate in hub card Get Directions onPress"
      pattern: "NavigateToPoi"
    - from: "VoltVenture/src/navigation/DiscoverNavigator.tsx"
      to: "VoltVenture/src/screens/discover/DiscoverScreen.tsx"
      via: "Stack.Screen name=DiscoverMain component=DiscoverScreen"
      pattern: "DiscoverScreen"
---

<objective>
Build all three Discover tab screens: DiscoverScreen (home with menu rows), CuratedRoutesScreen (route card FlatList), and VipHubsScreen (split map+list with inline expand). Update DiscoverNavigator to replace stub placeholders with real screen imports.

Purpose: Delivers the core discovery UX. User can navigate to curated routes and VIP hubs from the Discover home, browse route cards, and explore VIP hub locations on a map with detailed inline information.

Output: Three fully functional Discover tab screens. DiscoverNavigator wired to real components. Hub Get Directions routes to NavigateToPoi (stub from Plan 09-01 will be replaced by Plan 09-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-discovery-and-content/09-CONTEXT.md
@.planning/phases/09-discovery-and-content/09-UI-SPEC.md
@.planning/phases/09-discovery-and-content/09-RESEARCH.md
@.planning/phases/09-discovery-and-content/09-PATTERNS.md
@.planning/phases/09-discovery-and-content/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DiscoverScreen + CuratedRoutesScreen + update DiscoverNavigator</name>

  <read_first>
    VoltVenture/src/screens/app/AccountScreen.tsx — read the menuRow, menuRowLeft, menuRowText, and title styles; read the TouchableOpacity + icon + label + chevron-right JSX pattern (exact copy target for DiscoverScreen rows)
    VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx — read sectionHeader style (lines 153-162: fontSize 12, fontWeight 600, uppercase, letterSpacing 0.8, DSColors.textSecondary) and custom header pattern (lines 35-45)
    VoltVenture/src/screens/app/RideHistoryScreen.tsx — read FlatList pattern: keyExtractor, renderItem, ItemSeparatorComponent, contentContainerStyle
    VoltVenture/src/navigation/DiscoverNavigator.tsx — read current file (has stub placeholder components from Plan 09-01); this file will be updated to use real screen imports
    VoltVenture/src/theme/theme.ts — read DSColors and DSTypography exports needed across both screens
  </read_first>

  <action>
    STEP 1 — Create VoltVenture/src/screens/discover/DiscoverScreen.tsx:

    Screen type: StackScreenProps<DiscoverStackParamList, 'DiscoverMain'>
    Root: SafeAreaView (edges=['bottom']) > ScrollView (or plain View — content fits without scroll)

    Screen title area: Text "Discover" using DSTypography.heading (20px, weight 600), DSColors.textPrimary, paddingHorizontal 24, paddingTop 24, paddingBottom 16

    Section "EXPLORE": Text label using overline treatment (fontSize 13 per UI-SPEC — NOT 12 from PATTERNS.md sectionHeader; UI-SPEC typography section states 13px for label size; fontWeight 600, uppercase, letterSpacing 0.8, DSColors.textSecondary, paddingHorizontal 24, paddingTop 16, paddingBottom 8)
    Rows in EXPLORE section:
    - "Curated Routes": icon map-route, navigates to 'CuratedRoutes'
    - "VIP Hubs": icon lightning-bolt-circle, navigates to 'VipHubs' — this row gets borderBottomWidth: 1 (last in section)

    Section "INFO": Text label same overline treatment, text "INFO"
    Rows in INFO section:
    - "Support & Help": icon help-circle-outline, navigates to 'Support'
    - "Privacy Policy": icon file-document-outline, navigates to 'PrivacyPolicy'
    - "Terms of Service": icon file-check-outline, navigates to 'TermsOfService' — this row gets borderBottomWidth: 1 (last in section)

    Each menu row: TouchableOpacity, activeOpacity 0.7, accessibilityRole="button", accessibilityLabel="{row label}"
    Row style (exact match to AccountScreen): flexDirection row, alignItems center, justifyContent space-between, paddingHorizontal 24, paddingVertical 16, backgroundColor DSColors.surface, borderTopWidth 1, borderColor DSColors.border
    Row left: View flexDirection row, alignItems center, gap 12 — MaterialCommunityIcons size 20 DSColors.textPrimary + Text 15px weight 400 DSColors.textPrimary
    Row right: MaterialCommunityIcons name="chevron-right" size 20 DSColors.textSecondary

    All styles: StyleSheet.create + DSColors only (no NativeWind)

    STEP 2 — Create VoltVenture/src/screens/discover/CuratedRoutesScreen.tsx:

    Screen type: StackScreenProps<DiscoverStackParamList, 'CuratedRoutes'>
    Snackbar state: useState<boolean>(false) for snackVisible

    Root: SafeAreaView (edges=['bottom'], flex 1, backgroundColor DSColors.background)

    Custom header (identical to VoltCoinsRewardsScreen header):
    - flexDirection row, alignItems center, justifyContent space-between
    - paddingHorizontal 24, paddingTop 16, paddingBottom 12
    - borderBottomWidth 1, borderColor DSColors.border, backgroundColor DSColors.background
    - Left: TouchableOpacity onPress={navigation.goBack} accessibilityRole="button" accessibilityLabel="Go back" — arrow-left icon size 24 DSColors.textPrimary
    - Center: Text "Curated Routes" fontSize 17, fontWeight 600, DSColors.textPrimary
    - Right: View width 40 (invisible spacer)

    Mock route data (inline const, Route type { id, name, distanceKm, difficulty: 'Easy' | 'Moderate' | 'Challenging', tags }):
    - r1: Canal Ring Classic, 12 km, Easy, tags: ['Waterfront', 'Historic']
    - r2: Vondelpark Loop, 8 km, Easy, tags: ['Parks', 'Family-friendly']
    - r3: Harbor Views Ride, 18 km, Moderate, tags: ['Waterfront', 'Scenic']
    - r4: Amstel Riverside Run, 22 km, Moderate, tags: ['Riverside', 'Local']
    - r5: Noord Cross, 28 km, Challenging, tags: ['Cross-river', 'Advanced']

    FlatList:
    - data={MOCK_ROUTES}, keyExtractor={item => item.id}
    - contentContainerStyle: paddingHorizontal 16, paddingTop 16, paddingBottom 32
    - ItemSeparatorComponent: View height 12 (transparent — per UI-SPEC, NOT border separator from RideHistory)
    - renderItem: TouchableOpacity onPress={() => setSnackVisible(true)} activeOpacity 0.8

    Route card (per UI-SPEC §3):
    - backgroundColor DSColors.surface, borderRadius 12, borderWidth 1, borderColor DSColors.border, padding 16, elevation 1
    - Row 1: Text route name (20px weight 600 DSColors.textPrimary) + difficulty badge (right-aligned)
    - Row 2: MaterialCommunityIcons name="map-marker-distance" size 14 DSColors.textSecondary + Text "{distanceKm} km" 15px DSColors.textSecondary
    - Row 3: React Native Paper Chip components for tags, compact mode, gap 8, marginTop 8 (textColor DSColors.textSecondary, borderColor DSColors.border, backgroundColor DSColors.surface)

    Difficulty badge colors (inline rgba per UI-SPEC §Color — not DSColors):
    - Easy: backgroundColor rgba(125,146,32,0.12), color DSColors.accent (#7D9220)
    - Moderate: backgroundColor rgba(255,152,0,0.12), color #E65100
    - Challenging: backgroundColor rgba(176,0,32,0.12), color DSColors.destructive
    Badge style: borderRadius 4, paddingHorizontal 8, paddingVertical 3, fontSize 13, fontWeight 600
    Badge accessibilityLabel="{difficulty} difficulty"

    Snackbar in Portal (per RESEARCH.md Pattern 9):
    Portal > Snackbar visible={snackVisible} onDismiss={() => setSnackVisible(false)} duration={2500}
    Message: "Route details coming soon"

    STEP 3 — Update VoltVenture/src/navigation/DiscoverNavigator.tsx:
    Replace stub placeholder imports with real imports of DiscoverScreen and CuratedRoutesScreen. Leave VipHubs, Support, PrivacyPolicy, TermsOfService stubs in place — they will be replaced by Plan 09-03. Update Stack.Screen name="DiscoverMain" component to DiscoverScreen, name="CuratedRoutes" component to CuratedRoutesScreen.
  </action>

  <verify>
    VoltVenture/src/screens/discover/DiscoverScreen.tsx contains "Curated Routes" and "VIP Hubs" and "Support & Help" and "Privacy Policy" and "Terms of Service"
    VoltVenture/src/screens/discover/CuratedRoutesScreen.tsx contains "Canal Ring Classic" and "MOCK_ROUTES" and "snackVisible" and "Route details coming soon"
    VoltVenture/src/navigation/DiscoverNavigator.tsx contains "DiscoverScreen" and "CuratedRoutesScreen" as imports (not placeholder stubs)
  </verify>

  <acceptance_criteria>
    - DiscoverScreen.tsx: "EXPLORE" section has exactly 2 rows (Curated Routes with map-route icon, VIP Hubs with lightning-bolt-circle icon); "INFO" section has exactly 3 rows (Support & Help, Privacy Policy, Terms of Service); last row in each section has borderBottomWidth: 1; all rows have accessibilityRole="button"
    - DiscoverScreen.tsx: Screen title uses DSTypography.heading (20px weight 600); section headers use 13px weight 600 uppercase letterSpacing 0.8 DSColors.textSecondary
    - CuratedRoutesScreen.tsx: MOCK_ROUTES has exactly 5 items; difficulty badge for 'Easy' uses rgba(125,146,32,0.12) background and DSColors.accent text; Chip components used for tags; Snackbar message is exactly "Route details coming soon"
    - CuratedRoutesScreen.tsx: ItemSeparatorComponent height is 12 (transparent, not a border separator)
    - CuratedRoutesScreen.tsx: Snackbar wrapped in Portal
    - DiscoverNavigator.tsx: Stack.Screen name="DiscoverMain" uses real DiscoverScreen component; Stack.Screen name="CuratedRoutes" uses real CuratedRoutesScreen component
  </acceptance_criteria>

  <done>DiscoverScreen and CuratedRoutesScreen created and wired into DiscoverNavigator. User can tap Discover tab and navigate to Curated Routes. Route card tap shows Snackbar.</done>
</task>

<task type="auto">
  <name>Task 2: VipHubsScreen + update DiscoverNavigator to wire VipHubs</name>

  <read_first>
    VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx — read for full-screen MapView pattern (non-interactive options: scrollEnabled={false}, zoomEnabled={false}, pitchEnabled={false}, rotateEnabled={false}) and the import of MapView, Marker from react-native-maps
    VoltVenture/src/screens/app/RideHistoryScreen.tsx — read FlatList pattern for getItemLayout approach, keyExtractor, renderItem signature
    VoltVenture/src/components/common/PrimaryButton.tsx — read props interface (label: string, onPress: () => void)
    VoltVenture/src/navigation/DiscoverNavigator.tsx — read current state (stubs or real imports from Task 1) before updating VipHubs entry
    VoltVenture/src/theme/theme.ts — read DSColors.primary, DSColors.textOnPrimary, DSColors.accent, DSColors.destructive for badge and marker colors
  </read_first>

  <action>
    Create VoltVenture/src/screens/discover/VipHubsScreen.tsx:

    Screen type: StackScreenProps<DiscoverStackParamList, 'VipHubs'>

    Top-level navigation: VipHubsScreen needs to navigate outside its DiscoverStack to NavStack/NavigateToPoi. Use useNavigation with type CompositeNavigationProp or cast via (navigation as any).navigate('NavStack', ...) — consistent with how other screens navigate cross-stack. Check how MapScreen navigates to NavStack for the exact pattern.

    Constants at module level:
    - const { height: SCREEN_HEIGHT } = Dimensions.get('window')
    - const MAP_HEIGHT = Math.round(SCREEN_HEIGHT * 0.45)
    - const ITEM_HEIGHT = 108 (collapsed card estimate for getItemLayout per RESEARCH.md Pattern 11)

    VipHub type (inline): { id: string; name: string; latitude: number; longitude: number; distanceKm: number; status: 'Available' | 'Full'; description: string; hours: string }

    MOCK_HUBS array (5 items per UI-SPEC §4):
    - h1: VoltHub Central Station, 52.3791, 4.9003, 1.2 km, Available, "Premium charging hub adjacent to Amsterdam Centraal.", "24/7"
    - h2: Dam Square VoltHub, 52.3732, 4.8932, 0.8 km, Available, "Covered hub in the heart of Amsterdam.", "06:00–24:00"
    - h3: Leidseplein VoltHub, 52.3639, 4.8830, 1.5 km, Full, "Popular entertainment district hub.", "24/7"
    - h4: Waterlooplein VoltHub, 52.3675, 4.9023, 0.4 km, Available, "Hub near the flea market and opera house.", "08:00–22:00"
    - h5: Vondelpark VoltHub, 52.3577, 4.8743, 2.1 km, Available, "Scenic park-side charging station.", "07:00–22:00"

    Refs and state:
    - mapRef = useRef<MapView>(null)
    - flatListRef = useRef<FlatList>(null)
    - expandedHubId = useState<string | null>(null)

    Layout: SafeAreaView (edges=['bottom']) flex 1, backgroundColor DSColors.background
    Structure: custom header + MapView (fixed height MAP_HEIGHT) + FlatList (flex 1)

    Custom header: same as CuratedRoutesScreen, title "VIP Hubs"

    MapView:
    - ref={mapRef}, style={{ height: MAP_HEIGHT }}
    - scrollEnabled={false}, zoomEnabled={false}, pitchEnabled={false}, rotateEnabled={false}
    - initialRegion: latitude 52.3676, longitude 4.9041, latitudeDelta 0.04, longitudeDelta 0.04
    - Markers for each hub: key={hub.id}, coordinate, onPress={() => handleHubMarkerPress(hub, index), tracksViewChanges={false}
    - Hub marker visual (inline View, no separate component): View width 36, height 36, borderRadius 18, backgroundColor DSColors.primary, borderWidth 2, borderColor DSColors.textOnPrimary, alignItems center, justifyContent center — icon: star-circle size 18 DSColors.textOnPrimary. No tail needed on hub markers (keep it simple — the map context makes them identifiable).

    handleHubMarkerPress(hub, index):
    - setExpandedHubId(hub.id)
    - flatListRef.current?.scrollToIndex({ index, animated: true })
    - mapRef.current?.animateToRegion({ latitude: hub.latitude, longitude: hub.longitude, latitudeDelta: 0.01, longitudeDelta: 0.01 }, 500)

    FlatList:
    - ref={flatListRef}, data={MOCK_HUBS}, keyExtractor={item => item.id}
    - style={{ flex: 1 }}, contentContainerStyle={{ paddingBottom: 24 }}
    - getItemLayout={(data, index) => ({ length: ITEM_HEIGHT, offset: ITEM_HEIGHT * index, index })}
    - renderItem: renderHub function
    - ItemSeparatorComponent: transparent View height 8

    renderHub({ item, index }):
    const isExpanded = expandedHubId === item.id
    TouchableOpacity onPress:
    - setExpandedHubId(isExpanded ? null : item.id)
    - mapRef.current?.animateToRegion({ latitude: item.latitude, longitude: item.longitude, latitudeDelta: 0.01, longitudeDelta: 0.01 }, 500)
    - if (!isExpanded): flatListRef.current?.scrollToIndex({ index, animated: true })
    - activeOpacity 0.8

    Card container: backgroundColor DSColors.surface, borderRadius 12, borderWidth 1, borderColor DSColors.border, padding 16, marginHorizontal 16, marginVertical 4

    Collapsed content (always visible):
    - Row 1: Text hub name (17px weight 600 DSColors.textPrimary) + VIP badge (backgroundColor DSColors.primary, color DSColors.textOnPrimary, borderRadius 4, paddingHorizontal 6, paddingVertical 2, fontSize 13, fontWeight 600 — text "VIP")
    - Row 2: Text "{item.distanceKm.toFixed(1)} km" (map-marker icon size 14 DSColors.textSecondary + 15px DSColors.textSecondary) + status badge right-aligned
    Status badge Available: rgba(125,146,32,0.12) bg + DSColors.accent text
    Status badge Full: rgba(176,0,32,0.12) bg + DSColors.destructive text
    Status badge accessibilityLabel="Status: {item.status}"

    Expanded content ({isExpanded && ...}), marginTop 12 top separator:
    - Description: Text 15px weight 400 DSColors.textPrimary lineHeight 22, marginBottom 8
    - Amenities (3 rows with check-circle icon size 16 DSColors.accent):
      "Fast charging (up to 80% in 45 min)"
      "Covered, secure parking"
      "24h access — no closing time"
    - Hours row: clock-outline icon size 14 DSColors.textSecondary + Text item.hours 15px DSColors.textSecondary, marginBottom 12
    - PrimaryButton label="Get Directions" onPress: navigate to NavStack/NavigateToPoi with { name: item.name, location: { latitude: item.latitude, longitude: item.longitude } }, marginTop 4

    All styles: StyleSheet.create + DSColors only. No NativeWind.

    After creating VipHubsScreen, update DiscoverNavigator.tsx to replace the VipHubs stub with real VipHubsScreen import.
  </action>

  <verify>
    VoltVenture/src/screens/discover/VipHubsScreen.tsx contains "MOCK_HUBS" and "expandedHubId" and "flatListRef" and "mapRef" and "getItemLayout" and "NavigateToPoi"
    VoltVenture/src/screens/discover/VipHubsScreen.tsx contains "VoltHub Central Station" and "Leidseplein VoltHub"
    VoltVenture/src/navigation/DiscoverNavigator.tsx contains "VipHubsScreen" as a real import
  </verify>

  <acceptance_criteria>
    - VipHubsScreen.tsx: MapView has height MAP_HEIGHT (computed from Dimensions.get('window').height × 0.45); NOT StyleSheet.absoluteFill and NOT flex: 1
    - VipHubsScreen.tsx: FlatList has style={{ flex: 1 }} to fill remaining vertical space below MapView
    - VipHubsScreen.tsx: getItemLayout implemented with ITEM_HEIGHT constant (prevents scrollToIndex warning)
    - VipHubsScreen.tsx: MOCK_HUBS has exactly 5 items; h3 (Leidseplein) status is 'Full'; all others 'Available'
    - VipHubsScreen.tsx: VIP badge uses DSColors.primary background and DSColors.textOnPrimary text (NOT DSColors.accent text on primary background — would violate contrast rule)
    - VipHubsScreen.tsx: Hub markers use DSColors.primary background, DSColors.textOnPrimary border and icon, star-circle icon, tracksViewChanges={false}
    - VipHubsScreen.tsx: Expanded content shows exactly 3 amenity rows with check-circle icon and DSColors.accent color
    - VipHubsScreen.tsx: Get Directions navigates cross-stack to NavStack/NavigateToPoi (not within DiscoverStack)
    - DiscoverNavigator.tsx: Stack.Screen name="VipHubs" uses real VipHubsScreen component (not stub)
  </acceptance_criteria>

  <done>VipHubsScreen created with split map+list layout. Hub cards expand inline with description, amenities, hours, and Get Directions. Map pin tap scrolls list and expands card. List card tap focuses map pin. DiscoverNavigator updated to wire real VipHubsScreen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock data → MapView | Hardcoded hub coordinates render as Markers — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Information Disclosure | VipHubsScreen MOCK_HUBS | accept | Mock data only — no PII, no real hub locations, frontend-only |
| T-09-SC | Tampering | npm installs | accept | No new packages — all libraries from prior phases |
</threat_model>

<verification>
After both tasks complete:
1. Tapping Discover tab navigates to DiscoverScreen with "Discover" title, EXPLORE and INFO sections
2. Tapping "Curated Routes" navigates to CuratedRoutesScreen with 5 route cards
3. Tapping a route card shows Snackbar "Route details coming soon"
4. Difficulty badges display correct colors (green Easy, amber Moderate, red Challenging)
5. Tapping "VIP Hubs" navigates to VipHubsScreen with split layout (map top, list bottom)
6. 5 VIP hub markers visible on VipHubs map with star-circle icon
7. Tapping a hub card expands inline to show description, 3 amenities, hours, Get Directions
8. Tapping another hub card collapses previous and expands new one (or both can stay expanded — uncontrolled OK)
9. Get Directions in expanded hub card navigates to NavigateToPoi stub without crash
10. Tapping a map hub pin scrolls and expands the corresponding list card
</verification>

<success_criteria>
- DiscoverScreen shows correct menu rows for both EXPLORE and INFO sections
- CuratedRoutesScreen FlatList renders 5 route cards with difficulty-coded badges
- VipHubsScreen renders split layout: 45% map + 55% list (FlatList flex 1)
- Hub cards expand/collapse inline without navigation
- Cross-stack navigation from VipHubs Get Directions to NavStack/NavigateToPoi works without crash
- No TypeScript errors in new or modified files
</success_criteria>

<output>
Create `.planning/phases/09-discovery-and-content/09-02-SUMMARY.md` when done
</output>
