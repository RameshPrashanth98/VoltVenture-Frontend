---
phase: 09-discovery-and-content
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx
  - VoltVenture/src/screens/discover/SupportScreen.tsx
  - VoltVenture/src/screens/discover/PrivacyPolicyScreen.tsx
  - VoltVenture/src/screens/discover/TermsOfServiceScreen.tsx
  - VoltVenture/src/navigation/DiscoverNavigator.tsx
autonomous: true
requirements:
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "NavigateToPoiScreen shows a full-screen map with Electric Green polyline, ETA card with destination name and walk time, and back button — no Turn-by-Turn button"
    - "SupportScreen shows a FAQ accordion with 3 sections and 8 items, plus Contact Support Snackbar"
    - "PrivacyPolicyScreen shows 4 GDPR sections in a ScrollView with all content readable"
    - "TermsOfServiceScreen shows 4 terms sections in a ScrollView with all content readable"
    - "All four screens accessible: NavigateToPoi via Get Directions (café and hub cards); Support/Privacy/Terms via DiscoverScreen INFO section"
  artifacts:
    - path: "VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx"
      provides: "Full-screen navigation screen to generic POI (mirrors NavigateToBike minus Turn-by-Turn button)"
      exports: ["default NavigateToPoiScreen"]
    - path: "VoltVenture/src/screens/discover/SupportScreen.tsx"
      provides: "FAQ accordion (List.Accordion) + Contact Support Snackbar"
      exports: ["default SupportScreen"]
    - path: "VoltVenture/src/screens/discover/PrivacyPolicyScreen.tsx"
      provides: "GDPR privacy policy in 4 sections"
      exports: ["default PrivacyPolicyScreen"]
    - path: "VoltVenture/src/screens/discover/TermsOfServiceScreen.tsx"
      provides: "Terms of service in 4 sections"
      exports: ["default TermsOfServiceScreen"]
  key_links:
    - from: "VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx"
      to: "NavStackParamList NavigateToPoi"
      via: "StackScreenProps<NavStackParamList, 'NavigateToPoi'>"
      pattern: "NavigateToPoi"
    - from: "VoltVenture/src/navigation/DiscoverNavigator.tsx"
      to: "VoltVenture/src/screens/discover/SupportScreen.tsx"
      via: "Stack.Screen name=Support component=SupportScreen"
      pattern: "SupportScreen"
---

<objective>
Complete Phase 9 with the NavigateToPoiScreen (replaces stub from Plan 09-01) and the three Info screens (SupportScreen with FAQ accordion, PrivacyPolicyScreen, TermsOfServiceScreen). Update DiscoverNavigator to wire in all three Info screens.

Purpose: NavigateToPoiScreen enables the full café and hub Get Directions flow. The Info screens complete the DiscoverScreen INFO section, giving users access to support FAQ and legal content in-app.

Output: NavigateToPoiScreen fully implemented. SupportScreen, PrivacyPolicyScreen, TermsOfServiceScreen created. DiscoverNavigator fully wired with real screen components (no more stubs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-discovery-and-content/09-CONTEXT.md
@.planning/phases/09-discovery-and-content/09-UI-SPEC.md
@.planning/phases/09-discovery-and-content/09-RESEARCH.md
@.planning/phases/09-discovery-and-content/09-PATTERNS.md
@.planning/phases/09-discovery-and-content/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavigateToPoiScreen (replace stub with full implementation)</name>

  <read_first>
    VoltVenture/src/screens/navigation/NavigateToBikeScreen.tsx — read full file; NavigateToPoiScreen is a line-by-line adaptation of this file. Note: exact line numbers for haversineKm function, USER_LAT/USER_LON constants, Props type, param destructure, distanceKm/etaMin/distanceM computations, mapView initialRegion, Marker coordinate, Polyline coordinates, overlay card content, "View Turn-by-Turn" button (to OMIT), StyleSheet
    VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx — read current stub (created in Plan 09-01: just renders a View). This file will be REPLACED with the full implementation.
    VoltVenture/src/types/navigation.ts — verify NavStackParamList.NavigateToPoi type is: { name: string; location: { latitude: number; longitude: number } }
    VoltVenture/src/theme/theme.ts — read DSColors.primary (for polyline strokeColor) and DSTypography.headingMd (for ETA card destination name style)
  </read_first>

  <action>
    Replace the stub at VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx with the full implementation.

    This screen is a line-by-line copy of NavigateToBikeScreen.tsx with exactly these changes:

    CHANGE 1 — Props type (change screen name in generic):
    FROM: type Props = StackScreenProps<NavStackParamList, 'NavigateToBike'>
    TO:   type Props = StackScreenProps<NavStackParamList, 'NavigateToPoi'>

    CHANGE 2 — Function name and param destructure:
    FROM: export default function NavigateToBikeScreen({ route, navigation }: Props) { const { bike } = route.params;
    TO:   export default function NavigateToPoiScreen({ route, navigation }: Props) { const { name, location } = route.params;

    CHANGE 3 — Distance computation (change coordinate source):
    FROM: const distanceKm = haversineKm(USER_LAT, USER_LON, bike.latitude, bike.longitude);
    TO:   const distanceKm = haversineKm(USER_LAT, USER_LON, location.latitude, location.longitude);
    (etaMin and distanceM formulas remain identical)

    CHANGE 4 — MapView initialRegion (change coordinate refs):
    FROM: latitude: (USER_LAT + bike.latitude) / 2, longitude: (USER_LON + bike.longitude) / 2
    TO:   latitude: (USER_LAT + location.latitude) / 2, longitude: (USER_LON + location.longitude) / 2

    CHANGE 5 — Destination Marker coordinate:
    FROM: coordinate={{ latitude: bike.latitude, longitude: bike.longitude }}
    TO:   coordinate={{ latitude: location.latitude, longitude: location.longitude }}

    CHANGE 6 — Polyline endpoint coordinate:
    FROM: { latitude: bike.latitude, longitude: bike.longitude }
    TO:   { latitude: location.latitude, longitude: location.longitude }

    CHANGE 7 — ETA card destination name display:
    FROM: <Text style={styles.bikeName}>{bike.name}</Text>
    TO:   <Text style={styles.poiName}>{name}</Text>
    (rename the style key bikeName → poiName; same style values)

    CHANGE 8 — Add back button INSIDE the overlay card (new — NavigateToPoi specific per UI-SPEC §6):
    Add a TouchableOpacity at the TOP of the overlay card content (before the destination name Text):
    TouchableOpacity onPress={() => navigation.goBack()} style={styles.backButton} accessibilityRole="button" accessibilityLabel="Go back"
    Child: MaterialCommunityIcons name="arrow-left" size={20} color="#FFFFFF"
    Back button style: { marginBottom: 8, alignSelf: 'flex-start' }

    CHANGE 9 — OMIT the "View Turn-by-Turn" button entirely:
    NavigateToBikeScreen has a TouchableOpacity button that navigates to WalkingDirections. NavigateToPoiScreen does NOT have this button — remove it completely. No replacement.

    CHANGE 10 — haversineKm inline copy comment:
    Keep the comment "Inline copy from MapScreen.tsx — haversineKm is not exported" verbatim.
    haversineKm function body is identical — copy verbatim.
    USER_LAT = 52.3676, USER_LON = 4.9041 — identical.

    Add backButton to StyleSheet (same location as other style additions):
    backButton: { marginBottom: 8, alignSelf: 'flex-start' }

    All other code, imports, StyleSheet entries: copy verbatim from NavigateToBikeScreen.tsx (no other changes).
    All styles: StyleSheet.create + DSColors only.
  </action>

  <verify>
    VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx contains "NavigateToPoi" in the Props type generic
    VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx contains "location.latitude" and "location.longitude"
    VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx contains "navigation.goBack" (back button)
    VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx does NOT contain "WalkingDirections" (Turn-by-Turn button must be absent)
    VoltVenture/src/screens/navigation/NavigateToPoiScreen.tsx contains "haversineKm" (inline copy present)
  </verify>

  <acceptance_criteria>
    - NavigateToPoiScreen.tsx: Props type uses NavStackParamList and 'NavigateToPoi' (NOT 'NavigateToBike')
    - NavigateToPoiScreen.tsx: param destructure is { name, location } — NOT { bike }
    - NavigateToPoiScreen.tsx: distanceKm uses location.latitude and location.longitude (NOT bike.latitude/bike.longitude)
    - NavigateToPoiScreen.tsx: ETA card shows {name} as destination text (NOT bike.name)
    - NavigateToPoiScreen.tsx: back button (arrow-left, white) appears inside overlay card with navigation.goBack()
    - NavigateToPoiScreen.tsx: NO "View Turn-by-Turn" TouchableOpacity or navigation to WalkingDirections
    - NavigateToPoiScreen.tsx: Polyline strokeColor is DSColors.primary, strokeWidth 4
    - NavigateToPoiScreen.tsx: haversineKm inline function present with "not exported" comment
    - NavigateToPoiScreen.tsx: ETA format matches "{etaMin} min walk — {distanceM} m" (same formula as NavigateToBike)
  </acceptance_criteria>

  <done>NavigateToPoiScreen fully implemented. Café and hub Get Directions flows navigate here, displaying full-screen map with polyline, ETA card with destination name and walk time, and back button.</done>
</task>

<task type="auto">
  <name>Task 2: SupportScreen + PrivacyPolicyScreen + TermsOfServiceScreen + wire DiscoverNavigator</name>

  <read_first>
    VoltVenture/src/screens/app/VoltCoinsRewardsScreen.tsx — read for: custom header pattern (lines 35-45), Snackbar + Portal pattern (lines 30, 104-112), sectionHeader overline style; this is the primary structural analog for all three screens
    VoltVenture/src/navigation/DiscoverNavigator.tsx — read current state (has stubs for Support, PrivacyPolicy, TermsOfService from Plan 09-01); will be fully updated to wire real screens
    VoltVenture/src/theme/theme.ts — read DSColors.border, DSColors.textPrimary, DSColors.textSecondary, DSColors.surface, DSColors.background, DSColors.accent
  </read_first>

  <action>
    STEP 1 — Create VoltVenture/src/screens/discover/SupportScreen.tsx:

    Screen type: StackScreenProps<DiscoverStackParamList, 'Support'>
    State: useState<boolean>(false) for snackVisible

    Root: SafeAreaView (edges=['bottom'], flex 1, backgroundColor DSColors.background)

    Custom header (identical to CuratedRoutesScreen header): title "Support & Help"

    Body: ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 32 }}

    List.Section + List.Accordion structure (from RESEARCH.md Pattern 12 — first use of List.Accordion in codebase):
    Import: List, Divider from 'react-native-paper'

    Section 1 — "Rides & Billing" (List.Subheader with overline style):
    Item 1: title "How do I start a ride?"
      answer: "Tap any green bike pin on the map, book the bike, and scan the QR code on the bike's handlebars. The ride starts automatically."
    Item 2: title "How am I charged?"
      answer: "You're charged per minute from the moment the bike unlocks. The session cost is shown in real-time on the Active Ride screen."
    Item 3: title "How do I end a ride?"
      answer: "Tap 'End Ride' on the Active Ride screen. Park the bike in a designated VoltVenture zone and confirm. Payment is processed automatically."
    Divider after each accordion item (backgroundColor DSColors.border)

    Section 2 — "Account":
    Item 1: title "How do I reset my password?"
      answer: "On the Login screen, tap 'Forgot password?' and enter your email. You'll receive a reset link within a few minutes."
    Item 2: title "How do I add a payment method?"
      answer: "Go to Account → Payment Methods → Add Payment Method. Enter your card details and tap Save."
    Item 3: title "How do I view my ride history?"
      answer: "Go to Account → Ride History. All past rides with cost, duration, and distance are listed there."
    Divider after each item

    Section 3 — "Bikes & Safety":
    Item 1: title "What if the bike doesn't unlock?"
      answer: "Check your Bluetooth and internet connection, then try again. If the issue persists, tap 'Cancel Booking' and contact support."
    Item 2: title "What should I do in an emergency?"
      answer: "Call local emergency services immediately. You can also contact VoltVenture support via the Contact Support button below."
    Divider after each item

    List.Accordion usage pattern (uncontrolled — no expanded prop needed):
    List.Accordion title={question} titleStyle={styles.accordionTitle}
    Child: List.Item description={answer} descriptionStyle={styles.accordionBody} descriptionNumberOfLines={0}
    CRITICAL: descriptionNumberOfLines={0} is REQUIRED — default truncates at 2 lines per RESEARCH.md Pitfall 7

    List.Subheader style (overline treatment — fontSize 13, fontWeight 600, DSColors.textSecondary, uppercase, letterSpacing 0.8)
    accordionTitle style: fontSize 15, fontWeight 600, DSColors.textPrimary
    accordionBody style: fontSize 15, DSColors.textSecondary, lineHeight 22

    After ScrollView, "Contact Support" button:
    PrimaryButton label="Contact Support" onPress={() => setSnackVisible(true)} — marginHorizontal 24, marginTop 24, marginBottom 16

    Portal > Snackbar visible={snackVisible} onDismiss={() => setSnackVisible(false)} duration={2500}
    Message: "Support chat coming soon"

    All styles: StyleSheet.create + DSColors only.

    ---

    STEP 2 — Create VoltVenture/src/screens/discover/PrivacyPolicyScreen.tsx:

    Screen type: StackScreenProps<DiscoverStackParamList, 'PrivacyPolicy'>

    Root: SafeAreaView (edges=['bottom'], flex 1, backgroundColor DSColors.background)

    Custom header: title "Privacy Policy"

    Body: ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingHorizontal: 24, paddingTop: 24, paddingBottom: 64 }}

    4 sections (per UI-SPEC §8 — use exact copy text):

    Section 1 — "Data We Collect" (Text sectionTitle style):
    Paragraph 1: "VoltVenture collects information you provide when creating an account (name, email address), payment details processed securely through our payment provider, and location data while the app is in use to display nearby bikes and calculate ride distances."
    Paragraph 2: "We also collect device information (device model, OS version) and usage data (screens viewed, session duration) to improve app performance and identify issues."

    Section 2 — "How We Use Your Data":
    Paragraph 1: "Your data is used to provide and improve the VoltVenture service, process payments, display nearby bikes on the map, calculate ride costs and distances, and send optional promotional communications (if you have opted in via Notification Preferences)."
    Paragraph 2: "We do not sell your personal data to third parties. Anonymised aggregate data may be shared with municipal partners to improve cycling infrastructure planning."

    Section 3 — "Your Rights (GDPR)":
    Paragraph 1: "Under the General Data Protection Regulation (GDPR), you have the right to access, correct, or delete your personal data at any time. You may also restrict or object to certain processing activities and request data portability."
    Paragraph 2: "To exercise these rights, contact our Data Protection Officer at privacy@voltventure.app. We will respond within 30 days."

    Section 4 — "Data Retention":
    Paragraph 1: "Account data is retained for as long as your account is active. Ride history is retained for 24 months for billing and dispute purposes. If you delete your account, personal data is removed within 30 days, except where retention is required by law (e.g., financial records retained for 7 years per EU accounting regulations)."

    sectionTitle style: fontSize 17 (DSTypography.headingMd), fontWeight 600, DSColors.textPrimary, marginBottom 8, marginTop 24 (first section: marginTop 0)
    paragraph style: fontSize 15, fontWeight 400, DSColors.textPrimary, lineHeight 22, marginBottom 12

    All styles: StyleSheet.create + DSColors only.

    ---

    STEP 3 — Create VoltVenture/src/screens/discover/TermsOfServiceScreen.tsx:

    Identical structure to PrivacyPolicyScreen. Changes:
    - Screen type: StackScreenProps<DiscoverStackParamList, 'TermsOfService'>
    - Custom header title: "Terms of Service"
    - 4 sections (per UI-SPEC §9):

    Section 1 — "Use of the Service":
    "VoltVenture provides electric bike rental services through this mobile application. By creating an account and using the app, you agree to these Terms of Service. You must be at least 18 years of age to register and use the service."

    Section 2 — "Bookings & Payments":
    "Bookings are confirmed in the app and are subject to bike availability. Rides are charged per minute from the moment the bike unlocks. You authorise VoltVenture to charge your registered payment method at the end of each ride. All prices include applicable VAT."

    Section 3 — "User Responsibilities":
    "You are responsible for riding safely and in accordance with local traffic laws. You must wear a helmet where required by law. Bikes must be parked only in designated VoltVenture zones. You are liable for any damage to the bike caused by misuse or negligence."

    Section 4 — "Limitation of Liability":
    "VoltVenture is not liable for injuries, losses, or damages arising from the use of the service beyond the extent permitted by applicable law. We maintain public liability insurance for all rented bikes. If you experience a safety issue, contact us immediately at support@voltventure.app."

    Same sectionTitle and paragraph styles as PrivacyPolicyScreen.
    All styles: StyleSheet.create + DSColors only.

    ---

    STEP 4 — Update DiscoverNavigator.tsx to replace remaining stubs:
    Read the current DiscoverNavigator.tsx (should have Support, PrivacyPolicy, TermsOfService as stubs after Plans 09-01 and 09-02). Replace stub references with real imports:
    - Import SupportScreen from '../screens/discover/SupportScreen'
    - Import PrivacyPolicyScreen from '../screens/discover/PrivacyPolicyScreen'
    - Import TermsOfServiceScreen from '../screens/discover/TermsOfServiceScreen'
    Update Stack.Screen name="Support" component to SupportScreen
    Update Stack.Screen name="PrivacyPolicy" component to PrivacyPolicyScreen
    Update Stack.Screen name="TermsOfService" component to TermsOfServiceScreen
    Remove all stub placeholder component declarations if any remain.
    All 6 screens (DiscoverMain, CuratedRoutes, VipHubs, Support, PrivacyPolicy, TermsOfService) should now use real components.
  </action>

  <verify>
    VoltVenture/src/screens/discover/SupportScreen.tsx contains "List.Accordion" and "descriptionNumberOfLines" and "Support chat coming soon" and "Portal"
    VoltVenture/src/screens/discover/SupportScreen.tsx contains "How do I start a ride" and "What should I do in an emergency"
    VoltVenture/src/screens/discover/PrivacyPolicyScreen.tsx contains "Data We Collect" and "GDPR" and "privacy@voltventure.app"
    VoltVenture/src/screens/discover/TermsOfServiceScreen.tsx contains "Use of the Service" and "Limitation of Liability" and "support@voltventure.app"
    VoltVenture/src/navigation/DiscoverNavigator.tsx contains "SupportScreen" and "PrivacyPolicyScreen" and "TermsOfServiceScreen" as real imports (not stubs)
  </verify>

  <acceptance_criteria>
    - SupportScreen.tsx: uses List.Accordion (not custom TouchableOpacity expand/collapse); has 3 List.Section blocks; total FAQ items = 8 (3 + 3 + 2); every List.Item has descriptionNumberOfLines={0}; Snackbar message is exactly "Support chat coming soon"; Snackbar wrapped in Portal
    - SupportScreen.tsx: List.Subheader styled with 13px, weight 600, DSColors.textSecondary, uppercase, letterSpacing 0.8
    - SupportScreen.tsx: "Contact Support" button at bottom (PrimaryButton component, NOT a plain TouchableOpacity)
    - PrivacyPolicyScreen.tsx: exactly 4 section headers ("Data We Collect", "How We Use Your Data", "Your Rights (GDPR)", "Data Retention"); paragraph text uses DSColors.textPrimary (NOT textSecondary — legal text should be readable); ScrollView contentContainerStyle paddingBottom is 64 (per UI-SPEC 3xl)
    - TermsOfServiceScreen.tsx: exactly 4 section headers ("Use of the Service", "Bookings & Payments", "User Responsibilities", "Limitation of Liability"); same style pattern as PrivacyPolicyScreen; contains "support@voltventure.app"
    - DiscoverNavigator.tsx: ALL 6 Stack.Screen entries use real imported components (no stub/placeholder components); no inline placeholder component declarations remain
  </acceptance_criteria>

  <done>NavigateToPoiScreen fully implemented, replacing the Plan 09-01 stub. SupportScreen FAQ accordion works with 3 sections and 8 items. PrivacyPolicyScreen and TermsOfServiceScreen display 4 legal sections each. DiscoverNavigator fully wired with all real screen imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hardcoded legal text → ScrollView | Static content — no user input, no external fetch |
| FAQ content → List.Accordion | Static hardcoded strings — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | Legal screen content | accept | Hardcoded mock content — no real legal obligations, placeholder text only |
| T-09-SC | Tampering | npm installs | accept | No new packages — all libraries from prior phases |
</threat_model>

<verification>
After both tasks complete:
1. NavigateToPoiScreen: full-screen map with Electric Green polyline renders for POI destination
2. NavigateToPoiScreen: ETA card shows destination name and "{N} min walk — {M} m"
3. NavigateToPoiScreen: back button (arrow-left, white) inside ETA card, no Turn-by-Turn button
4. End-to-end: tap café pin → CafeDetailSheet → Get Directions → NavigateToPoiScreen with café name shown
5. End-to-end: expand VIP hub card → Get Directions → NavigateToPoiScreen with hub name shown
6. SupportScreen: all 8 FAQ items expand/collapse via List.Accordion with full text visible
7. Contact Support button shows Snackbar "Support chat coming soon"
8. PrivacyPolicyScreen: all 4 sections readable; scrollable to bottom
9. TermsOfServiceScreen: all 4 sections readable; scrollable to bottom
10. DiscoverNavigator has no stub placeholder components — all 6 screens are real implementations
</verification>

<success_criteria>
- NavigateToPoiScreen accepts { name, location } params and displays them correctly — no reference to bike type
- SupportScreen List.Accordion shows full FAQ answer text (descriptionNumberOfLines={0} in effect)
- Legal screens contain all 4 sections per UI-SPEC with exact copy text
- DiscoverNavigator fully resolved — no placeholder components in any Stack.Screen
- No TypeScript errors in new or modified files
- All Phase 9 requirement IDs covered: DISC-05 (Plan 01+02), DISC-06 (Plan 02), DISC-07 (Plan 02), CONT-01 (Plan 01), CONT-02 (Plan 03), CONT-03 (Plan 03)
</success_criteria>

<output>
Create `.planning/phases/09-discovery-and-content/09-03-SUMMARY.md` when done
</output>
