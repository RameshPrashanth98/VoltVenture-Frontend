---
context: phase
phase: 10-emulator-setup-smoke-test
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-14T00:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Emulator-first — `npx expo run:android` fails if emulator is not running. Always start emulator and confirm `adb devices` shows `device` first.
- [ ] CONSTRAINT: Build from C:\vv — project files were copied to C:\vv to work around CMake path length issues. Always run `cd /c/vv/VoltVenture && npx expo run:android`, never from D:\.
- [ ] CONSTRAINT: gradle.properties is at C:\vv, not gitignored D:\ copy — verify C:\vv\VoltVenture\android\gradle.properties has newArchEnabled=false and -Xmx2g before building.
- [ ] CONSTRAINT: Close Android Studio before building — the emulator + AS + Gradle daemon + multiple clang++ processes exhaust 15 GB RAM. Start emulator standalone, close AS.
- [ ] CONSTRAINT: cmake_build_parallel_level=1 — set CMAKE_BUILD_PARALLEL_LEVEL=1 to prevent clang++ OOM during worklets/reanimated native compilation.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Build from D:\ | CMake path length limit (250 chars) — reanimated object paths exceed limit at D:\ long path | blocking | Always build from C:\vv |
| Android Studio open during build | 15 GB RAM exhausted: AS + emulator + Gradle(2g) + parallel clang++ | blocking | Close AS, keep emulator running standalone |
| ninja parallel clang++ jobs | Each C++20 file (Folly/JSI/Hermes headers) uses 500MB–1GB per clang++ process | blocking | Set CMAKE_BUILD_PARALLEL_LEVEL=1 |
| Stale .cxx cache from D:\ | Gradle build cache restores old D:\ paths; cmake regenerates at D:\ | blocking | If D:\ paths appear, delete C:\vv node_modules\react-native-reanimated\android\.cxx and react-native-worklets\android\.cxx then wipe D:\gradle\caches\build-cache-1 |

<current_state>
Phase 10, Plans 10.1 and 10.2 are complete. Plan 10.3 (build + smoke test) is in progress — build has NOT yet succeeded.

**Session 2026-09-14 summary:**
- README.md updated: expanded features (all 9 phases), MapLibre v11 tech stack, all 13 phases in roadmap, Windows build note
- 3 commits pushed to GitHub (origin/main is now up to date: 2d348a6)
- User confirmed intent to do local build (option 1 — close AS + CMAKE_BUILD_PARALLEL_LEVEL=1)
- Build was NOT attempted this session — user paused before running it

**Build environment status (unchanged from 2026-08-25):**
- Project files COPIED to C:\vv (real directory, not junction). Original files remain at D:\.
- android/gradle.properties at C:\vv: -Xmx2g, newArchEnabled=false, JAVA_TOOL_OPTIONS in jvmargs
- Gradle build cache (D:\gradle\caches\build-cache-1) was wiped in 2026-08-25 session
- android/app/.cxx and android/app/build deleted (were stale with D:\ paths) in 2026-08-25 session
- node_modules/react-native-reanimated/android/.cxx and react-native-worklets/android/.cxx — may have been recreated; check before building

**Last build result (2026-08-25):**
- 10 min run, failed at `:react-native-worklets:buildCMakeDebug[x86_64][worklets]`
- LLVM ERROR: out of memory in clang++ during worklets C++ compilation
- Fix confirmed: close Android Studio + CMAKE_BUILD_PARALLEL_LEVEL=1
</current_state>

<completed_work>

- Plan 10.1: MapLibre dep swap + app configuration (commit: 96c3648)
- Plan 10.2: 7 screen migrations (commits: a4ba80c, bda4705, 4647bce, 056876a, 735509f, d27387e, 41e1cd5)
- Plan 10.3 build attempts (2026-08-25 session):
  - Identified CMake path length blocker (D:\ path > 250 chars for reanimated) ✅ diagnosed
  - Tried junction C:\vv → D:\ — failed (CMake resolves real path) ❌
  - Copied project files to C:\vv (real directory) ✅
  - Cleared stale D:\ CMake caches from C:\vv copy ✅
  - Cleared Gradle build cache (D:\gradle\caches\build-cache-1) ✅
  - Cleared android/app/.cxx and android/app/build (stale D:\ paths in CMake API replies) ✅
  - Resolved JDK 25 restricted method warning via JAVA_TOOL_OPTIONS + gradle.properties ✅
  - Reduced JVM heap -Xmx4g → -Xmx2g to prevent native OOM in Gradle daemon ✅
  - Identified worklets OOM as final blocker ✅ diagnosed
- README.md updated and pushed to GitHub (commit: 2d348a6) ✅
</completed_work>

<remaining_work>

- Plan 10.3: Build, Smoke Test & VERIFICATION.md (SETUP-01, SETUP-02)
  1. Close Android Studio (keep emulator running standalone via emulator process)
  2. Confirm `adb devices` shows `device`
  3. Verify C:\vv\VoltVenture\android\gradle.properties: -Xmx2g, newArchEnabled=false
  4. Build with parallel limit:
     ```
     cd /c/vv/VoltVenture && CMAKE_BUILD_PARALLEL_LEVEL=1 JAVA_TOOL_OPTIONS="--enable-native-access=ALL-UNNAMED" npx expo run:android
     ```
  5. If worklets still OOM: close browser/other apps, wait for emulator to idle, retry
  6. Smoke test SETUP-01: launch, splash, auth screen, no Metro errors, no runtime exceptions
  7. Smoke test SETUP-02: log in → Map tab, Discover tab, Account tab, tab switching
  8. Write `.planning/phases/10-emulator-setup-smoke-test/VERIFICATION.md` with results
  9. Commit VERIFICATION.md and update STATE.md — Phase 10 complete if SETUP-01 + SETUP-02 PASS

- Phases 11–13: UAT walkthroughs (collaborative — user runs app, reports results, AI logs + fixes)
</remaining_work>

<decisions_made>

- Project copied to C:\vv (real, not junction) to get short CMake paths — user OK with temp storage use
- C:\vv is NOT permanent — user has limited C: storage; build from C:\vv only during UAT
- gradle.properties at C:\vv: -Xmx2g (reduced from 4g to prevent JVM native OOM during dexing)
- CMAKE_BUILD_PARALLEL_LEVEL=1 — required to prevent clang++ OOM on 15 GB machine with AS + emulator
- JAVA_TOOL_OPTIONS="--enable-native-access=ALL-UNNAMED" — suppresses JDK 25 warning in all subprocess JVMs
- android/gradle.properties: --enable-native-access=ALL-UNNAMED already in org.gradle.jvmargs (covers Gradle daemon itself)
- GRADLE_USER_HOME=D:\gradle — confirmed; build cache at D:\gradle\caches\build-cache-1
- D:\ module paths in autolinking: expected behavior (source files still at D:\); does not block build
- Long-term fix recommended: enable Windows long paths via registry (admin required) to build from D:\
- Phases 11–13 UAT: collaborative (user taps through app, AI logs pass/fail and fixes failures)
- README.md: no mention of AI tooling in public-facing docs
</decisions_made>

<blockers>

- **Active blocker: worklets clang++ OOM** — react-native-worklets C++20 compilation exhausts RAM when Android Studio is open. Fix: close AS + CMAKE_BUILD_PARALLEL_LEVEL=1.
- **Storage concern**: C:\vv is a temporary copy (~1.2 GB); user has limited C: drive space. After UAT, files at C:\vv can be deleted. Long-term: enable Windows long paths to build from D:\.
</blockers>

<context>
All build environment setup work is complete. The only remaining blocker before smoke testing is the worklets clang++ OOM. The fix is well understood: close Android Studio and set CMAKE_BUILD_PARALLEL_LEVEL=1. Once the build succeeds, the smoke test is a quick 5–10 min walk-through.

After smoke test (Phase 10), Phases 11–13 are collaborative UAT — user taps through each screen group, reports pass/fail, AI fixes failures immediately. These phases have no code work pending and can proceed immediately after the build.

Build command for next session:
```bash
cd /c/vv/VoltVenture
CMAKE_BUILD_PARALLEL_LEVEL=1 JAVA_TOOL_OPTIONS="--enable-native-access=ALL-UNNAMED" npx expo run:android
```
</context>

<next_action>
1. Close Android Studio (emulator can stay running)
2. Confirm `adb devices` shows `device`
3. Run: `cd /c/vv/VoltVenture && CMAKE_BUILD_PARALLEL_LEVEL=1 JAVA_TOOL_OPTIONS="--enable-native-access=ALL-UNNAMED" npx expo run:android`
4. Walk smoke test checklists once APK is installed
5. Proceed through Phases 11–13 UAT collaboratively
</next_action>
